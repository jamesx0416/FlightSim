import { TempNode, NodeBuilder, TextureNode } from 'three/webgpu';
import { Node } from '@takram/three-geospatial/webgpu';
import { AtmosphereContextNode } from './AtmosphereContextNode';
export declare class MoonNode extends TempNode {
    static get type(): string;
    private readonly atmosphereContext;
    rayDirectionECEF?: Node;
    colorNode?: TextureNode | null;
    normalNode?: TextureNode | null;
    angularRadius: import('three/webgpu').UniformNode<number>;
    intensity: import('three/webgpu').UniformNode<number>;
    constructor(atmosphereContext: AtmosphereContextNode);
    setup(builder: NodeBuilder): unknown;
}
