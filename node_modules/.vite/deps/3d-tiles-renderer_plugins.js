import {
  at,
  ct,
  it,
  lt,
  rt,
  v,
  w,
  x
} from "./chunk-XXG7WX7C.js";
import {
  FullScreenQuad
} from "./chunk-EMQHGZIF.js";
import {
  $t,
  C,
  G,
  GLTFLoader,
  Mt,
  N,
  Nt,
  U,
  ft,
  jt,
  z
} from "./chunk-W6EXJ5YZ.js";
import {
  u,
  y
} from "./chunk-DV4HTFPB.js";
import {
  BatchedMesh,
  Box2,
  Box3,
  Box3Helper,
  BoxGeometry,
  BufferAttribute,
  BufferGeometry,
  CanvasTexture,
  Color,
  CustomBlending,
  DataTexture,
  DefaultLoadingManager,
  DoubleSide,
  EdgesGeometry,
  FileLoader,
  Group,
  LineBasicMaterial,
  LineSegments,
  LinearFilter,
  LinearMipMapLinearFilter,
  MathUtils,
  Matrix2,
  Matrix3,
  Matrix4,
  Mesh,
  MeshBasicMaterial,
  MeshStandardMaterial,
  OneFactor,
  PlaneGeometry,
  PointsMaterial,
  Quaternion,
  REVISION,
  RGFormat,
  Ray,
  Raycaster,
  SRGBColorSpace,
  ShaderMaterial,
  Source,
  Sphere,
  Texture,
  TextureUtils,
  Triangle,
  UnsignedByteType,
  Vector2,
  Vector3,
  Vector4,
  WebGLArrayRenderTarget,
  WebGLRenderTarget,
  WebGLRenderer,
  ZeroFactor
} from "./chunk-3CCQRIJC.js";
import "./chunk-G3PMV62Z.js";

// node_modules/3d-tiles-renderer/build/WMSCapabilitiesLoader-BJRXfwtc.js
var fe = new Vector2();
var ye = /* @__PURE__ */ Symbol("TILE_X");
var xe = /* @__PURE__ */ Symbol("TILE_Y");
var ae = /* @__PURE__ */ Symbol("TILE_LEVEL");
var dn = class {
  get tiling() {
    return this.imageSource.tiling;
  }
  constructor(e = {}) {
    const {
      pixelSize: t = null,
      center: s = false,
      useRecommendedSettings: n = true,
      imageSource: i = null
    } = e;
    this.priority = -10, this.tiles = null, this.imageSource = i, this.pixelSize = t, this.center = s, this.useRecommendedSettings = n, t !== null && console.warn('ImageFormatPlugin: "pixelSize" has been deprecated in favor of scaling the tiles root.');
  }
  // Plugin functions
  init(e) {
    this.useRecommendedSettings && (e.errorTarget = 1), this.tiles = e, this.imageSource.fetchOptions = e.fetchOptions, this.imageSource.fetchData = (t, s) => (e.invokeAllPlugins((n) => t = n.preprocessURL ? n.preprocessURL(t, null) : t), e.invokeOnePlugin((n) => n !== this && n.fetchData && n.fetchData(t, s)));
  }
  async loadRootTileset() {
    const { tiles: e, imageSource: t } = this;
    return t.url = t.url || e.rootURL, e.invokeAllPlugins((s) => t.url = s.preprocessURL ? s.preprocessURL(t.url, null) : t.url), await t.init(), e.rootURL = t.url, this.getTileset(t.url);
  }
  async parseToMesh(e, t, s, n, i) {
    if (i.aborted)
      return null;
    const { imageSource: r } = this, o = t[ye], l2 = t[xe], c = t[ae], u2 = await r.processBufferToTexture(e);
    if (i.aborted)
      return u2.dispose(), u2.image.close(), null;
    r.setData(o, l2, c, u2);
    let h = 1, d = 1, m = 0, f = 0, p = 0;
    const g = t.boundingVolume.box;
    g && ([m, f, p] = g, h = g[3], d = g[7]);
    const y2 = new PlaneGeometry(2 * h, 2 * d), x2 = new Mesh(y2, new MeshBasicMaterial({ map: u2, transparent: true }));
    x2.position.set(m, f, p);
    const T = r.tiling.getTileContentUVBounds(o, l2, c), { uv: S } = y2.attributes;
    for (let _ = 0; _ < S.count; _++)
      fe.fromBufferAttribute(S, _), fe.x = MathUtils.mapLinear(fe.x, 0, 1, T[0], T[2]), fe.y = MathUtils.mapLinear(fe.y, 0, 1, T[1], T[3]), S.setXY(_, fe.x, fe.y);
    return x2;
  }
  preprocessNode(e) {
    const { tiling: t } = this, s = t.maxLevel;
    e[ae] < s && e.parent !== null && this.expandChildren(e);
  }
  disposeTile(e) {
    const t = e[ye], s = e[xe], n = e[ae], { imageSource: i } = this;
    i.has(t, s, n) && i.release(t, s, n);
  }
  // Local functions
  getTileset(e) {
    const { tiling: t, tiles: s } = this, n = t.minLevel, { tileCountX: i, tileCountY: r } = t.getLevel(n), o = [];
    for (let c = 0; c < i; c++)
      for (let u2 = 0; u2 < r; u2++) {
        const h = this.createChild(c, u2, n);
        h !== null && o.push(h);
      }
    const l2 = {
      asset: {
        version: "1.1"
      },
      geometricError: 1e5,
      root: {
        refine: "REPLACE",
        geometricError: 1e5,
        boundingVolume: this.createBoundingVolume(0, 0, -1),
        children: o,
        [ae]: -1,
        [ye]: 0,
        [xe]: 0
      }
    };
    return s.preprocessTileset(l2, e), l2;
  }
  getUrl(e, t, s) {
    return this.imageSource.getUrl(e, t, s);
  }
  createBoundingVolume(e, t, s) {
    const { center: n, pixelSize: i, tiling: r } = this, { pixelWidth: o, pixelHeight: l2 } = r.getLevel(r.maxLevel), [c, u2, h, d] = s === -1 ? r.getContentBounds(true) : r.getTileBounds(e, t, s, true);
    let m = (h - c) / 2, f = (d - u2) / 2, p = c + m, g = u2 + f;
    return n && (p -= 0.5, g -= 0.5), i ? (p *= o * i, m *= o * i, g *= l2 * i, f *= l2 * i) : (p *= r.aspectRatio, m *= r.aspectRatio), {
      box: [
        // center
        p,
        g,
        0,
        // x, y, z half vectors
        m,
        0,
        0,
        0,
        f,
        0,
        0,
        0,
        0
      ]
    };
  }
  createChild(e, t, s) {
    const { pixelSize: n, tiling: i } = this;
    if (!i.getTileExists(e, t, s))
      return null;
    const { pixelWidth: r, pixelHeight: o } = i.getLevel(s);
    let l2 = Math.max(i.aspectRatio / r, 1 / o);
    if (n) {
      const c = i.getLevel(i.maxLevel);
      l2 *= n * Math.max(c.pixelWidth, c.pixelHeight);
    }
    return {
      refine: "REPLACE",
      geometricError: l2,
      boundingVolume: this.createBoundingVolume(e, t, s),
      content: {
        uri: this.getUrl(e, t, s)
      },
      children: [],
      // save the tile params so we can expand later
      [ye]: e,
      [xe]: t,
      [ae]: s
    };
  }
  expandChildren(e) {
    const t = e[ae], s = e[ye], n = e[xe], { tileSplitX: i, tileSplitY: r } = this.tiling.getLevel(t);
    for (let o = 0; o < i; o++)
      for (let l2 = 0; l2 < r; l2++) {
        const c = this.createChild(i * s + o, r * n + l2, t + 1);
        c && e.children.push(c);
      }
  }
};
var pt = new Vector3();
var Ge = new Vector3();
function bi(a, e, t) {
  const n = t + 1e-5;
  let i = e + 1e-5;
  Math.abs(i) > Math.PI / 2 && (i = i - 1e-5), a.getCartographicToPosition(e, t, 0, pt), a.getCartographicToPosition(i, t, 0, Ge);
  const r = pt.distanceTo(Ge) / 1e-5;
  return a.getCartographicToPosition(e, n, 0, Ge), [pt.distanceTo(Ge) / 1e-5, r];
}
var _i = 30;
var Si = 15;
var ft2 = new Vector3();
var ss = new Vector3();
var ie = new Vector2();
var mt = new Sphere();
var ct2 = class extends dn {
  get projection() {
    return this.tiling.projection;
  }
  constructor(e = {}) {
    const {
      shape: t = "planar",
      endCaps: s = true,
      ...n
    } = e;
    super(n), this.shape = t, this.endCaps = s;
  }
  // override the parse to mesh logic to support a region mesh
  async parseToMesh(e, t, ...s) {
    const n = await super.parseToMesh(e, t, ...s), { shape: i, projection: r, tiles: o, tiling: l2 } = this;
    if (i === "ellipsoid") {
      const c = o.ellipsoid, u2 = t[ae], h = t[ye], d = t[xe], [m, f, p, g] = t.boundingVolume.region, y2 = Math.ceil((g - f) * MathUtils.RAD2DEG * 0.25), x2 = Math.ceil((p - m) * MathUtils.RAD2DEG * 0.25), M = Math.max(Si, y2), T = Math.max(_i, x2), S = new PlaneGeometry(1, 1, T, M), [_, C2, L, P] = l2.getTileBounds(h, d, u2, true, true), R = l2.getTileContentUVBounds(h, d, u2), { position: V, normal: k, uv: W } = S.attributes, $ = V.count;
      t.engineData.boundingVolume.getSphere(mt);
      for (let H = 0; H < $; H++) {
        ft2.fromBufferAttribute(V, H), ie.fromBufferAttribute(W, H);
        const I = r.convertNormalizedToLongitude(MathUtils.mapLinear(ie.x, 0, 1, _, L));
        let A = r.convertNormalizedToLatitude(MathUtils.mapLinear(ie.y, 0, 1, C2, P));
        if (r.isMercator && this.endCaps && (P === 1 && ie.y === 1 && (A = Math.PI / 2), C2 === 0 && ie.y === 0 && (A = -Math.PI / 2)), r.isMercator && ie.y !== 0 && ie.y !== 1) {
          const v2 = r.convertNormalizedToLatitude(1), N2 = 1 / M, Q = MathUtils.mapLinear(ie.y - N2, 0, 1, f, g), F = MathUtils.mapLinear(ie.y + N2, 0, 1, f, g);
          A > v2 && Q < v2 && (A = v2), A < -v2 && F > -v2 && (A = -v2);
        }
        c.getCartographicToPosition(A, I, 0, ft2).sub(mt.center), c.getCartographicToNormal(A, I, ss);
        const D = MathUtils.mapLinear(r.convertLongitudeToNormalized(I), _, L, R[0], R[2]), O = MathUtils.mapLinear(r.convertLatitudeToNormalized(A), C2, P, R[1], R[3]);
        W.setXY(H, D, O), V.setXYZ(H, ...ft2), k.setXYZ(H, ...ss);
      }
      n.geometry = S, n.position.copy(mt.center);
    }
    return n;
  }
  createBoundingVolume(e, t, s) {
    if (this.shape === "ellipsoid") {
      const { tiling: n, endCaps: i } = this, r = s === -1, o = r ? n.getContentBounds(true) : n.getTileBounds(e, t, s, true, true), l2 = r ? n.getContentBounds() : n.getTileBounds(e, t, s, false, true);
      return i && (o[3] === 1 && (l2[3] = Math.PI / 2), o[1] === 0 && (l2[1] = -Math.PI / 2)), {
        region: [...l2, -1, 1]
      };
    } else
      return super.createBoundingVolume(e, t, s);
  }
  createChild(...e) {
    const t = super.createChild(...e), { shape: s, projection: n, tiling: i } = this;
    if (t && s === "ellipsoid") {
      const r = t[ae], o = t[ye], l2 = t[xe];
      if (r === -1)
        return t.geometricError = 1e50, parent;
      const [c, u2, h, d] = i.getTileBounds(o, l2, r, true), { tilePixelWidth: m, tilePixelHeight: f } = i.getLevel(r), p = (h - c) / m, g = (d - u2) / f, [
        /* west */
        ,
        y2,
        x2,
        M
      ] = i.getTileBounds(o, l2, r), T = y2 > 0 != M > 0 ? 0 : Math.min(Math.abs(y2), Math.abs(M)), S = n.convertLatitudeToNormalized(T), _ = n.getLongitudeDerivativeAtNormalized(c), C2 = n.getLatitudeDerivativeAtNormalized(S), [L, P] = bi(this.tiles.ellipsoid, T, x2), R = Math.max(p * _ * L, g * C2 * P);
      t.geometricError = R;
    }
    return t;
  }
};
var re = class {
  get isMercator() {
    return this.scheme === "EPSG:3857";
  }
  constructor(e = "EPSG:4326") {
    this.scheme = e, this.tileCountX = 1, this.tileCountY = 1, this.setScheme(e);
  }
  setScheme(e) {
    switch (this.scheme = e, e) {
      // equirect
      case "CRS:84":
      case "EPSG:4326":
        this.tileCountX = 2, this.tileCountY = 1;
        break;
      // mercator
      case "EPSG:3857":
        this.tileCountX = 1, this.tileCountY = 1;
        break;
      case "none":
        this.tileCountX = 1, this.tileCountY = 1;
        break;
      default:
        throw new Error(`ProjectionScheme: Unknown projection scheme "${e}"`);
    }
  }
  convertNormalizedToLatitude(e) {
    if (this.scheme === "none")
      return e;
    if (this.isMercator) {
      const t = MathUtils.mapLinear(e, 0, 1, -1, 1);
      return 2 * Math.atan(Math.exp(t * Math.PI)) - Math.PI / 2;
    } else
      return MathUtils.mapLinear(e, 0, 1, -Math.PI / 2, Math.PI / 2);
  }
  convertNormalizedToLongitude(e) {
    return this.scheme === "none" ? e : MathUtils.mapLinear(e, 0, 1, -Math.PI, Math.PI);
  }
  convertLatitudeToNormalized(e) {
    if (this.scheme === "none")
      return e;
    if (this.isMercator) {
      const t = Math.log(Math.tan(Math.PI / 4 + e / 2));
      return 1 / 2 + 1 * t / (2 * Math.PI);
    } else
      return MathUtils.mapLinear(e, -Math.PI / 2, Math.PI / 2, 0, 1);
  }
  convertLongitudeToNormalized(e) {
    return this.scheme === "none" ? e : (e + Math.PI) / (2 * Math.PI);
  }
  getLongitudeDerivativeAtNormalized(e) {
    return this.scheme === "none" ? 1 : 2 * Math.PI;
  }
  getLatitudeDerivativeAtNormalized(e) {
    if (this.scheme === "none")
      return 1;
    {
      let s = e - 1e-5;
      return s < 0 && (s = e + 1e-5), this.isMercator ? Math.abs(this.convertNormalizedToLatitude(e) - this.convertNormalizedToLatitude(s)) / 1e-5 : Math.PI;
    }
  }
  getBounds() {
    return this.scheme === "none" ? [0, 0, 1, 1] : [
      this.convertNormalizedToLongitude(0),
      this.convertNormalizedToLatitude(0),
      this.convertNormalizedToLongitude(1),
      this.convertNormalizedToLatitude(1)
    ];
  }
  toNormalizedPoint(e, t) {
    const s = [e, t];
    return s[0] = this.convertLongitudeToNormalized(s[0]), s[1] = this.convertLatitudeToNormalized(s[1]), s;
  }
  toNormalizedRange(e) {
    return [
      ...this.toNormalizedPoint(e[0], e[1]),
      ...this.toNormalizedPoint(e[2], e[3])
    ];
  }
  toCartographicPoint(e, t) {
    const s = [e, t];
    return s[0] = this.convertNormalizedToLongitude(s[0]), s[1] = this.convertNormalizedToLatitude(s[1]), s;
  }
  toCartographicRange(e) {
    return [
      ...this.toCartographicPoint(e[0], e[1]),
      ...this.toCartographicPoint(e[2], e[3])
    ];
  }
  clampToBounds(e, t = false) {
    const s = [...e];
    let n;
    t ? n = [0, 0, 1, 1] : n = this.getBounds();
    const [i, r, o, l2] = n;
    return s[0] = MathUtils.clamp(s[0], i, o), s[1] = MathUtils.clamp(s[1], r, l2), s[2] = MathUtils.clamp(s[2], i, o), s[3] = MathUtils.clamp(s[3], r, l2), s;
  }
};
function Ae(...a) {
  return a.join("_");
}
var pn = class {
  constructor() {
    this.cache = {}, this.count = 0, this.cachedBytes = 0, this.active = 0;
  }
  // overridable
  fetchItem() {
  }
  disposeItem() {
  }
  getMemoryUsage(e) {
    return 0;
  }
  // sets the data in the cache explicitly without need to load
  setData(...e) {
    const { cache: t } = this, s = e.pop(), n = Ae(...e);
    if (n in t)
      throw new Error(`DataCache: "${n}" is already present.`);
    return this.cache[n] = {
      abortController: new AbortController(),
      result: s,
      count: 1,
      bytes: this.getMemoryUsage(s)
    }, this.count++, this.cachedBytes += this.cache[n].bytes, s;
  }
  // fetches the associated data if it doesn't exist and increments the lock counter
  lock(...e) {
    const { cache: t } = this, s = Ae(...e);
    if (s in t)
      t[s].count++;
    else {
      const n = new AbortController(), i = {
        abortController: n,
        result: null,
        count: 1,
        bytes: 0,
        args: e
      };
      this.active++, i.result = this.fetchItem(e, n.signal), i.result instanceof Promise ? i.result.then((r) => (i.result = r, i.bytes = this.getMemoryUsage(r), this.cachedBytes += i.bytes, r)).finally(() => {
        this.active--;
      }).catch((r) => {
      }) : (this.active--, i.bytes = this.getMemoryUsage(i.result), this.cachedBytes += i.bytes), this.cache[s] = i, this.count++;
    }
    return t[s].result;
  }
  // decrements the lock counter for the item and deletes the item if it has reached zero
  release(...e) {
    const t = Ae(...e);
    this.releaseViaFullKey(t);
  }
  // get the loaded item
  get(...e) {
    const { cache: t } = this, s = Ae(...e);
    return s in t && t[s].count > 0 ? t[s].result : null;
  }
  has(...e) {
    const { cache: t } = this;
    return Ae(...e) in t;
  }
  forEachItem(e) {
    const { cache: t } = this;
    for (const s in t) {
      const n = t[s];
      n.result instanceof Promise || e(n.result, n.args);
    }
  }
  // dispose all items
  dispose() {
    const { cache: e } = this;
    for (const t in e) {
      const { abortController: s } = e[t];
      s.abort(), this.releaseViaFullKey(t, true);
    }
    this.cache = {};
  }
  // releases an item with an optional force flag
  releaseViaFullKey(e, t = false) {
    const { cache: s } = this;
    if (e in s && s[e].count > 0) {
      const n = s[e];
      if (n.count--, n.count === 0 || t) {
        const i = () => {
          if (s[e] !== n)
            return;
          const { result: r, abortController: o } = n;
          o.abort(), r instanceof Promise ? r.then((l2) => {
            this.disposeItem(l2), this.count--, this.cachedBytes -= n.bytes;
          }).catch(() => {
          }) : (this.disposeItem(r), this.count--, this.cachedBytes -= n.bytes), delete s[e];
        };
        t ? i() : queueMicrotask(() => {
          n.count === 0 && i();
        });
      }
      return true;
    }
    throw new Error("DataCache: Attempting to release key that does not exist");
  }
};
function ns(a, e) {
  const [t, s, n, i] = a, [r, o, l2, c] = e;
  return !(t >= l2 || n <= r || s >= c || i <= o);
}
var fn = class {
  get levelCount() {
    return this._levels.length;
  }
  get maxLevel() {
    return this.levelCount - 1;
  }
  get minLevel() {
    const e = this._levels;
    for (let t = 0; t < e.length; t++)
      if (e[t] !== null)
        return t;
    return -1;
  }
  // prioritize user-set bounds over projection bounds if present
  get contentBounds() {
    return this._contentBounds ?? this.projection.getBounds();
  }
  get aspectRatio() {
    const { pixelWidth: e, pixelHeight: t } = this.getLevel(this.maxLevel);
    return e / t;
  }
  constructor() {
    this.flipY = false, this.pixelOverlap = 0, this._contentBounds = null, this.projection = new re("none"), this._levels = [];
  }
  // build the zoom levels
  setLevel(e, t = {}) {
    const s = this._levels;
    for (; s.length < e; )
      s.push(null);
    const {
      tileSplitX: n = 2,
      tileSplitY: i = 2
    } = t, {
      tilePixelWidth: r = 256,
      tilePixelHeight: o = 256,
      tileCountX: l2 = n ** e,
      tileCountY: c = i ** e,
      tileBounds: u2 = null
    } = t, {
      pixelWidth: h = r * l2,
      pixelHeight: d = o * c
    } = t;
    s[e] = {
      // The pixel resolution of each tile.
      tilePixelWidth: r,
      tilePixelHeight: o,
      // The total pixel resolution of the final image at this level. These numbers
      // may not be a round multiple of the tile width.
      pixelWidth: h,
      pixelHeight: d,
      // Or the total number of tiles that can be loaded at this level.
      tileCountX: l2,
      tileCountY: c,
      // The number of tiles that the tiles at this layer split in to
      tileSplitX: n,
      tileSplitY: i,
      // The bounds covered by the extent of the tiles at this loaded. The actual content covered by the overall tileset
      // may be a subset of this range (eg there may be unused space).
      tileBounds: u2
    };
  }
  generateLevels(e, t, s, n = {}) {
    const {
      minLevel: i = 0,
      tilePixelWidth: r = 256,
      tilePixelHeight: o = 256
    } = n, l2 = e - 1, {
      pixelWidth: c = r * t * 2 ** l2,
      pixelHeight: u2 = o * s * 2 ** l2
    } = n;
    for (let h = i; h < e; h++) {
      const d = e - h - 1, m = Math.ceil(c * 2 ** -d), f = Math.ceil(u2 * 2 ** -d), p = Math.ceil(m / r), g = Math.ceil(f / o);
      this.setLevel(h, {
        tilePixelWidth: r,
        tilePixelHeight: o,
        pixelWidth: m,
        pixelHeight: f,
        tileCountX: p,
        tileCountY: g
      });
    }
  }
  getLevel(e) {
    return this._levels[e];
  }
  // bounds representing the contentful region of the image
  setContentBounds(e, t, s, n) {
    this._contentBounds = [e, t, s, n];
  }
  setProjection(e) {
    this.projection = e;
  }
  // query functions
  getTileAtPoint(e, t, s, n = false) {
    const { flipY: i } = this, { tileCountX: r, tileCountY: o, tileBounds: l2 } = this.getLevel(s), c = 1 / r, u2 = 1 / o;
    if (n || ([e, t] = this.toNormalizedPoint(e, t)), l2) {
      const m = this.toNormalizedRange(l2);
      e = MathUtils.mapLinear(e, m[0], m[2], 0, 1), t = MathUtils.mapLinear(t, m[1], m[3], 0, 1);
    }
    const h = Math.floor(e / c);
    let d = Math.floor(t / u2);
    return i && (d = o - 1 - d), [h, d];
  }
  getTilesInRange(e, t, s, n, i, r = false) {
    const o = [e, t, s, n], l2 = this.getContentBounds(r);
    let c = this.getLevel(i).tileBounds;
    if (!ns(o, l2))
      return [0, 0, -1, -1];
    if (c && (r && (c = this.toNormalizedRange(c)), !ns(o, l2)))
      return [0, 0, -1, -1];
    const [u2, h, d, m] = this.clampToContentBounds(o, r), f = this.getTileAtPoint(u2, h, i, r), p = this.getTileAtPoint(d, m, i, r);
    this.flipY && ([f[1], p[1]] = [p[1], f[1]]);
    const { tileCountX: g, tileCountY: y2 } = this.getLevel(i), [x2, M] = f, [T, S] = p;
    return T < 0 || S < 0 || x2 >= g || M >= y2 ? [0, 0, -1, -1] : [
      MathUtils.clamp(x2, 0, g - 1),
      MathUtils.clamp(M, 0, y2 - 1),
      MathUtils.clamp(T, 0, g - 1),
      MathUtils.clamp(S, 0, y2 - 1)
    ];
  }
  getTileExists(e, t, s) {
    const [n, i, r, o] = this.contentBounds, [l2, c, u2, h] = this.getTileBounds(e, t, s);
    return !(l2 >= u2 || c >= h) && l2 <= r && c <= o && u2 >= n && h >= i;
  }
  getContentBounds(e = false) {
    const { projection: t } = this, s = [...this.contentBounds];
    return e && (s[0] = t.convertLongitudeToNormalized(s[0]), s[1] = t.convertLatitudeToNormalized(s[1]), s[2] = t.convertLongitudeToNormalized(s[2]), s[3] = t.convertLatitudeToNormalized(s[3])), s;
  }
  // returns the UV range associated with the content in the given tile
  getTileContentUVBounds(e, t, s) {
    const [n, i, r, o] = this.getTileBounds(e, t, s, true, true), [l2, c, u2, h] = this.getTileBounds(e, t, s, true, false);
    return [
      MathUtils.mapLinear(n, l2, u2, 0, 1),
      MathUtils.mapLinear(i, c, h, 0, 1),
      MathUtils.mapLinear(r, l2, u2, 0, 1),
      MathUtils.mapLinear(o, c, h, 0, 1)
    ];
  }
  getTileBounds(e, t, s, n = false, i = true) {
    const { flipY: r, pixelOverlap: o, projection: l2 } = this, { tilePixelWidth: c, tilePixelHeight: u2, pixelWidth: h, pixelHeight: d, tileBounds: m } = this.getLevel(s);
    let f = c * e - o, p = u2 * t - o, g = f + c + o * 2, y2 = p + u2 + o * 2;
    if (f = Math.max(f, 0), p = Math.max(p, 0), g = Math.min(g, h), y2 = Math.min(y2, d), f = f / h, g = g / h, p = p / d, y2 = y2 / d, r) {
      const M = (y2 - p) / 2, S = 1 - (p + y2) / 2;
      p = S - M, y2 = S + M;
    }
    let x2 = [f, p, g, y2];
    if (m) {
      const M = this.toNormalizedRange(m);
      x2[0] = MathUtils.mapLinear(x2[0], 0, 1, M[0], M[2]), x2[2] = MathUtils.mapLinear(x2[2], 0, 1, M[0], M[2]), x2[1] = MathUtils.mapLinear(x2[1], 0, 1, M[1], M[3]), x2[3] = MathUtils.mapLinear(x2[3], 0, 1, M[1], M[3]);
    }
    return i && (x2 = this.clampToBounds(x2, true)), n || (x2[0] = l2.convertNormalizedToLongitude(x2[0]), x2[1] = l2.convertNormalizedToLatitude(x2[1]), x2[2] = l2.convertNormalizedToLongitude(x2[2]), x2[3] = l2.convertNormalizedToLatitude(x2[3])), x2;
  }
  toNormalizedPoint(e, t) {
    return this.projection.toNormalizedPoint(e, t);
  }
  toNormalizedRange(e) {
    return this.projection.toNormalizedRange(e);
  }
  toCartographicPoint(e, t) {
    return this.projection.toCartographicPoint(e, t);
  }
  toCartographicRange(e) {
    return this.projection.toCartographicRange(e);
  }
  clampToContentBounds(e, t = false) {
    const s = [...e], [n, i, r, o] = this.getContentBounds(t);
    return s[0] = MathUtils.clamp(s[0], n, r), s[1] = MathUtils.clamp(s[1], i, o), s[2] = MathUtils.clamp(s[2], n, r), s[3] = MathUtils.clamp(s[3], i, o), s;
  }
  clampToBounds(e, t = false) {
    return this.projection.clampToBounds(e, t);
  }
};
var Ue = class extends pn {
  constructor(e = {}) {
    super();
    const {
      fetchOptions: t = {}
    } = e;
    this.tiling = new fn(), this.fetchOptions = t, this.fetchData = (...s) => fetch(...s);
  }
  // async function for initializing the tiled image set
  init() {
  }
  // helper for processing the buffer into a texture
  async processBufferToTexture(e) {
    const t = new Blob([e]), s = await createImageBitmap(t, {
      premultiplyAlpha: "none",
      colorSpaceConversion: "none",
      imageOrientation: "flipY"
    }), n = new Texture(s);
    return n.generateMipmaps = false, n.colorSpace = SRGBColorSpace, n.needsUpdate = true, n;
  }
  getMemoryUsage(e) {
    const { format: t, type: s, image: n, generateMipmaps: i } = e, { width: r, height: o } = n, l2 = TextureUtils.getByteLength(r, o, t, s);
    return i ? l2 * 4 / 3 : l2;
  }
  // fetch the item with the given key fields
  fetchItem(e, t) {
    const s = {
      ...this.fetchOptions,
      signal: t
    }, n = this.getUrl(...e);
    return this.fetchData(n, s).then((i) => i.arrayBuffer()).then((i) => this.processBufferToTexture(i));
  }
  // dispose of the item that was fetched
  disposeItem(e) {
    e.dispose(), e.image instanceof ImageBitmap && e.image.close();
  }
  getUrl(...e) {
  }
};
var Ne = class extends Ue {
  constructor(e = {}) {
    const {
      levels: t = 20,
      tileDimension: s = 256,
      projection: n = "EPSG:3857",
      url: i = null,
      ...r
    } = e;
    super(r), this.tileDimension = s, this.levels = t, this.projection = n, this.url = i;
  }
  getUrl(e, t, s) {
    return this.url.replace(/{\s*z\s*}/gi, s).replace(/{\s*x\s*}/gi, e).replace(/{\s*(y|reverseY|-\s*y)\s*}/gi, t);
  }
  init() {
    const { tiling: e, tileDimension: t, levels: s, url: n, projection: i } = this;
    return e.flipY = !/{\s*reverseY|-\s*y\s*}/g.test(n), e.setProjection(new re(i)), e.setContentBounds(...e.projection.getBounds()), Array.isArray(s) ? s.forEach((r, o) => {
      r !== null && e.setLevel(o, {
        tilePixelWidth: t,
        tilePixelHeight: t,
        ...r
      });
    }) : e.generateLevels(s, e.projection.tileCountX, e.projection.tileCountY, {
      tilePixelWidth: t,
      tilePixelHeight: t
    }), this.url = n, Promise.resolve();
  }
};
var Xt = class extends Ue {
  constructor(e = {}) {
    const {
      url: t = null,
      ...s
    } = e;
    super(s), this.tileSets = null, this.extension = null, this.url = t;
  }
  getUrl(e, t, s) {
    const { url: n, extension: i, tileSets: r, tiling: o } = this;
    return new URL(`${parseInt(r[s - o.minLevel].href)}/${e}/${t}.${i}`, n).toString();
  }
  init() {
    const { url: e } = this;
    return this.fetchData(new URL("tilemapresource.xml", e), this.fetchOptions).then((t) => t.text()).then((t) => {
      const { tiling: s } = this, n = new DOMParser().parseFromString(t, "text/xml"), i = n.querySelector("BoundingBox"), r = n.querySelector("TileFormat"), l2 = [...n.querySelector("TileSets").querySelectorAll("TileSet")].map((y2) => ({
        href: parseInt(y2.getAttribute("href")),
        unitsPerPixel: parseFloat(y2.getAttribute("units-per-pixel")),
        order: parseInt(y2.getAttribute("order"))
      })).sort((y2, x2) => y2.order - x2.order), c = parseFloat(i.getAttribute("minx")) * MathUtils.DEG2RAD, u2 = parseFloat(i.getAttribute("maxx")) * MathUtils.DEG2RAD, h = parseFloat(i.getAttribute("miny")) * MathUtils.DEG2RAD, d = parseFloat(i.getAttribute("maxy")) * MathUtils.DEG2RAD, m = parseInt(r.getAttribute("width")), f = parseInt(r.getAttribute("height")), p = r.getAttribute("extension"), g = n.querySelector("SRS").textContent;
      this.extension = p, this.url = e, this.tileSets = l2, s.setProjection(new re(g)), s.setContentBounds(c, h, u2, d), l2.forEach(({ order: y2 }) => {
        s.setLevel(y2, {
          tileCountX: s.projection.tileCountX * 2 ** y2,
          tilePixelWidth: m,
          tilePixelHeight: f
        });
      });
    });
  }
};
function Mi(a) {
  return /(:84|:crs84)$/i.test(a);
}
var mn = class extends Ue {
  constructor(e = {}) {
    const {
      capabilities: t = null,
      layer: s = null,
      tileMatrixSet: n = null,
      style: i = null,
      url: r = null,
      dimensions: o = {},
      ...l2
    } = e;
    super(l2), this.capabilities = t, this.layer = s, this.tileMatrixSet = n, this.style = i, this.dimensions = o, this.url = r;
  }
  getUrl(e, t, s) {
    return this.url.replace(/{\s*TileMatrix\s*}/gi, s).replace(/{\s*TileCol\s*}/gi, e).replace(/{\s*TileRow\s*}/gi, t);
  }
  init() {
    const { tiling: e, dimensions: t, capabilities: s } = this;
    let { layer: n, tileMatrixSet: i, style: r, url: o } = this;
    n ? typeof n == "string" && (n = s.layers.find((u2) => u2.identifier === n)) : n = s.layers[0], i ? typeof i == "string" && (i = n.tileMatrixSets.find((u2) => u2.identifier === i)) : i = n.tileMatrixSets[0], r || (r = n.styles.find((u2) => u2.isDefault).identifier), o || (o = n.resourceUrls[0].template);
    const l2 = i.supportedCRS, c = l2.includes("4326") || Mi(l2) ? "EPSG:4326" : "EPSG:3857";
    e.flipY = true, e.setProjection(new re(c)), n.boundingBox !== null ? e.setContentBounds(...n.boundingBox.bounds) : e.setContentBounds(...e.projection.getBounds()), i.tileMatrices.forEach((u2, h) => {
      const { tileWidth: d, tileHeight: m, matrixWidth: f, matrixHeight: p } = u2;
      e.setLevel(h, {
        tilePixelWidth: d,
        tilePixelHeight: m,
        tileCountX: f || e.projection.tileCountX * 2 ** h,
        tileCountY: p || e.projection.tileCountY * 2 ** h,
        tileBounds: u2.bounds
      });
    }), o = o.replace(/{\s*TileMatrixSet\s*}/g, i.identifier).replace(/{\s*Style\s*}/g, r);
    for (const u2 in t)
      o = o.replace(new RegExp(`{\\s*${u2}\\s*}`), t[u2]);
    return n.dimensions.forEach((u2) => {
      o = o.replace(new RegExp(`{\\s*${u2.identifier}\\s*}`), u2.defaultValue);
    }), this.url = o, Promise.resolve();
  }
};
var gn = class extends Ue {
  // TODO: layer and styles can be arrays, comma separated lists
  constructor(e = {}) {
    const {
      url: t = null,
      layer: s = null,
      styles: n = null,
      contentBoundingBox: i = null,
      version: r = "1.3.0",
      crs: o = "EPSG:4326",
      format: l2 = "image/png",
      transparent: c = false,
      levels: u2 = 18,
      tileDimension: h = 256,
      ...d
    } = e;
    super(d), this.url = t, this.layer = s, this.crs = o, this.format = l2, this.tileDimension = h, this.styles = n, this.version = r, this.levels = u2, this.transparent = c, this.contentBoundingBox = i;
  }
  init() {
    const { tiling: e, levels: t, tileDimension: s, contentBoundingBox: n } = this;
    return e.setProjection(new re(this.crs)), e.flipY = true, e.generateLevels(t, e.projection.tileCountX, e.projection.tileCountY, {
      tilePixelWidth: s,
      tilePixelHeight: s
    }), n !== null ? e.setContentBounds(...n) : e.setContentBounds(...e.projection.getBounds()), Promise.resolve();
  }
  // TODO: handle this in ProjectionScheme or TilingScheme? Or Loader?
  normalizedToMercatorX(e) {
    return MathUtils.mapLinear(e, 0, 1, -20037508342789244e-9, 20037508342789244e-9);
  }
  normalizedToMercatorY(e) {
    return MathUtils.mapLinear(e, 0, 1, -20037508342789244e-9, 20037508342789244e-9);
  }
  getUrl(e, t, s) {
    const {
      tiling: n,
      layer: i,
      crs: r,
      format: o,
      tileDimension: l2,
      styles: c,
      version: u2,
      transparent: h
    } = this, d = u2 === "1.1.1" ? "SRS" : "CRS";
    let m;
    if (r === "EPSG:3857") {
      const p = n.getTileBounds(e, t, s, true, false), g = this.normalizedToMercatorX(p[0]), y2 = this.normalizedToMercatorY(p[1]), x2 = this.normalizedToMercatorX(p[2]), M = this.normalizedToMercatorY(p[3]);
      m = [g, y2, x2, M];
    } else {
      const [p, g, y2, x2] = n.getTileBounds(e, t, s, false, false).map((M) => M * MathUtils.RAD2DEG);
      r === "EPSG:4326" ? u2 === "1.1.1" ? m = [p, g, y2, x2] : m = [g, p, x2, y2] : m = [p, g, y2, x2];
    }
    const f = new URLSearchParams({
      SERVICE: "WMS",
      REQUEST: "GetMap",
      VERSION: u2,
      LAYERS: i,
      [d]: r,
      BBOX: m.join(","),
      WIDTH: l2,
      HEIGHT: l2,
      FORMAT: o,
      TRANSPARENT: h ? "TRUE" : "FALSE"
    });
    return c != null && f.set("STYLES", c), new URL("?" + f.toString(), this.url).toString();
  }
};
var eo = class extends ct2 {
  constructor(e = {}) {
    const {
      levels: t,
      tileDimension: s,
      projection: n,
      url: i,
      ...r
    } = e;
    super(r), this.name = "XYZ_TILES_PLUGIN", this.imageSource = new Ne({ url: i, levels: t, tileDimension: s, projection: n });
  }
};
var Ci = class extends ct2 {
  constructor(e = {}) {
    const { url: t, ...s } = e;
    super(s), this.name = "TMS_TILES_PLUGIN", this.imageSource = new Xt({ url: t });
  }
};
var to = class extends ct2 {
  constructor(e = {}) {
    const {
      capabilities: t,
      layer: s,
      tileMatrixSet: n,
      style: i,
      dimensions: r,
      ...o
    } = e;
    super(o), this.name = "WTMS_TILES_PLUGIN", this.imageSource = new mn({
      capabilities: t,
      layer: s,
      tileMatrixSet: n,
      style: i,
      dimensions: r
    });
  }
};
var so = class extends ct2 {
  constructor(e = {}) {
    const {
      url: t,
      layer: s,
      crs: n,
      format: i,
      tileDimension: r,
      styles: o,
      version: l2,
      ...c
    } = e;
    super(c), this.name = "WMS_TILES_PLUGIN", this.imageSource = new gn({
      url: t,
      layer: s,
      crs: n,
      format: i,
      tileDimension: r,
      styles: o,
      version: l2
    });
  }
};
var is = new Vector3();
var ze = new Triangle();
var U2 = new Vector3();
var oe = new Vector3();
var Ai = class extends ct {
  constructor(e = DefaultLoadingManager) {
    super(), this.manager = e, this.ellipsoid = new Mt(), this.skirtLength = 1e3, this.smoothSkirtNormals = true, this.generateNormals = true, this.solid = false, this.minLat = -Math.PI / 2, this.maxLat = Math.PI / 2, this.minLon = -Math.PI, this.maxLon = Math.PI;
  }
  parse(e) {
    const {
      ellipsoid: t,
      solid: s,
      skirtLength: n,
      smoothSkirtNormals: i,
      generateNormals: r,
      minLat: o,
      maxLat: l2,
      minLon: c,
      maxLon: u2
    } = this, {
      header: h,
      indices: d,
      vertexData: m,
      edgeIndices: f,
      extensions: p
    } = super.parse(e), g = new BufferGeometry(), y2 = new MeshStandardMaterial(), x2 = new Mesh(g, y2);
    x2.position.set(...h.center);
    const M = "octvertexnormals" in p, T = M || r, S = m.u.length, _ = [], C2 = [], L = [], P = [];
    let R = 0, V = 0;
    for (let I = 0; I < S; I++)
      W(I, U2), $(U2.x, U2.y, U2.z, oe), C2.push(U2.x, U2.y), _.push(...oe);
    for (let I = 0, A = d.length; I < A; I++)
      L.push(d[I]);
    if (T)
      if (M) {
        const I = p.octvertexnormals.normals;
        for (let A = 0, D = I.length; A < D; A++)
          P.push(I[A]);
      } else {
        const I = new BufferGeometry(), A = d.length > 21845 ? new Uint32Array(d) : new Uint16Array(d);
        I.setIndex(new BufferAttribute(A, 1, false)), I.setAttribute("position", new BufferAttribute(new Float32Array(_), 3, false)), I.computeVertexNormals();
        const O = I.getAttribute("normal").array;
        p.octvertexnormals = { normals: O };
        for (let v2 = 0, N2 = O.length; v2 < N2; v2++)
          P.push(O[v2]);
      }
    if (g.addGroup(R, d.length, V), R += d.length, V++, s) {
      const I = _.length / 3;
      for (let A = 0; A < S; A++)
        W(A, U2), $(U2.x, U2.y, U2.z, oe, -n), C2.push(U2.x, U2.y), _.push(...oe);
      for (let A = d.length - 1; A >= 0; A--)
        L.push(d[A] + I);
      if (T) {
        const A = p.octvertexnormals.normals;
        for (let D = 0, O = A.length; D < O; D++)
          P.push(-A[D]);
      }
      g.addGroup(R, d.length, V), R += d.length, V++;
    }
    if (n > 0) {
      const {
        westIndices: I,
        eastIndices: A,
        southIndices: D,
        northIndices: O
      } = f;
      let v2;
      const N2 = H(I);
      v2 = _.length / 3, C2.push(...N2.uv), _.push(...N2.positions);
      for (let B = 0, Z = N2.indices.length; B < Z; B++)
        L.push(N2.indices[B] + v2);
      const Q = H(A);
      v2 = _.length / 3, C2.push(...Q.uv), _.push(...Q.positions);
      for (let B = 0, Z = Q.indices.length; B < Z; B++)
        L.push(Q.indices[B] + v2);
      const F = H(D);
      v2 = _.length / 3, C2.push(...F.uv), _.push(...F.positions);
      for (let B = 0, Z = F.indices.length; B < Z; B++)
        L.push(F.indices[B] + v2);
      const G2 = H(O);
      v2 = _.length / 3, C2.push(...G2.uv), _.push(...G2.positions);
      for (let B = 0, Z = G2.indices.length; B < Z; B++)
        L.push(G2.indices[B] + v2);
      T && (P.push(...N2.normals), P.push(...Q.normals), P.push(...F.normals), P.push(...G2.normals)), g.addGroup(R, d.length, V), R += d.length, V++;
    }
    for (let I = 0, A = _.length; I < A; I += 3)
      _[I + 0] -= h.center[0], _[I + 1] -= h.center[1], _[I + 2] -= h.center[2];
    const k = _.length / 3 > 65535 ? new Uint32Array(L) : new Uint16Array(L);
    if (g.setIndex(new BufferAttribute(k, 1, false)), g.setAttribute("position", new BufferAttribute(new Float32Array(_), 3, false)), g.setAttribute("uv", new BufferAttribute(new Float32Array(C2), 2, false)), T && g.setAttribute("normal", new BufferAttribute(new Float32Array(P), 3, false)), "watermask" in p) {
      const { mask: I, size: A } = p.watermask, D = new Uint8Array(2 * A * A);
      for (let v2 = 0, N2 = I.length; v2 < N2; v2++) {
        const Q = I[v2] === 255 ? 0 : 255;
        D[2 * v2 + 0] = Q, D[2 * v2 + 1] = Q;
      }
      const O = new DataTexture(D, A, A, RGFormat, UnsignedByteType);
      O.flipY = true, O.minFilter = LinearMipMapLinearFilter, O.magFilter = LinearFilter, O.needsUpdate = true, y2.roughnessMap = O;
    }
    return x2.userData.minHeight = h.minHeight, x2.userData.maxHeight = h.maxHeight, "metadata" in p && (x2.userData.metadata = p.metadata.json), x2;
    function W(I, A) {
      return A.x = m.u[I], A.y = m.v[I], A.z = m.height[I], A;
    }
    function $(I, A, D, O, v2 = 0) {
      const N2 = MathUtils.lerp(h.minHeight, h.maxHeight, D), Q = MathUtils.lerp(c, u2, I), F = MathUtils.lerp(o, l2, A);
      return t.getCartographicToPosition(F, Q, N2 + v2, O), O;
    }
    function H(I) {
      const A = [], D = [], O = [], v2 = [], N2 = [];
      for (let G2 = 0, B = I.length; G2 < B; G2++)
        W(I[G2], U2), A.push(U2.x, U2.y), O.push(U2.x, U2.y), $(U2.x, U2.y, U2.z, oe), D.push(...oe), $(U2.x, U2.y, U2.z, oe, -n), v2.push(...oe);
      const Q = I.length - 1;
      for (let G2 = 0; G2 < Q; G2++) {
        const B = G2, Z = G2 + 1, pe = G2 + I.length, ht = G2 + I.length + 1;
        N2.push(B, pe, Z), N2.push(Z, pe, ht);
      }
      let F = null;
      if (T) {
        const G2 = (D.length + v2.length) / 3;
        if (i) {
          F = new Array(G2 * 3);
          const B = p.octvertexnormals.normals, Z = F.length / 2;
          for (let pe = 0, ht = G2 / 2; pe < ht; pe++) {
            const dt = I[pe], Te = 3 * pe, Zt = B[3 * dt + 0], Jt = B[3 * dt + 1], Kt = B[3 * dt + 2];
            F[Te + 0] = Zt, F[Te + 1] = Jt, F[Te + 2] = Kt, F[Z + Te + 0] = Zt, F[Z + Te + 1] = Jt, F[Z + Te + 2] = Kt;
          }
        } else {
          F = [], ze.a.fromArray(D, 0), ze.b.fromArray(v2, 0), ze.c.fromArray(D, 3), ze.getNormal(is);
          for (let B = 0; B < G2; B++)
            F.push(...is);
        }
      }
      return {
        uv: [...A, ...O],
        positions: [...D, ...v2],
        indices: N2,
        normals: F
      };
    }
  }
};
var z2 = 0;
var ce = ["a", "b", "c"];
var w2 = new Vector4();
var rs = new Vector4();
var os = new Vector4();
var as = new Vector4();
var yn = class {
  constructor() {
    this.attributeList = null, this.splitOperations = [], this.trianglePool = new Ii();
  }
  forEachSplitPermutation(e) {
    const { splitOperations: t } = this, s = (n = 0) => {
      if (n >= t.length) {
        e();
        return;
      }
      t[n].keepPositive = true, s(n + 1), t[n].keepPositive = false, s(n + 1);
    };
    s();
  }
  // Takes an operation that returns a value for the given vertex passed to the callback. Triangles
  // are clipped along edges where the interpolated value is equal to 0. The polygons on the positive
  // side of the operation are kept if "keepPositive" is true.
  // callback( geometry, i0, i1, i2, barycoord );
  addSplitOperation(e, t = true) {
    this.splitOperations.push({
      callback: e,
      keepPositive: t
    });
  }
  // Removes all split operations
  clearSplitOperations() {
    this.splitOperations.length = 0;
  }
  // clips an object hierarchy
  clipObject(e) {
    const t = e.clone(), s = [];
    return t.traverse((n) => {
      n.isMesh && (n.geometry = this.clip(n).geometry, (n.geometry.index ? n.geometry.index.count / 3 : n.attributes.position.count / 3) === 0 && s.push(n));
    }), s.forEach((n) => {
      n.removeFromParent();
    }), t;
  }
  // Returns a new mesh that has been clipped by the split operations. Range indicates the range of
  // elements to include when clipping.
  clip(e, t = null) {
    const s = this.getClippedData(e, t);
    return this.constructMesh(s.attributes, s.index, e);
  }
  // Appends the clip operation data to the given "target" object so multiple ranges can be appended.
  // The "target" object is returned with an "index" field, "vertexIsClipped" field, and series of arrays
  // in "attributes".
  // attributes - set of attribute arrays
  // index - triangle indices referencing vertices in attributes
  // vertexIsClipped - array indicating whether a vertex is on a clipped edge
  getClippedData(e, t = null, s = {}) {
    const { trianglePool: n, splitOperations: i, attributeList: r } = this, o = e.geometry, l2 = o.attributes.position, c = o.index;
    let u2 = 0;
    const h = {};
    s.index = s.index || [], s.vertexIsClipped = s.vertexIsClipped || [], s.attributes = s.attributes || {};
    for (const p in o.attributes) {
      if (r !== null) {
        if (r instanceof Function && !r(p))
          continue;
        if (Array.isArray(r) && !r.includes(p))
          continue;
      }
      s.attributes[p] = [];
    }
    let d = 0, m = c ? c.count : l2.count;
    t !== null && (d = t.start, m = t.count);
    for (let p = d, g = d + m; p < g; p += 3) {
      let y2 = p + 0, x2 = p + 1, M = p + 2;
      c && (y2 = c.getX(y2), x2 = c.getX(x2), M = c.getX(M));
      const T = n.get();
      T.initFromIndices(y2, x2, M);
      let S = [T];
      for (let _ = 0; _ < i.length; _++) {
        const { keepPositive: C2, callback: L } = i[_], P = [];
        for (let R = 0; R < S.length; R++) {
          const V = S[R], { indices: k, barycoord: W } = V;
          V.clipValues.a = L(o, k.a, k.b, k.c, W.a, e.matrixWorld), V.clipValues.b = L(o, k.a, k.b, k.c, W.b, e.matrixWorld), V.clipValues.c = L(o, k.a, k.b, k.c, W.c, e.matrixWorld), this.splitTriangle(V, !C2, P);
        }
        S = P;
      }
      for (let _ = 0, C2 = S.length; _ < C2; _++) {
        const L = S[_];
        f(L, o);
      }
      n.reset();
    }
    return s;
    function f(p, g) {
      for (let y2 = 0; y2 < 3; y2++) {
        const x2 = p.getVertexHash(y2, g);
        x2 in h || (h[x2] = u2, u2++, p.getVertexData(y2, g, s.attributes), s.vertexIsClipped.push(p.clipValues[ce[y2]] === z2));
        const M = h[x2];
        s.index.push(M);
      }
    }
  }
  // Takes the set of resultant data and constructs a mesh
  constructMesh(e, t, s) {
    const n = s.geometry, i = new BufferGeometry(), r = e.position.length / 3 > 65535 ? new Uint32Array(t) : new Uint16Array(t);
    i.setIndex(new BufferAttribute(r, 1, false));
    for (const l2 in e) {
      const c = n.getAttribute(l2), u2 = new c.array.constructor(e[l2]), h = new BufferAttribute(u2, c.itemSize, c.normalized);
      h.gpuType = c.gpuType, i.setAttribute(l2, h);
    }
    const o = new Mesh(i, s.material.clone());
    return o.position.copy(s.position), o.quaternion.copy(s.quaternion), o.scale.copy(s.scale), o;
  }
  // Splits the given triangle
  splitTriangle(e, t, s) {
    const { trianglePool: n } = this, i = [], r = [], o = [];
    for (let l2 = 0; l2 < 3; l2++) {
      const c = ce[l2], u2 = ce[(l2 + 1) % 3], h = e.clipValues[c], d = e.clipValues[u2];
      (h < z2 != d < z2 || h === z2) && (i.push(l2), r.push([c, u2]), h === d ? o.push(0) : o.push(MathUtils.mapLinear(z2, h, d, 0, 1)));
    }
    if (i.length !== 2)
      Math.min(
        e.clipValues.a,
        e.clipValues.b,
        e.clipValues.c
      ) < z2 === t && s.push(e);
    else if (i.length === 2) {
      const l2 = n.get().initFromTriangle(e), c = n.get().initFromTriangle(e), u2 = n.get().initFromTriangle(e);
      (i[0] + 1) % 3 === i[1] ? (l2.lerpVertexFromEdge(e, r[0][0], r[0][1], o[0], "a"), l2.copyVertex(e, r[0][1], "b"), l2.lerpVertexFromEdge(e, r[1][0], r[1][1], o[1], "c"), l2.clipValues.a = z2, l2.clipValues.c = z2, c.lerpVertexFromEdge(e, r[0][0], r[0][1], o[0], "a"), c.copyVertex(e, r[1][1], "b"), c.copyVertex(e, r[0][0], "c"), c.clipValues.a = z2, u2.lerpVertexFromEdge(e, r[0][0], r[0][1], o[0], "a"), u2.lerpVertexFromEdge(e, r[1][0], r[1][1], o[1], "b"), u2.copyVertex(e, r[1][1], "c"), u2.clipValues.a = z2, u2.clipValues.b = z2) : (l2.lerpVertexFromEdge(e, r[0][0], r[0][1], o[0], "a"), l2.lerpVertexFromEdge(e, r[1][0], r[1][1], o[1], "b"), l2.copyVertex(e, r[0][0], "c"), l2.clipValues.a = z2, l2.clipValues.b = z2, c.lerpVertexFromEdge(e, r[0][0], r[0][1], o[0], "a"), c.copyVertex(e, r[0][1], "b"), c.lerpVertexFromEdge(e, r[1][0], r[1][1], o[1], "c"), c.clipValues.a = z2, c.clipValues.c = z2, u2.copyVertex(e, r[0][1], "a"), u2.copyVertex(e, r[1][0], "b"), u2.lerpVertexFromEdge(e, r[1][0], r[1][1], o[1], "c"), u2.clipValues.c = z2);
      let d, m;
      d = Math.min(l2.clipValues.a, l2.clipValues.b, l2.clipValues.c), m = d < z2, m === t && s.push(l2), d = Math.min(c.clipValues.a, c.clipValues.b, c.clipValues.c), m = d < z2, m === t && s.push(c), d = Math.min(u2.clipValues.a, u2.clipValues.b, u2.clipValues.c), m = d < z2, m === t && s.push(u2);
    }
  }
};
var Ii = class {
  constructor() {
    this.pool = [], this.index = 0;
  }
  get() {
    if (this.index >= this.pool.length) {
      const t = new Li();
      this.pool.push(t);
    }
    const e = this.pool[this.index];
    return this.index++, e;
  }
  reset() {
    this.index = 0;
  }
};
var Li = class {
  constructor() {
    this.indices = {
      a: -1,
      b: -1,
      c: -1
    }, this.clipValues = {
      a: -1,
      b: -1,
      c: -1
    }, this.barycoord = new Triangle();
  }
  // returns a hash for the given [0, 2] index based on attributes of the referenced geometry
  getVertexHash(e, t) {
    const { barycoord: s, indices: n } = this, i = ce[e], r = s[i];
    if (r.x === 1)
      return n[ce[0]];
    if (r.y === 1)
      return n[ce[1]];
    if (r.z === 1)
      return n[ce[2]];
    {
      const { attributes: o } = t;
      let l2 = "";
      for (const c in o) {
        const u2 = o[c];
        switch (ls(u2, n.a, n.b, n.c, r, w2), (c === "normal" || c === "tangent" || c === "bitangent") && w2.normalize(), u2.itemSize) {
          case 4:
            l2 += we(w2.x, w2.y, w2.z, w2.w);
            break;
          case 3:
            l2 += we(w2.x, w2.y, w2.z);
            break;
          case 2:
            l2 += we(w2.x, w2.y);
            break;
          case 1:
            l2 += we(w2.x);
            break;
        }
        l2 += "|";
      }
      return l2;
    }
  }
  // Accumulate the vertex data in the given attribute arrays
  getVertexData(e, t, s) {
    const { barycoord: n, indices: i } = this, r = ce[e], o = n[r], { attributes: l2 } = t;
    for (const c in l2) {
      if (!s[c])
        continue;
      const u2 = l2[c], h = s[c];
      switch (ls(u2, i.a, i.b, i.c, o, w2), (c === "normal" || c === "tangent" || c === "bitangent") && w2.normalize(), u2.itemSize) {
        case 4:
          h.push(w2.x, w2.y, w2.z, w2.w);
          break;
        case 3:
          h.push(w2.x, w2.y, w2.z);
          break;
        case 2:
          h.push(w2.x, w2.y);
          break;
        case 1:
          h.push(w2.x);
          break;
      }
    }
  }
  // Copy the indices from a target triangle
  initFromTriangle(e) {
    return this.initFromIndices(
      e.indices.a,
      e.indices.b,
      e.indices.c
    );
  }
  // Set the indices for the given
  initFromIndices(e, t, s) {
    return this.indices.a = e, this.indices.b = t, this.indices.c = s, this.clipValues.a = -1, this.clipValues.b = -1, this.clipValues.c = -1, this.barycoord.a.set(1, 0, 0), this.barycoord.b.set(0, 1, 0), this.barycoord.c.set(0, 0, 1), this;
  }
  // Lerp the given vertex along to the provided edge of the provided triangle
  lerpVertexFromEdge(e, t, s, n, i) {
    this.clipValues[i] = MathUtils.lerp(e.clipValues[t], e.clipValues[s], n), this.barycoord[i].lerpVectors(e.barycoord[t], e.barycoord[s], n);
  }
  // Copy a vertex from the provided triangle
  copyVertex(e, t, s) {
    this.clipValues[s] = e.clipValues[t], this.barycoord[s].copy(e.barycoord[t]);
  }
};
function ls(a, e, t, s, n, i) {
  switch (rs.fromBufferAttribute(a, e), os.fromBufferAttribute(a, t), as.fromBufferAttribute(a, s), i.set(0, 0, 0, 0).addScaledVector(rs, n.x).addScaledVector(os, n.y).addScaledVector(as, n.z), a.itemSize) {
    case 3:
      w2.w = 0;
      break;
    case 2:
      w2.w = 0, w2.z = 0;
      break;
    case 1:
      w2.w = 0, w2.z = 0, w2.y = 0;
      break;
  }
  return i;
}
function we(...a) {
  let s = "";
  for (let n = 0, i = a.length; n < i; n++)
    s += ~~(a[n] * 1e5 + 0.5), n !== i - 1 && (s += "_");
  return s;
}
var cs = {};
var vi = new Vector3();
var gt = new Vector3();
var yt = new Vector3();
var Ei = new Vector3();
var wi = new Vector3();
var Y = new Vector3();
var be = new Vector3();
var j = new Vector2();
var le = new Vector2();
var us = new Vector2();
var Pi = class extends yn {
  constructor() {
    super(), this.ellipsoid = new Mt(), this.skirtLength = 1e3, this.smoothSkirtNormals = true, this.solid = false, this.minLat = -Math.PI / 2, this.maxLat = Math.PI / 2, this.minLon = -Math.PI, this.maxLon = Math.PI, this.attributeList = ["position", "normal", "uv"];
  }
  clipToQuadrant(e, t, s) {
    const { solid: n, skirtLength: i, ellipsoid: r, smoothSkirtNormals: o } = this;
    this.clearSplitOperations(), this.addSplitOperation(hs("x"), !t), this.addSplitOperation(hs("y"), !s);
    let l2, c;
    const u2 = e.geometry.groups[0], h = this.getClippedData(e, u2);
    if (this.adjustVertices(h, e.position, 0), n) {
      l2 = {
        index: h.index.slice().reverse(),
        attributes: {}
      };
      for (const S in h.attributes)
        l2.attributes[S] = h.attributes[S].slice();
      const T = l2.attributes.normal;
      if (T)
        for (let S = 0; S < T.length; S += 3)
          T[S + 0] *= -1, T[S + 1] *= -1, T[S + 2] *= -1;
      this.adjustVertices(l2, e.position, -i);
    }
    if (i > 0) {
      c = {
        index: [],
        attributes: {
          position: [],
          normal: [],
          uv: []
        }
      };
      let T = 0;
      const S = {}, _ = (k, W, $) => {
        const H = we(...k, ...$, ...W);
        H in S || (S[H] = T, T++, c.attributes.position.push(...k), c.attributes.normal.push(...$), c.attributes.uv.push(...W)), c.index.push(S[H]);
      }, C2 = h.index, L = h.attributes.uv, P = h.attributes.position, R = h.attributes.normal, V = h.index.length / 3;
      for (let k = 0; k < V; k++) {
        const W = 3 * k;
        for (let $ = 0; $ < 3; $++) {
          const H = ($ + 1) % 3, I = C2[W + $], A = C2[W + H];
          if (j.fromArray(L, I * 2), le.fromArray(L, A * 2), j.x === le.x && (j.x === 0 || j.x === 0.5 || j.x === 1) || j.y === le.y && (j.y === 0 || j.y === 0.5 || j.y === 1)) {
            gt.fromArray(P, I * 3), yt.fromArray(P, A * 3);
            const D = gt, O = yt, v2 = Ei.copy(gt), N2 = wi.copy(yt);
            Y.copy(v2).add(e.position), r.getPositionToNormal(Y, Y), v2.addScaledVector(Y, -i), Y.copy(N2).add(e.position), r.getPositionToNormal(Y, Y), N2.addScaledVector(Y, -i), o && R ? (Y.fromArray(R, I * 3), be.fromArray(R, A * 3)) : (Y.subVectors(D, O), be.subVectors(D, v2).cross(Y).normalize(), Y.copy(be)), _(O, le, be), _(D, j, Y), _(v2, j, Y), _(O, le, be), _(v2, j, Y), _(N2, le, be);
          }
        }
      }
    }
    const d = h.index.length, m = h;
    if (l2) {
      const { index: T, attributes: S } = l2, _ = m.attributes.position.length / 3;
      for (let C2 = 0, L = T.length; C2 < L; C2++)
        m.index.push(T[C2] + _);
      for (const C2 in h.attributes)
        m.attributes[C2].push(...S[C2]);
    }
    if (c) {
      const { index: T, attributes: S } = c, _ = m.attributes.position.length / 3;
      for (let C2 = 0, L = T.length; C2 < L; C2++)
        m.index.push(T[C2] + _);
      for (const C2 in h.attributes)
        m.attributes[C2].push(...S[C2]);
    }
    const f = t ? 0 : -0.5, p = s ? 0 : -0.5, g = m.attributes.uv;
    for (let T = 0, S = g.length; T < S; T += 2)
      g[T] = (g[T] + f) * 2, g[T + 1] = (g[T + 1] + p) * 2;
    const y2 = this.constructMesh(m.attributes, m.index, e);
    y2.userData.minHeight = e.userData.minHeight, y2.userData.maxHeight = e.userData.maxHeight;
    let x2 = 0, M = 0;
    return y2.geometry.addGroup(M, d, x2), M += d, x2++, l2 && (y2.geometry.addGroup(M, l2.index.length, x2), M += l2.index.length, x2++), c && (y2.geometry.addGroup(M, c.index.length, x2), M += c.index.length, x2++), y2;
  }
  adjustVertices(e, t, s) {
    const { ellipsoid: n, minLat: i, maxLat: r, minLon: o, maxLon: l2 } = this, { attributes: c, vertexIsClipped: u2 } = e, h = c.position, d = c.uv, m = h.length / 3;
    for (let f = 0; f < m; f++) {
      const p = j.fromArray(d, f * 2);
      u2 && u2[f] && (Math.abs(p.x - 0.5) < 1e-10 && (p.x = 0.5), Math.abs(p.y - 0.5) < 1e-10 && (p.y = 0.5), j.toArray(d, f * 2));
      const g = MathUtils.lerp(i, r, p.y), y2 = MathUtils.lerp(o, l2, p.x), x2 = vi.fromArray(h, f * 3).add(t);
      n.getPositionToCartographic(x2, cs), n.getCartographicToPosition(g, y2, cs.height + s, x2), x2.sub(t), x2.toArray(h, f * 3);
    }
  }
};
function hs(a) {
  return (e, t, s, n, i) => {
    const r = e.attributes.uv;
    return j.fromBufferAttribute(r, t), le.fromBufferAttribute(r, s), us.fromBufferAttribute(r, n), j[a] * i.x + le[a] * i.y + us[a] * i.z - 0.5;
  };
}
var ds = /* @__PURE__ */ Symbol("TILE_X");
var ps = /* @__PURE__ */ Symbol("TILE_Y");
var Pe = /* @__PURE__ */ Symbol("TILE_LEVEL");
var me = /* @__PURE__ */ Symbol("TILE_AVAILABLE");
var He = 1e4;
var fs = new Vector3();
function Ri(a, e, t, s) {
  if (a && e < a.length) {
    const n = a[e];
    for (let i = 0, r = n.length; i < r; i++) {
      const { startX: o, startY: l2, endX: c, endY: u2 } = n[i];
      if (t >= o && t <= c && s >= l2 && s <= u2)
        return true;
    }
  }
  return false;
}
function xn(a) {
  const { available: e = null, maxzoom: t = null } = a;
  return t === null ? e.length - 1 : t;
}
function Di(a) {
  const { metadataAvailability: e = -1 } = a;
  return e;
}
function xt(a, e) {
  const t = a[Pe], s = Di(e), n = xn(e);
  return t < n && s !== -1 && t % s === 0;
}
function Bi(a, e, t, s, n) {
  return n.tiles[0].replace(/{\s*z\s*}/g, t).replace(/{\s*x\s*}/g, a).replace(/{\s*y\s*}/g, e).replace(/{\s*version\s*}/g, s);
}
var Oi = class {
  constructor(e = {}) {
    const {
      useRecommendedSettings: t = true,
      skirtLength: s = null,
      smoothSkirtNormals: n = true,
      generateNormals: i = true,
      solid: r = false
    } = e;
    this.name = "QUANTIZED_MESH_PLUGIN", this.priority = -1e3, this.tiles = null, this.layer = null, this.useRecommendedSettings = t, this.skirtLength = s, this.smoothSkirtNormals = n, this.solid = r, this.generateNormals = i, this.attribution = null, this.tiling = new fn(), this.projection = new re();
  }
  // Plugin function
  init(e) {
    e.fetchOptions.headers = e.fetchOptions.headers || {}, e.fetchOptions.headers.Accept = "application/vnd.quantized-mesh,application/octet-stream;q=0.9", this.useRecommendedSettings && (e.errorTarget = 2), this.tiles = e;
  }
  loadRootTileset() {
    const { tiles: e } = this;
    let t = new URL("layer.json", new URL(e.rootURL, location.href));
    return e.invokeAllPlugins((s) => t = s.preprocessURL ? s.preprocessURL(t, null) : t), e.invokeOnePlugin((s) => s.fetchData && s.fetchData(t, this.tiles.fetchOptions)).then((s) => s.json()).then((s) => {
      this.layer = s;
      const {
        projection: n = "EPSG:4326",
        extensions: i = [],
        attribution: r = "",
        available: o = null
      } = s, {
        tiling: l2,
        tiles: c,
        projection: u2
      } = this;
      r && (this.attribution = {
        value: r,
        type: "string",
        collapsible: true
      }), i.length > 0 && (c.fetchOptions.headers.Accept += `;extensions=${i.join("-")}`), u2.setScheme(n);
      const { tileCountX: h, tileCountY: d } = u2;
      l2.setProjection(u2), l2.generateLevels(xn(s) + 1, h, d);
      const m = [];
      for (let g = 0; g < h; g++) {
        const y2 = this.createChild(0, g, 0, o);
        y2 && m.push(y2);
      }
      const f = {
        asset: {
          version: "1.1"
        },
        geometricError: 1 / 0,
        root: {
          refine: "REPLACE",
          geometricError: 1 / 0,
          boundingVolume: {
            region: [...this.tiling.getContentBounds(), -He, He]
          },
          children: m,
          [me]: o,
          [Pe]: -1
        }
      };
      let p = c.rootURL;
      return c.invokeAllPlugins((g) => p = g.preprocessURL ? g.preprocessURL(p, null) : p), c.preprocessTileset(f, p), f;
    });
  }
  parseToMesh(e, t, s, n) {
    const {
      skirtLength: i,
      solid: r,
      smoothSkirtNormals: o,
      generateNormals: l2,
      tiles: c
    } = this, u2 = c.ellipsoid;
    let h;
    if (s === "quantized_tile_split") {
      const p = new URL(n).searchParams, g = p.get("left") === "true", y2 = p.get("bottom") === "true", x2 = new Pi();
      x2.ellipsoid.copy(u2), x2.solid = r, x2.smoothSkirtNormals = o, x2.skirtLength = i === null ? t.geometricError : i;
      const [M, T, S, _] = t.parent.boundingVolume.region;
      x2.minLat = T, x2.maxLat = _, x2.minLon = M, x2.maxLon = S, h = x2.clipToQuadrant(t.parent.engineData.scene, g, y2);
    } else if (s === "terrain") {
      const p = new Ai(c.manager);
      p.ellipsoid.copy(u2), p.solid = r, p.smoothSkirtNormals = o, p.generateNormals = l2, p.skirtLength = i === null ? t.geometricError : i;
      const [g, y2, x2, M] = t.boundingVolume.region;
      p.minLat = y2, p.maxLat = M, p.minLon = g, p.maxLon = x2, h = p.parse(e);
    } else
      return;
    const { minHeight: d, maxHeight: m, metadata: f } = h.userData;
    return t.boundingVolume.region[4] = d, t.boundingVolume.region[5] = m, t.engineData.boundingVolume.setRegionData(u2, ...t.boundingVolume.region), f && ("geometricerror" in f && (t.geometricError = f.geometricerror), xt(t, this.layer) && "available" in f && t.children.length === 0 && (t[me] = [
      ...new Array(t[Pe] + 1).fill(null),
      ...f.available
    ])), this.expandChildren(t), h;
  }
  getAttributions(e) {
    this.attribution && e.push(this.attribution);
  }
  // Local functions
  createChild(e, t, s, n) {
    const { tiles: i, layer: r, tiling: o, projection: l2 } = this, c = i.ellipsoid, u2 = n === null && e === 0 || Ri(n, e, t, s), h = Bi(t, s, e, 1, r), d = [...o.getTileBounds(t, s, e), -He, He], [
      /* west */
      ,
      m,
      /* east */
      ,
      f,
      /* minHeight */
      ,
      p
    ] = d, g = m > 0 != f > 0 ? 0 : Math.min(Math.abs(m), Math.abs(f));
    c.getCartographicToPosition(g, 0, p, fs), fs.z = 0;
    const y2 = l2.tileCountX, T = Math.max(...c.radius) * 2 * Math.PI * 0.25 / (65 * y2) / 2 ** e, S = {
      [me]: null,
      [Pe]: e,
      [ds]: t,
      [ps]: s,
      refine: "REPLACE",
      geometricError: T,
      boundingVolume: { region: d },
      content: u2 ? { uri: h } : null,
      children: []
    };
    return xt(S, r) || (S[me] = n), S;
  }
  expandChildren(e) {
    const t = e[Pe], s = e[ds], n = e[ps], i = e[me];
    if (t >= this.tiling.maxLevel)
      return;
    let r = false;
    for (let o = 0; o < 2; o++)
      for (let l2 = 0; l2 < 2; l2++) {
        const c = this.createChild(t + 1, 2 * s + o, 2 * n + l2, i);
        c.content !== null ? (e.children.push(c), r = true) : (e.children.push(c), c.content = { uri: `tile.quantized_tile_split?bottom=${l2 === 0}&left=${o === 0}` });
      }
    r || (e.children.length = 0);
  }
  fetchData(e, t) {
    if (/quantized_tile_split/.test(e))
      return new ArrayBuffer();
  }
  disposeTile(e) {
    xt(e, this.layer) && (e[me] = null), me in e && (e.children.forEach((t) => {
      this.tiles.processNodeQueue.remove(t);
    }), e.children.length = 0);
  }
};
var no = class extends lt {
  constructor(e = {}) {
    super({
      assetTypeHandler: (t, s, n) => {
        t === "TERRAIN" && s.getPluginByName("QUANTIZED_MESH_PLUGIN") === null ? (console.warn(
          'CesiumIonAuthPlugin: CesiumIonAuthPlugin plugin auto-registration has been deprecated. Please implement a custom "assetTypeHandler" for "TERRAIN" using "QuantizedMeshPlugin", instead.'
        ), s.registerPlugin(new Oi({
          useRecommendedSettings: this.useRecommendedSettings
        }))) : t === "IMAGERY" && s.getPluginByName("TMS_TILES_PLUGIN") === null ? (console.warn(
          'CesiumIonAuthPlugin: CesiumIonAuthPlugin plugin auto-registration has been deprecated. Please implement a custom "assetTypeHandler" for "IMAGERY" using "TMSTilesPlugin", instead.'
        ), s.registerPlugin(new Ci({
          useRecommendedSettings: this.useRecommendedSettings,
          shape: "ellipsoid"
        }))) : console.warn(`CesiumIonAuthPlugin: Cesium Ion asset type "${t}" unhandled.`);
      },
      ...e
    }), e.__suppress_warning__ && console.warn(
      'CesiumIonAuthPlugin: Plugin has been moved to "3d-tiles-renderer/core/plugins".'
    );
  }
};
var Tt = new Matrix4();
var ro = class {
  constructor() {
    this.name = "UPDATE_ON_CHANGE_PLUGIN", this.tiles = null, this.needsUpdate = false, this.cameraMatrices = /* @__PURE__ */ new Map();
  }
  init(e) {
    this.tiles = e, this._needsUpdateCallback = () => {
      this.needsUpdate = true;
    }, this._onCameraAdd = ({ camera: t }) => {
      this.needsUpdate = true, this.cameraMatrices.set(t, new Matrix4());
    }, this._onCameraDelete = ({ camera: t }) => {
      this.needsUpdate = true, this.cameraMatrices.delete(t);
    }, e.addEventListener("needs-update", this._needsUpdateCallback), e.addEventListener("add-camera", this._onCameraAdd), e.addEventListener("delete-camera", this._onCameraDelete), e.addEventListener("camera-resolution-change", this._needsUpdateCallback), e.cameras.forEach((t) => {
      this._onCameraAdd({ camera: t });
    });
  }
  doTilesNeedUpdate() {
    const e = this.tiles;
    let t = false;
    this.cameraMatrices.forEach((n, i) => {
      Tt.copy(e.group.matrixWorld).premultiply(i.matrixWorldInverse).premultiply(i.projectionMatrixInverse), t = t || !Tt.equals(n), n.copy(Tt);
    });
    const s = this.needsUpdate;
    return this.needsUpdate = false, s || t;
  }
  preprocessNode() {
    this.needsUpdate = true;
  }
  dispose() {
    const e = this.tiles;
    e.removeEventListener("camera-resolution-change", this._needsUpdateCallback), e.removeEventListener("needs-update", this._needsUpdateCallback), e.removeEventListener("add-camera", this._onCameraAdd), e.removeEventListener("delete-camera", this._onCameraDelete);
  }
};
var ms = new Vector3();
function Ie(a, e) {
  if (a.isInterleavedBufferAttribute || a.array instanceof e)
    return a;
  const s = e === Int8Array || e === Int16Array || e === Int32Array ? -1 : 0, n = new e(a.count * a.itemSize), i = new BufferAttribute(n, a.itemSize, true), r = a.itemSize, o = a.count;
  for (let l2 = 0; l2 < o; l2++)
    for (let c = 0; c < r; c++) {
      const u2 = MathUtils.clamp(a.getComponent(l2, c), s, 1);
      i.setComponent(l2, c, u2);
    }
  return i;
}
function Ui(a, e = Int16Array) {
  const t = a.geometry, s = t.attributes, n = s.position;
  if (n.isInterleavedBufferAttribute || n.array instanceof e)
    return n;
  const i = new e(n.count * n.itemSize), r = new BufferAttribute(i, n.itemSize, false), o = n.itemSize, l2 = n.count;
  t.computeBoundingBox();
  const c = t.boundingBox, { min: u2, max: h } = c, d = 2 ** (8 * e.BYTES_PER_ELEMENT - 1) - 1, m = -d;
  for (let f = 0; f < l2; f++)
    for (let p = 0; p < o; p++) {
      const g = p === 0 ? "x" : p === 1 ? "y" : "z", y2 = u2[g], x2 = h[g], M = MathUtils.mapLinear(
        n.getComponent(f, p),
        y2,
        x2,
        m,
        d
      );
      r.setComponent(f, p, M);
    }
  c.getCenter(ms).multiply(a.scale).applyQuaternion(a.quaternion), a.position.add(ms), a.scale.x *= 0.5 * (h.x - u2.x) / d, a.scale.y *= 0.5 * (h.y - u2.y) / d, a.scale.z *= 0.5 * (h.z - u2.z) / d, s.position = r, a.geometry.boundingBox = null, a.geometry.boundingSphere = null, a.updateMatrixWorld();
}
var oo = class {
  constructor(e) {
    this._options = {
      // whether to generate normals if they don't already exist.
      generateNormals: false,
      // whether to disable use of mipmaps since they are typically not necessary
      // with something like 3d tiles.
      disableMipmaps: true,
      // whether to compress certain attributes
      compressIndex: true,
      compressNormals: false,
      compressUvs: false,
      compressPosition: false,
      // the TypedArray type to use when compressing the attributes
      uvType: Int8Array,
      normalType: Int8Array,
      positionType: Int16Array,
      ...e
    }, this.name = "TILES_COMPRESSION_PLUGIN", this.priority = -100;
  }
  processTileModel(e, t) {
    const {
      generateNormals: s,
      disableMipmaps: n,
      compressIndex: i,
      compressUvs: r,
      compressNormals: o,
      compressPosition: l2,
      uvType: c,
      normalType: u2,
      positionType: h
    } = this._options;
    e.traverse((d) => {
      if (d.material && n) {
        const m = d.material;
        for (const f in m) {
          const p = m[f];
          p && p.isTexture && p.generateMipmaps && (p.generateMipmaps = false, p.minFilter = LinearFilter);
        }
      }
      if (d.geometry) {
        const m = d.geometry, f = m.attributes;
        if (r) {
          const { uv: p, uv1: g, uv2: y2, uv3: x2 } = f;
          p && (f.uv = Ie(p, c)), g && (f.uv1 = Ie(g, c)), y2 && (f.uv2 = Ie(y2, c)), x2 && (f.uv3 = Ie(x2, c));
        }
        if (s && !f.normals && m.computeVertexNormals(), o && f.normals && (f.normals = Ie(f.normals, u2)), l2 && Ui(d, h), i && m.index) {
          const p = f.position.count, g = m.index, y2 = p > 65535 ? Uint32Array : p > 255 ? Uint16Array : Uint8Array;
          if (!(g.array instanceof y2)) {
            const x2 = new y2(m.index.count);
            x2.set(g.array);
            const M = new BufferAttribute(x2, 1);
            m.setIndex(M);
          }
        }
      }
    });
  }
};
function q(a, e, t) {
  return a && e in a ? a[e] : t;
}
function Tn(a) {
  return a !== "BOOLEAN" && a !== "STRING" && a !== "ENUM";
}
function Ni(a) {
  return /^FLOAT/.test(a);
}
function Ve(a) {
  return /^VEC/.test(a);
}
function Fe(a) {
  return /^MAT/.test(a);
}
function bn(a, e, t, s = null) {
  return Fe(t) || Ve(t) ? s.fromArray(a, e) : a[e];
}
function Nt2(a) {
  const { type: e, componentType: t } = a;
  switch (e) {
    case "SCALAR":
      return t === "INT64" ? 0n : 0;
    case "VEC2":
      return new Vector2();
    case "VEC3":
      return new Vector3();
    case "VEC4":
      return new Vector4();
    case "MAT2":
      return new Matrix2();
    case "MAT3":
      return new Matrix3();
    case "MAT4":
      return new Matrix4();
    case "BOOLEAN":
      return false;
    case "STRING":
      return "";
    // the final value for enums is a string but are represented as integers
    // during intermediate steps
    case "ENUM":
      return 0;
  }
}
function gs(a, e) {
  if (e == null)
    return false;
  switch (a) {
    case "SCALAR":
      return typeof e == "number" || typeof e == "bigint";
    case "VEC2":
      return e.isVector2;
    case "VEC3":
      return e.isVector3;
    case "VEC4":
      return e.isVector4;
    case "MAT2":
      return e.isMatrix2;
    case "MAT3":
      return e.isMatrix3;
    case "MAT4":
      return e.isMatrix4;
    case "BOOLEAN":
      return typeof e == "boolean";
    case "STRING":
      return typeof e == "string";
    case "ENUM":
      return typeof e == "number" || typeof e == "bigint";
  }
  throw new Error("ClassProperty: invalid type.");
}
function De(a, e = null) {
  switch (a) {
    case "INT8":
      return Int8Array;
    case "INT16":
      return Int16Array;
    case "INT32":
      return Int32Array;
    case "INT64":
      return BigInt64Array;
    case "UINT8":
      return Uint8Array;
    case "UINT16":
      return Uint16Array;
    case "UINT32":
      return Uint32Array;
    case "UINT64":
      return BigUint64Array;
    case "FLOAT32":
      return Float32Array;
    case "FLOAT64":
      return Float64Array;
  }
  switch (e) {
    case "BOOLEAN":
      return Uint8Array;
    case "STRING":
      return Uint8Array;
  }
  throw new Error("ClassProperty: invalid type.");
}
function Vi(a, e = null) {
  if (a.array) {
    e = e && Array.isArray(e) ? e : [], e.length = a.count;
    for (let s = 0, n = e.length; s < n; s++)
      e[s] = st(a, e[s]);
  } else
    e = st(a, e);
  return e;
}
function st(a, e = null) {
  const t = a.default, s = a.type;
  if (e = e || Nt2(a), t === null) {
    switch (s) {
      case "SCALAR":
        return 0;
      case "VEC2":
        return e.set(0, 0);
      case "VEC3":
        return e.set(0, 0, 0);
      case "VEC4":
        return e.set(0, 0, 0, 0);
      case "MAT2":
        return e.identity();
      case "MAT3":
        return e.identity();
      case "MAT4":
        return e.identity();
      case "BOOLEAN":
        return false;
      case "STRING":
        return "";
      case "ENUM":
        return "";
    }
    throw new Error("ClassProperty: invalid type.");
  } else if (Fe(s))
    e.fromArray(t);
  else if (Ve(s))
    e.fromArray(t);
  else
    return t;
}
function Fi(a, e) {
  if (a.noData === null)
    return e;
  const t = a.noData, s = a.type;
  if (Array.isArray(e))
    for (let r = 0, o = e.length; r < o; r++)
      e[r] = n(e[r]);
  else
    e = n(e);
  return e;
  function n(r) {
    return i(r) && (r = st(a, r)), r;
  }
  function i(r) {
    if (Fe(s)) {
      const o = r.elements;
      for (let l2 = 0, c = t.length; l2 < c; l2++)
        if (t[l2] !== o[l2])
          return false;
      return true;
    } else if (Ve(s)) {
      for (let o = 0, l2 = t.length; o < l2; o++)
        if (t[o] !== r.getComponent(o))
          return false;
      return true;
    } else
      return t === r;
  }
}
function ki(a, e) {
  switch (a) {
    case "INT8":
      return Math.max(e / 127, -1);
    case "INT16":
      return Math.max(e, 32767, -1);
    case "INT32":
      return Math.max(e / 2147483647, -1);
    case "INT64":
      return Math.max(Number(e) / 9223372036854776e3, -1);
    // eslint-disable-line no-loss-of-precision
    case "UINT8":
      return e / 255;
    case "UINT16":
      return e / 65535;
    case "UINT32":
      return e / 4294967295;
    case "UINT64":
      return Number(e) / 18446744073709552e3;
  }
}
function Gi(a, e) {
  const {
    type: t,
    componentType: s,
    scale: n,
    offset: i,
    normalized: r
  } = a;
  if (Array.isArray(e))
    for (let h = 0, d = e.length; h < d; h++)
      e[h] = o(e[h]);
  else
    e = o(e);
  return e;
  function o(h) {
    return Fe(t) ? h = c(h) : Ve(t) ? h = l2(h) : h = u2(h), h;
  }
  function l2(h) {
    return h.x = u2(h.x), h.y = u2(h.y), "z" in h && (h.z = u2(h.z)), "w" in h && (h.w = u2(h.w)), h;
  }
  function c(h) {
    const d = h.elements;
    for (let m = 0, f = d.length; m < f; m++)
      d[m] = u2(d[m]);
    return h;
  }
  function u2(h) {
    return r && (h = ki(s, h)), (r || Ni(s)) && (h = h * n + i), h;
  }
}
function Yt(a, e, t = null) {
  if (a.array) {
    Array.isArray(e) || (e = new Array(a.count || 0)), e.length = t !== null ? t : a.count;
    for (let s = 0, n = e.length; s < n; s++)
      gs(a.type, e[s]) || (e[s] = Nt2(a));
  } else
    gs(a.type, e) || (e = Nt2(a));
  return e;
}
function nt(a, e) {
  for (const t in e)
    t in a || delete e[t];
  for (const t in a) {
    const s = a[t];
    e[t] = Yt(s, e[t]);
  }
}
function zi(a) {
  switch (a) {
    case "ENUM":
      return 1;
    case "SCALAR":
      return 1;
    case "VEC2":
      return 2;
    case "VEC3":
      return 3;
    case "VEC4":
      return 4;
    case "MAT2":
      return 4;
    case "MAT3":
      return 9;
    case "MAT4":
      return 16;
    // unused
    case "BOOLEAN":
      return -1;
    case "STRING":
      return -1;
    default:
      return -1;
  }
}
var ut = class {
  constructor(e, t, s = null) {
    this.name = t.name || null, this.description = t.description || null, this.type = t.type, this.componentType = t.componentType || null, this.enumType = t.enumType || null, this.array = t.array || false, this.count = t.count || 0, this.normalized = t.normalized || false, this.offset = t.offset || 0, this.scale = q(t, "scale", 1), this.max = q(t, "max", 1 / 0), this.min = q(t, "min", -1 / 0), this.required = t.required || false, this.noData = q(t, "noData", null), this.default = q(t, "default", null), this.semantic = q(t, "semantic", null), this.enumSet = null, this.accessorProperty = s, s && (this.offset = q(s, "offset", this.offset), this.scale = q(s, "scale", this.scale), this.max = q(s, "max", this.max), this.min = q(s, "min", this.min)), t.type === "ENUM" && (this.enumSet = e[this.enumType], this.componentType === null && (this.componentType = q(this.enumSet, "valueType", "UINT16")));
  }
  // shape the given target to match the data type of the property
  // enums are set to their integer value
  shapeToProperty(e, t = null) {
    return Yt(this, e, t);
  }
  // resolve the given object to the default value for the property for a single element
  // enums are set to a default string
  resolveDefaultElement(e) {
    return st(this, e);
  }
  // resolve the target to the default value for the property for every element if it's an array
  // enums are set to a default string
  resolveDefault(e) {
    return Vi(this, e);
  }
  // converts any instances of no data to the default value
  resolveNoData(e) {
    return Fi(this, e);
  }
  // converts enums integers in the given target to strings
  resolveEnumsToStrings(e) {
    const t = this.enumSet;
    if (this.type === "ENUM")
      if (Array.isArray(e))
        for (let n = 0, i = e.length; n < i; n++)
          e[n] = s(e[n]);
      else
        e = s(e);
    return e;
    function s(n) {
      const i = t.values.find((r) => r.value === n);
      return i === null ? "" : i.name;
    }
  }
  // apply scales
  adjustValueScaleOffset(e) {
    return Tn(this.type) ? Gi(this, e) : e;
  }
};
var $t2 = class {
  constructor(e, t = {}, s = {}, n = null) {
    this.definition = e, this.class = t[e.class], this.className = e.class, this.enums = s, this.data = n, this.name = "name" in e ? e.name : null, this.properties = null;
  }
  getPropertyNames() {
    return Object.keys(this.class.properties);
  }
  includesData(e) {
    return !!this.definition.properties[e];
  }
  dispose() {
  }
  _initProperties(e = ut) {
    const t = {};
    for (const s in this.class.properties)
      t[s] = new e(this.enums, this.class.properties[s], this.definition.properties[s]);
    this.properties = t;
  }
};
var Hi = class extends ut {
  constructor(e, t, s = null) {
    super(e, t, s), this.attribute = (s == null ? void 0 : s.attribute) ?? null;
  }
};
var qi = class extends $t2 {
  constructor(...e) {
    super(...e), this.isPropertyAttributeAccessor = true, this._initProperties(Hi);
  }
  getData(e, t, s = {}) {
    const n = this.properties;
    nt(n, s);
    for (const i in n)
      s[i] = this.getPropertyValue(i, e, t, s[i]);
    return s;
  }
  getPropertyValue(e, t, s, n = null) {
    if (t >= this.count)
      throw new Error("PropertyAttributeAccessor: Requested index is outside the range of the buffer.");
    const i = this.properties[e], r = i.type;
    if (i) {
      if (!this.definition.properties[e])
        return i.resolveDefault(n);
    } else throw new Error("PropertyAttributeAccessor: Requested class property does not exist.");
    n = i.shapeToProperty(n);
    const o = s.getAttribute(i.attribute.toLowerCase());
    if (Fe(r)) {
      const l2 = n.elements;
      for (let c = 0, u2 = l2.length; c < u2; c < u2)
        l2[c] = o.getComponent(t, c);
    } else if (Ve(r))
      n.fromBufferAttribute(o, t);
    else if (r === "SCALAR" || r === "ENUM")
      n = o.getX(t);
    else
      throw new Error("StructuredMetadata.PropertyAttributeAccessor: BOOLEAN and STRING types are not supported by property attributes.");
    return n = i.adjustValueScaleOffset(n), n = i.resolveEnumsToStrings(n), n = i.resolveNoData(n), n;
  }
};
var Wi = class extends ut {
  constructor(e, t, s = null) {
    super(e, t, s), this.values = (s == null ? void 0 : s.values) ?? null, this.valueLength = zi(this.type), this.arrayOffsets = q(s, "arrayOffsets", null), this.stringOffsets = q(s, "stringOffsets", null), this.arrayOffsetType = q(s, "arrayOffsetType", "UINT32"), this.stringOffsetType = q(s, "stringOffsetType", "UINT32");
  }
  // returns the necessary array length based on the array offsets if present
  getArrayLengthFromId(e, t) {
    let s = this.count;
    if (this.arrayOffsets !== null) {
      const { arrayOffsets: n, arrayOffsetType: i } = this, r = De(i), o = new r(e[n]);
      s = o[t + 1] - o[t];
    }
    return s;
  }
  // returns the index offset into the data buffer for the given id based on the
  // the array offsets if present
  getIndexOffsetFromId(e, t) {
    let s = t;
    if (this.arrayOffsets) {
      const { arrayOffsets: n, arrayOffsetType: i } = this, r = De(i);
      s = new r(e[n])[s];
    } else this.array && (s *= this.count);
    return s;
  }
};
var ji = class extends $t2 {
  constructor(...e) {
    super(...e), this.isPropertyTableAccessor = true, this.count = this.definition.count, this._initProperties(Wi);
  }
  getData(e, t = {}) {
    const s = this.properties;
    nt(s, t);
    for (const n in s)
      t[n] = this.getPropertyValue(n, e, t[n]);
    return t;
  }
  // reads an individual element
  _readValueAtIndex(e, t, s, n = null) {
    const i = this.properties[e], { componentType: r, type: o } = i, l2 = this.data, c = l2[i.values], u2 = De(r, o), h = new u2(c), d = i.getIndexOffsetFromId(l2, t);
    if (Tn(o) || o === "ENUM")
      return bn(h, (d + s) * i.valueLength, o, n);
    if (o === "STRING") {
      let m = d + s, f = 0;
      if (i.stringOffsets !== null) {
        const { stringOffsets: g, stringOffsetType: y2 } = i, x2 = De(y2), M = new x2(l2[g]);
        f = M[m + 1] - M[m], m = M[m];
      }
      const p = new Uint8Array(h.buffer, m, f);
      n = new TextDecoder().decode(p);
    } else if (o === "BOOLEAN") {
      const m = d + s, f = Math.floor(m / 8), p = m % 8;
      n = (h[f] >> p & 1) === 1;
    }
    return n;
  }
  // Reads the data for the given table index
  getPropertyValue(e, t, s = null) {
    if (t >= this.count)
      throw new Error("PropertyTableAccessor: Requested index is outside the range of the table.");
    const n = this.properties[e];
    if (n) {
      if (!this.definition.properties[e])
        return n.resolveDefault(s);
    } else throw new Error("PropertyTableAccessor: Requested property does not exist.");
    const i = n.array, r = this.data, o = n.getArrayLengthFromId(r, t);
    if (s = n.shapeToProperty(s, o), i)
      for (let l2 = 0, c = s.length; l2 < c; l2++)
        s[l2] = this._readValueAtIndex(e, t, l2, s[l2]);
    else
      s = this._readValueAtIndex(e, t, 0, s);
    return s = n.adjustValueScaleOffset(s), s = n.resolveEnumsToStrings(s), s = n.resolveNoData(s), s;
  }
};
var Le = new Box2();
var ys = class {
  constructor() {
    this._renderer = new WebGLRenderer(), this._target = new WebGLRenderTarget(1, 1), this._texTarget = new WebGLRenderTarget(), this._quad = new FullScreenQuad(new ShaderMaterial({
      blending: CustomBlending,
      blendDst: ZeroFactor,
      blendSrc: OneFactor,
      uniforms: {
        map: { value: null },
        pixel: { value: new Vector2() }
      },
      vertexShader: (
        /* glsl */
        `
				void main() {

					gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

				}
			`
      ),
      fragmentShader: (
        /* glsl */
        `
				uniform sampler2D map;
				uniform ivec2 pixel;

				void main() {

					gl_FragColor = texelFetch( map, pixel, 0 );

				}
			`
      )
    }));
  }
  // increases the width of the target render target to support more data
  increaseSizeTo(e) {
    this._target.setSize(Math.max(this._target.width, e), 1);
  }
  // read data from the rendered texture asynchronously
  readDataAsync(e) {
    const { _renderer: t, _target: s } = this;
    return t.readRenderTargetPixelsAsync(s, 0, 0, e.length / 4, 1, e);
  }
  // read data from the rendered texture
  readData(e) {
    const { _renderer: t, _target: s } = this;
    t.readRenderTargetPixels(s, 0, 0, e.length / 4, 1, e);
  }
  // render a single pixel from the source at the destination point on the render target
  // takes the texture, pixel to read from, and pixel to render in to
  renderPixelToTarget(e, t, s) {
    const { _renderer: n, _target: i } = this;
    Le.min.copy(t), Le.max.copy(t), Le.max.x += 1, Le.max.y += 1, n.initRenderTarget(i), n.copyTextureToTexture(e, i.texture, Le, s, 0);
  }
};
var he = new class {
  constructor() {
    let a = null;
    Object.getOwnPropertyNames(ys.prototype).forEach((e) => {
      e !== "constructor" && (this[e] = (...t) => (a = a || new ys(), a[e](...t)));
    });
  }
}();
var xs = new Vector2();
var Ts = new Vector2();
var bs = new Vector2();
function Xi(a, e) {
  return e === 0 ? a.getAttribute("uv") : a.getAttribute(`uv${e}`);
}
function _n(a, e, t = new Array(3)) {
  let s = 3 * e, n = 3 * e + 1, i = 3 * e + 2;
  return a.index && (s = a.index.getX(s), n = a.index.getX(n), i = a.index.getX(i)), t[0] = s, t[1] = n, t[2] = i, t;
}
function Sn(a, e, t, s, n) {
  const [i, r, o] = s, l2 = Xi(a, e);
  xs.fromBufferAttribute(l2, i), Ts.fromBufferAttribute(l2, r), bs.fromBufferAttribute(l2, o), n.set(0, 0, 0).addScaledVector(xs, t.x).addScaledVector(Ts, t.y).addScaledVector(bs, t.z);
}
function Mn(a, e, t, s) {
  const n = a.x - Math.floor(a.x), i = a.y - Math.floor(a.y), r = Math.floor(n * e % e), o = Math.floor(i * t % t);
  return s.set(r, o), s;
}
var _s = new Vector2();
var Ss = new Vector2();
var Ms = new Vector2();
var Yi = class extends ut {
  constructor(e, t, s = null) {
    super(e, t, s), this.channels = q(s, "channels", [0]), this.index = q(s, "index", null), this.texCoord = q(s, "texCoord", null), this.valueLength = parseInt(this.type.replace(/[^0-9]/g, "")) || 1;
  }
  // takes the buffer to read from and the value index to read
  readDataFromBuffer(e, t, s = null) {
    const n = this.type;
    if (n === "BOOLEAN" || n === "STRING")
      throw new Error("PropertyTextureAccessor: BOOLEAN and STRING types not supported.");
    return bn(e, t * this.valueLength, n, s);
  }
};
var $i = class extends $t2 {
  constructor(...e) {
    super(...e), this.isPropertyTextureAccessor = true, this._asyncRead = false, this._initProperties(Yi);
  }
  // Reads the full set of property data
  getData(e, t, s, n = {}) {
    const i = this.properties;
    nt(i, n);
    const r = Object.keys(i), o = r.map((l2) => n[l2]);
    return this.getPropertyValuesAtTexel(r, e, t, s, o), r.forEach((l2, c) => n[l2] = o[c]), n;
  }
  // Reads the full set of property data asynchronously
  async getDataAsync(e, t, s, n = {}) {
    const i = this.properties;
    nt(i, n);
    const r = Object.keys(i), o = r.map((l2) => n[l2]);
    return await this.getPropertyValuesAtTexelAsync(r, e, t, s, o), r.forEach((l2, c) => n[l2] = o[c]), n;
  }
  // Reads values asynchronously
  getPropertyValuesAtTexelAsync(...e) {
    this._asyncRead = true;
    const t = this.getPropertyValuesAtTexel(...e);
    return this._asyncRead = false, t;
  }
  // Reads values from the textures synchronously
  getPropertyValuesAtTexel(e, t, s, n, i = []) {
    for (; i.length < e.length; ) i.push(null);
    i.length = e.length, he.increaseSizeTo(i.length);
    const r = this.data, o = this.definition.properties, l2 = this.properties, c = _n(n, t);
    for (let d = 0, m = e.length; d < m; d++) {
      const f = e[d];
      if (!o[f])
        continue;
      const p = l2[f], g = r[p.index];
      Sn(n, p.texCoord, s, c, _s), Mn(_s, g.image.width, g.image.height, Ss), Ms.set(d, 0), he.renderPixelToTarget(g, Ss, Ms);
    }
    const u2 = new Uint8Array(e.length * 4);
    if (this._asyncRead)
      return he.readDataAsync(u2).then(() => (h.call(this), i));
    return he.readData(u2), h.call(this), i;
    function h() {
      for (let d = 0, m = e.length; d < m; d++) {
        const f = e[d], p = l2[f], g = p.type;
        if (i[d] = Yt(p, i[d]), p) {
          if (!o[f]) {
            i[d] = p.resolveDefault(i);
            continue;
          }
        } else throw new Error("PropertyTextureAccessor: Requested property does not exist.");
        const y2 = p.valueLength * (p.count || 1), x2 = p.channels.map((_) => u2[4 * d + _]), M = p.componentType, T = De(M, g), S = new T(y2);
        if (new Uint8Array(S.buffer).set(x2), p.array) {
          const _ = i[d];
          for (let C2 = 0, L = _.length; C2 < L; C2++)
            _[C2] = p.readDataFromBuffer(S, C2, _[C2]);
        } else
          i[d] = p.readDataFromBuffer(S, 0, i[d]);
        i[d] = p.adjustValueScaleOffset(i[d]), i[d] = p.resolveEnumsToStrings(i[d]), i[d] = p.resolveNoData(i[d]);
      }
    }
  }
  // dispose all of the texture data used
  dispose() {
    this.data.forEach((e) => {
      e && (e.dispose(), e.image instanceof ImageBitmap && e.image.close());
    });
  }
};
var Cs = class {
  constructor(e, t, s, n = null, i = null) {
    const {
      schema: r,
      propertyTables: o = [],
      propertyTextures: l2 = [],
      propertyAttributes: c = []
    } = e, { enums: u2, classes: h } = r, d = o.map((p) => new ji(p, h, u2, s));
    let m = [], f = [];
    n && (n.propertyTextures && (m = n.propertyTextures.map((p) => new $i(l2[p], h, u2, t))), n.propertyAttributes && (f = n.propertyAttributes.map((p) => new qi(c[p], h, u2)))), this.schema = r, this.tableAccessors = d, this.textureAccessors = m, this.attributeAccessors = f, this.object = i, this.textures = t, this.nodeMetadata = n;
  }
  // Property Tables
  getPropertyTableData(e, t, s = null) {
    if (!Array.isArray(e) || !Array.isArray(t))
      s = s || {}, s = this.tableAccessors[e].getData(t, s);
    else {
      s = s || [];
      const n = Math.min(e.length, t.length);
      s.length = n;
      for (let i = 0; i < n; i++) {
        const r = this.tableAccessors[e[i]];
        s[i] = r.getData(t[i], s[i]);
      }
    }
    return s;
  }
  getPropertyTableInfo(e = null) {
    if (e === null && (e = this.tableAccessors.map((t, s) => s)), Array.isArray(e))
      return e.map((t) => {
        const s = this.tableAccessors[t];
        return {
          name: s.name,
          className: s.definition.class
        };
      });
    {
      const t = this.tableAccessors[e];
      return {
        name: t.name,
        className: t.definition.class
      };
    }
  }
  // Property Textures
  getPropertyTextureData(e, t, s = []) {
    const n = this.textureAccessors;
    s.length = n.length;
    for (let i = 0; i < n.length; i++) {
      const r = n[i];
      s[i] = r.getData(e, t, this.object.geometry, s[i]);
    }
    return s;
  }
  async getPropertyTextureDataAsync(e, t, s = []) {
    const n = this.textureAccessors;
    s.length = n.length;
    const i = [];
    for (let r = 0; r < n.length; r++) {
      const l2 = n[r].getDataAsync(e, t, this.object.geometry, s[r]).then((c) => {
        s[r] = c;
      });
      i.push(l2);
    }
    return await Promise.all(i), s;
  }
  getPropertyTextureInfo() {
    return this.textureAccessors;
  }
  // Property Attributes
  getPropertyAttributeData(e, t = []) {
    const s = this.attributeAccessors;
    t.length = s.length;
    for (let n = 0; n < s.length; n++) {
      const i = s[n];
      t[n] = i.getData(e, this.object.geometry, t[n]);
    }
    return t;
  }
  getPropertyAttributeInfo() {
    return this.attributeAccessors.map((e) => ({
      name: e.name,
      className: e.definition.class
    }));
  }
  dispose() {
    this.textureAccessors.forEach((e) => e.dispose()), this.tableAccessors.forEach((e) => e.dispose()), this.attributeAccessors.forEach((e) => e.dispose());
  }
};
var ve = "EXT_structural_metadata";
function Qi(a, e = []) {
  var n;
  const t = ((n = a.json.textures) == null ? void 0 : n.length) || 0, s = new Array(t).fill(null);
  return e.forEach(({ properties: i }) => {
    for (const r in i) {
      const { index: o } = i[r];
      s[o] === null && (s[o] = a.loadTexture(o));
    }
  }), Promise.all(s);
}
function Zi(a, e = []) {
  var n;
  const t = ((n = a.json.bufferViews) == null ? void 0 : n.length) || 0, s = new Array(t).fill(null);
  return e.forEach(({ properties: i }) => {
    for (const r in i) {
      const { values: o, arrayOffsets: l2, stringOffsets: c } = i[r];
      s[o] === null && (s[o] = a.loadBufferView(o)), s[l2] === null && (s[l2] = a.loadBufferView(l2)), s[c] === null && (s[c] = a.loadBufferView(c));
    }
  }), Promise.all(s);
}
var Ji = class {
  constructor(e) {
    this.parser = e, this.name = ve;
  }
  async afterRoot({ scene: e, parser: t }) {
    const s = t.json.extensionsUsed;
    if (!s || !s.includes(ve))
      return;
    let n = null, i = t.json.extensions[ve];
    if (i.schemaUri) {
      const { manager: c, path: u2, requestHeader: h, crossOrigin: d } = t.options, m = new URL(i.schemaUri, u2).toString(), f = new FileLoader(c);
      f.setCrossOrigin(d), f.setResponseType("json"), f.setRequestHeader(h), n = f.loadAsync(m).then((p) => {
        i = { ...i, schema: p };
      });
    }
    const [r, o] = await Promise.all([
      Qi(t, i.propertyTextures),
      Zi(t, i.propertyTables),
      n
    ]), l2 = new Cs(i, r, o);
    e.userData.structuralMetadata = l2, e.traverse((c) => {
      var u2;
      if (t.associations.has(c)) {
        const { meshes: h, primitives: d } = t.associations.get(c), m = (u2 = t.json.meshes[h]) == null ? void 0 : u2.primitives[d];
        if (m && m.extensions && m.extensions[ve]) {
          const f = m.extensions[ve];
          c.userData.structuralMetadata = new Cs(i, r, o, f, c);
        } else
          c.userData.structuralMetadata = l2;
      }
    });
  }
};
var As = new Vector2();
var Is = new Vector2();
var Ls = new Vector2();
function Ki(a) {
  return a.x > a.y && a.x > a.z ? 0 : a.y > a.z ? 1 : 2;
}
var er = class {
  constructor(e, t, s) {
    this.geometry = e, this.textures = t, this.data = s, this._asyncRead = false, this.featureIds = s.featureIds.map((n) => {
      const { texture: i, ...r } = n, o = {
        label: null,
        propertyTable: null,
        nullFeatureId: null,
        ...r
      };
      return i && (o.texture = {
        texCoord: 0,
        channels: [0],
        ...i
      }), o;
    });
  }
  // returns list of textures
  getTextures() {
    return this.textures;
  }
  // returns a set of info for each feature
  getFeatureInfo() {
    return this.featureIds;
  }
  // performs texture data read back asynchronously
  getFeaturesAsync(...e) {
    this._asyncRead = true;
    const t = this.getFeatures(...e);
    return this._asyncRead = false, t;
  }
  // returns all features for the given point on the given triangle
  getFeatures(e, t) {
    const { geometry: s, textures: n, featureIds: i } = this, r = new Array(i.length).fill(null), o = i.length;
    he.increaseSizeTo(o);
    const l2 = _n(s, e), c = l2[Ki(t)];
    for (let d = 0, m = i.length; d < m; d++) {
      const f = i[d], p = "nullFeatureId" in f ? f.nullFeatureId : null;
      if ("texture" in f) {
        const g = n[f.texture.index];
        Sn(s, f.texture.texCoord, t, l2, As), Mn(As, g.image.width, g.image.height, Is), Ls.set(d, 0), he.renderPixelToTarget(n[f.texture.index], Is, Ls);
      } else if ("attribute" in f) {
        const y2 = s.getAttribute(`_feature_id_${f.attribute}`).getX(c);
        y2 !== p && (r[d] = y2);
      } else {
        const g = c;
        g !== p && (r[d] = g);
      }
    }
    const u2 = new Uint8Array(o * 4);
    if (this._asyncRead)
      return he.readDataAsync(u2).then(() => (h(), r));
    return he.readData(u2), h(), r;
    function h() {
      const d = new Uint32Array(1);
      for (let m = 0, f = i.length; m < f; m++) {
        const p = i[m], g = "nullFeatureId" in p ? p.nullFeatureId : null;
        if ("texture" in p) {
          const { channels: y2 } = p.texture, x2 = y2.map((T) => u2[4 * m + T]);
          new Uint8Array(d.buffer).set(x2);
          const M = d[0];
          M !== g && (r[m] = M);
        }
      }
    }
  }
  // dispose all of the texture data used
  dispose() {
    this.textures.forEach((e) => {
      e && (e.dispose(), e.image instanceof ImageBitmap && e.image.close());
    });
  }
};
var it2 = "EXT_mesh_features";
function vs(a, e, t) {
  a.traverse((s) => {
    var n;
    if (e.associations.has(s)) {
      const { meshes: i, primitives: r } = e.associations.get(s), o = (n = e.json.meshes[i]) == null ? void 0 : n.primitives[r];
      o && o.extensions && o.extensions[it2] && t(s, o.extensions[it2]);
    }
  });
}
var tr = class {
  constructor(e) {
    this.parser = e, this.name = it2;
  }
  async afterRoot({ scene: e, parser: t }) {
    var o;
    const s = t.json.extensionsUsed;
    if (!s || !s.includes(it2))
      return;
    const n = ((o = t.json.textures) == null ? void 0 : o.length) || 0, i = new Array(n).fill(null);
    vs(e, t, (l2, { featureIds: c }) => {
      c.forEach((u2) => {
        if (u2.texture && i[u2.texture.index] === null) {
          const h = u2.texture.index;
          i[h] = t.loadTexture(h);
        }
      });
    });
    const r = await Promise.all(i);
    vs(e, t, (l2, c) => {
      l2.userData.meshFeatures = new er(l2.geometry, r, c);
    });
  }
};
var sr = class {
  constructor() {
    this.name = "CESIUM_RTC";
  }
  afterRoot(e) {
    if (e.parser.json.extensions && e.parser.json.extensions.CESIUM_RTC) {
      const { center: t } = e.parser.json.extensions.CESIUM_RTC;
      t && (e.scene.position.x += t[0], e.scene.position.y += t[1], e.scene.position.z += t[2]);
    }
  }
};
var ao = class {
  constructor(e) {
    e = {
      metadata: true,
      rtc: true,
      plugins: [],
      dracoLoader: null,
      ktxLoader: null,
      meshoptDecoder: null,
      autoDispose: true,
      ...e
    }, this.tiles = null, this.metadata = e.metadata, this.rtc = e.rtc, this.plugins = e.plugins, this.dracoLoader = e.dracoLoader, this.ktxLoader = e.ktxLoader, this.meshoptDecoder = e.meshoptDecoder, this._gltfRegex = /\.(gltf|glb)$/g, this._dracoRegex = /\.drc$/g, this._loader = null;
  }
  init(e) {
    const t = new GLTFLoader(e.manager);
    this.dracoLoader && (t.setDRACOLoader(this.dracoLoader), e.manager.addHandler(this._dracoRegex, this.dracoLoader)), this.ktxLoader && t.setKTX2Loader(this.ktxLoader), this.meshoptDecoder && t.setMeshoptDecoder(this.meshoptDecoder), this.rtc && t.register(() => new sr()), this.metadata && (t.register(() => new Ji()), t.register(() => new tr())), this.plugins.forEach((s) => t.register(s)), e.manager.addHandler(this._gltfRegex, t), this.tiles = e, this._loader = t;
  }
  dispose() {
    this.tiles.manager.removeHandler(this._gltfRegex), this.tiles.manager.removeHandler(this._dracoRegex), this.autoDispose && (this.ktxLoader.dispose(), this.dracoLoader.dispose());
  }
};
var qe = new Sphere();
var lo = class {
  constructor(e) {
    e = {
      up: "+z",
      recenter: true,
      lat: null,
      lon: null,
      height: 0,
      azimuth: 0,
      elevation: 0,
      roll: 0,
      ...e
    }, this.tiles = null, this.up = e.up.toLowerCase().replace(/\s+/, ""), this.lat = e.lat, this.lon = e.lon, this.height = e.height, this.azimuth = e.azimuth, this.elevation = e.elevation, this.roll = e.roll, this.recenter = e.recenter, this._callback = null;
  }
  init(e) {
    this.tiles = e, this._callback = () => {
      const { up: t, lat: s, lon: n, height: i, azimuth: r, elevation: o, roll: l2, recenter: c } = this;
      if (s !== null && n !== null)
        this.transformLatLonHeightToOrigin(s, n, i, r, o, l2);
      else {
        const { ellipsoid: u2 } = e, h = Math.min(...u2.radius);
        if (e.getBoundingSphere(qe), qe.center.length() > h * 0.5) {
          const d = {};
          u2.getPositionToCartographic(qe.center, d), this.transformLatLonHeightToOrigin(d.lat, d.lon, d.height);
        } else {
          const d = e.group;
          switch (d.rotation.set(0, 0, 0), t) {
            case "x":
            case "+x":
              d.rotation.z = Math.PI / 2;
              break;
            case "-x":
              d.rotation.z = -Math.PI / 2;
              break;
            case "y":
            case "+y":
              break;
            case "-y":
              d.rotation.z = Math.PI;
              break;
            case "z":
            case "+z":
              d.rotation.x = -Math.PI / 2;
              break;
            case "-z":
              d.rotation.x = Math.PI / 2;
              break;
          }
          e.group.position.copy(qe.center).applyEuler(d.rotation).multiplyScalar(-1);
        }
      }
      c || e.group.position.setScalar(0), e.removeEventListener("load-root-tileset", this._callback);
    }, e.addEventListener("load-root-tileset", this._callback), e.root && this._callback();
  }
  transformLatLonHeightToOrigin(e, t, s = 0, n = 0, i = 0, r = 0) {
    const { group: o, ellipsoid: l2 } = this.tiles;
    l2.getObjectFrame(e, t, s, n, i, r, o.matrix, U), o.matrix.invert().decompose(o.position, o.quaternion, o.scale), o.updateMatrixWorld();
  }
  dispose() {
    const { group: e } = this.tiles;
    e.position.setScalar(0), e.quaternion.identity(), e.scale.set(1, 1, 1), this.tiles.removeEventListener("load-root-tileset", this._callback);
  }
};
var co = class {
  set delay(e) {
    this.deferCallbacks.delay = e;
  }
  get delay() {
    return this.deferCallbacks.delay;
  }
  set bytesTarget(e) {
    this.lruCache.minBytesSize = e;
  }
  get bytesTarget() {
    return this.lruCache.minBytesSize;
  }
  get estimatedGpuBytes() {
    return this.lruCache.cachedBytes;
  }
  constructor(e = {}) {
    const {
      delay: t = 0,
      bytesTarget: s = 0
    } = e;
    this.name = "UNLOAD_TILES_PLUGIN", this.tiles = null, this.lruCache = new z(), this.deferCallbacks = new nr(), this.delay = t, this.bytesTarget = s;
  }
  init(e) {
    this.tiles = e;
    const { lruCache: t, deferCallbacks: s } = this;
    s.callback = (i) => {
      t.markUnused(i), t.scheduleUnload(false);
    };
    const n = (i) => {
      const r = i.engineData.scene;
      e.visibleTiles.has(i) || e.invokeOnePlugin((l2) => l2.unloadTileFromGPU && l2.unloadTileFromGPU(r, i));
    };
    this._onUpdateBefore = () => {
      t.unloadPriorityCallback = e.lruCache.unloadPriorityCallback, t.computeMemoryUsageCallback = e.lruCache.computeMemoryUsageCallback, t.minSize = 1 / 0, t.maxSize = 1 / 0, t.maxBytesSize = 1 / 0, t.unloadPercent = 1, t.autoMarkUnused = false;
    }, this._onVisibilityChangeCallback = ({ tile: i, visible: r }) => {
      r ? (t.add(i, n), e.markTileUsed(i), s.cancel(i)) : s.run(i);
    }, e.forEachLoadedModel((i, r) => {
      const o = e.visibleTiles.has(r);
      this._onVisibilityChangeCallback({ scene: i, visible: o });
    }), e.addEventListener("tile-visibility-change", this._onVisibilityChangeCallback), e.addEventListener("update-before", this._onUpdateBefore);
  }
  unloadTileFromGPU(e, t) {
    e && e.traverse((s) => {
      if (s.material) {
        const n = s.material;
        n.dispose();
        for (const i in n) {
          const r = n[i];
          r && r.isTexture && r.dispose();
        }
      }
      s.geometry && s.geometry.dispose();
    });
  }
  dispose() {
    this.tiles.removeEventListener("tile-visibility-change", this._onVisibilityChangeCallback), this.tiles.removeEventListener("update-before", this._onUpdateBefore), this.deferCallbacks.cancelAll();
  }
};
var nr = class {
  constructor(e = () => {
  }) {
    this.map = /* @__PURE__ */ new Map(), this.callback = e, this.delay = 0;
  }
  run(e) {
    const { map: t, delay: s } = this;
    if (t.has(e))
      throw new Error("DeferCallbackManager: Callback already initialized.");
    s === 0 ? this.callback(e) : t.set(e, setTimeout(() => this.callback(e), s));
  }
  cancel(e) {
    const { map: t } = this;
    t.has(e) && (clearTimeout(t.get(e)), t.delete(e));
  }
  cancelAll() {
    this.map.forEach((e, t) => {
      this.cancel(t);
    });
  }
};
var { clamp: bt } = MathUtils;
var ir = class {
  constructor() {
    this.duration = 250, this.fadeCount = 0, this._lastTick = -1, this._fadeState = /* @__PURE__ */ new Map(), this.onFadeComplete = null, this.onFadeStart = null, this.onFadeSetComplete = null, this.onFadeSetStart = null;
  }
  // delete the object from the fade, reset the material data
  deleteObject(e) {
    e && this.completeFade(e);
  }
  // Ensure we're storing a fade timer for the provided object
  // Returns whether a new state had to be added
  guaranteeState(e) {
    const t = this._fadeState;
    if (t.has(e))
      return false;
    const s = {
      fadeInTarget: 0,
      fadeOutTarget: 0,
      fadeIn: 0,
      fadeOut: 0
    };
    return t.set(e, s), true;
  }
  // Force the fade to complete in the direction it is already trending
  completeFade(e) {
    const t = this._fadeState;
    if (!t.has(e))
      return;
    const s = t.get(e).fadeOutTarget === 0;
    t.delete(e), this.fadeCount--, this.onFadeComplete && this.onFadeComplete(e, s), this.fadeCount === 0 && this.onFadeSetComplete && this.onFadeSetComplete();
  }
  completeAllFades() {
    this._fadeState.forEach((e, t) => {
      this.completeFade(t);
    });
  }
  forEachObject(e) {
    this._fadeState.forEach((t, s) => {
      e(s, t);
    });
  }
  // Fade the object in
  fadeIn(e) {
    const t = this.guaranteeState(e), s = this._fadeState.get(e);
    s.fadeInTarget = 1, s.fadeOutTarget = 0, s.fadeOut = 0, t && (this.fadeCount++, this.fadeCount === 1 && this.onFadeSetStart && this.onFadeSetStart(), this.onFadeStart && this.onFadeStart(e));
  }
  // Fade the object out
  fadeOut(e) {
    const t = this.guaranteeState(e), s = this._fadeState.get(e);
    s.fadeOutTarget = 1, t && (s.fadeInTarget = 1, s.fadeIn = 1, this.fadeCount++, this.fadeCount === 1 && this.onFadeSetStart && this.onFadeSetStart(), this.onFadeStart && this.onFadeStart(e));
  }
  isFading(e) {
    return this._fadeState.has(e);
  }
  isFadingOut(e) {
    const t = this._fadeState.get(e);
    return t && t.fadeOutTarget === 1;
  }
  // Tick the fade timer for each actively fading object
  update() {
    const e = window.performance.now();
    this._lastTick === -1 && (this._lastTick = e);
    const t = bt((e - this._lastTick) / this.duration, 0, 1);
    this._lastTick = e, this._fadeState.forEach((n, i) => {
      const {
        fadeOutTarget: r,
        fadeInTarget: o
      } = n;
      let {
        fadeOut: l2,
        fadeIn: c
      } = n;
      const u2 = Math.sign(o - c);
      c = bt(c + u2 * t, 0, 1);
      const h = Math.sign(r - l2);
      l2 = bt(l2 + h * t, 0, 1), n.fadeIn = c, n.fadeOut = l2, ((l2 === 1 || l2 === 0) && (c === 1 || c === 0) || l2 >= c) && this.completeFade(i);
    });
  }
};
var _t = /* @__PURE__ */ Symbol("FADE_PARAMS");
function Cn(a, e) {
  if (a[_t])
    return a[_t];
  const t = {
    fadeIn: { value: 0 },
    fadeOut: { value: 0 },
    fadeTexture: { value: null }
  };
  return a[_t] = t, a.defines = {
    ...a.defines || {},
    FEATURE_FADE: 0
  }, a.onBeforeCompile = (s) => {
    e && e(s), s.uniforms = {
      ...s.uniforms,
      ...t
    }, s.vertexShader = s.vertexShader.replace(
      /void\s+main\(\)\s+{/,
      (n) => (
        /* glsl */
        `
					#ifdef USE_BATCHING_FRAG

					varying float vBatchId;

					#endif

					${n}

						#ifdef USE_BATCHING_FRAG

						// add 0.5 to the value to avoid floating error that may cause flickering
						vBatchId = getIndirectIndex( gl_DrawID ) + 0.5;

						#endif
				`
      )
    ), s.fragmentShader = s.fragmentShader.replace(/void main\(/, (n) => (
      /* glsl */
      `
				#if FEATURE_FADE

				// adapted from https://www.shadertoy.com/view/Mlt3z8
				float bayerDither2x2( vec2 v ) {

					return mod( 3.0 * v.y + 2.0 * v.x, 4.0 );

				}

				float bayerDither4x4( vec2 v ) {

					vec2 P1 = mod( v, 2.0 );
					vec2 P2 = floor( 0.5 * mod( v, 4.0 ) );
					return 4.0 * bayerDither2x2( P1 ) + bayerDither2x2( P2 );

				}

				// the USE_BATCHING define is not available in fragment shaders
				#ifdef USE_BATCHING_FRAG

				// functions for reading the fade state of a given batch id
				uniform sampler2D fadeTexture;
				varying float vBatchId;
				vec2 getFadeValues( const in float i ) {

					int size = textureSize( fadeTexture, 0 ).x;
					int j = int( i );
					int x = j % size;
					int y = j / size;
					return texelFetch( fadeTexture, ivec2( x, y ), 0 ).rg;

				}

				#else

				uniform float fadeIn;
				uniform float fadeOut;

				#endif

				#endif

				${n}
			`
    )).replace(/#include <dithering_fragment>/, (n) => (
      /* glsl */
      `

				${n}

				#if FEATURE_FADE

				#ifdef USE_BATCHING_FRAG

				vec2 fadeValues = getFadeValues( vBatchId );
				float fadeIn = fadeValues.r;
				float fadeOut = fadeValues.g;

				#endif

				float bayerValue = bayerDither4x4( floor( mod( gl_FragCoord.xy, 4.0 ) ) );
				float bayerBins = 16.0;
				float dither = ( 0.5 + bayerValue ) / bayerBins;
				if ( dither >= fadeIn ) {

					discard;

				}

				if ( dither < fadeOut ) {

					discard;

				}

				#endif

			`
    ));
  }, t;
}
var rr = class {
  constructor() {
    this._fadeParams = /* @__PURE__ */ new WeakMap(), this.fading = 0;
  }
  // Set the fade parameters for the given scene
  setFade(e, t, s) {
    if (!e)
      return;
    const n = this._fadeParams;
    e.traverse((i) => {
      const r = i.material;
      if (r && n.has(r)) {
        const o = n.get(r);
        o.fadeIn.value = t, o.fadeOut.value = s;
        const u2 = +(!(t === 0 || t === 1) || !(s === 0 || s === 1));
        r.defines.FEATURE_FADE !== u2 && (this.fading += u2 === 1 ? 1 : -1, r.defines.FEATURE_FADE = u2, r.needsUpdate = true);
      }
    });
  }
  // initialize materials in the object
  prepareScene(e) {
    e.traverse((t) => {
      t.material && this.prepareMaterial(t.material);
    });
  }
  // delete the object from the fade, reset the material data
  deleteScene(e) {
    if (!e)
      return;
    this.setFade(e, 1, 0);
    const t = this._fadeParams;
    e.traverse((s) => {
      const n = s.material;
      n && t.delete(n);
    });
  }
  // initialize the material
  prepareMaterial(e) {
    const t = this._fadeParams;
    t.has(e) || t.set(e, Cn(e, e.onBeforeCompile));
  }
};
var or = class {
  constructor(e, t = new MeshBasicMaterial()) {
    this.other = e, this.material = t, this.visible = true, this.parent = null, this._instanceInfo = [], this._visibilityChanged = true;
    const s = new Proxy(this, {
      get(n, i) {
        if (i in n)
          return n[i];
        {
          const r = e[i];
          return r instanceof Function ? (...o) => (n.syncInstances(), r.call(s, ...o)) : e[i];
        }
      },
      set(n, i, r) {
        return i in n ? n[i] = r : e[i] = r, true;
      },
      deleteProperty(n, i) {
        return i in n ? delete n[i] : delete e[i];
      }
      // ownKeys() {},
      // has(target, key) {},
      // defineProperty(target, key, descriptor) {},
      // getOwnPropertyDescriptor(target, key) {},
    });
    return s;
  }
  syncInstances() {
    const e = this._instanceInfo, t = this.other._instanceInfo;
    for (; t.length > e.length; ) {
      const s = e.length;
      e.push(new Proxy({ visible: false }, {
        get(n, i) {
          return i in n ? n[i] : t[s][i];
        },
        set(n, i, r) {
          return i in n ? n[i] = r : t[s][i] = r, true;
        }
      }));
    }
  }
};
var ar = class extends or {
  constructor(...e) {
    super(...e);
    const t = this.material, s = Cn(t, t.onBeforeCompile);
    t.defines.FEATURE_FADE = 1, t.defines.USE_BATCHING_FRAG = 1, t.needsUpdate = true, this.fadeTexture = null, this._fadeParams = s;
  }
  // Set the fade state
  setFadeAt(e, t, s) {
    this._initFadeTexture(), this.fadeTexture.setValueAt(e, t * 255, s * 255);
  }
  // initialize the texture and resize it if needed
  _initFadeTexture() {
    let e = Math.sqrt(this._maxInstanceCount);
    e = Math.ceil(e);
    const t = e * e * 2, s = this.fadeTexture;
    if (!s || s.image.data.length !== t) {
      const n = new Uint8Array(t), i = new lr(n, e, e, RGFormat, UnsignedByteType);
      if (s) {
        s.dispose();
        const r = s.image.data, o = this.fadeTexture.image.data, l2 = Math.min(r.length, o.length);
        o.set(new r.constructor(r.buffer, 0, l2));
      }
      this.fadeTexture = i, this._fadeParams.fadeTexture.value = i, i.needsUpdate = true;
    }
  }
  // dispose the fade texture. Super cannot be used here due to proxy
  dispose() {
    this.fadeTexture && this.fadeTexture.dispose();
  }
};
var lr = class extends DataTexture {
  setValueAt(e, ...t) {
    const { data: s, width: n, height: i } = this.image, r = Math.floor(s.length / (n * i));
    let o = false;
    for (let l2 = 0; l2 < r; l2++) {
      const c = e * r + l2, u2 = s[c], h = t[l2] || 0;
      u2 !== h && (s[c] = h, o = true);
    }
    o && (this.needsUpdate = true);
  }
};
var Es = /* @__PURE__ */ Symbol("HAS_POPPED_IN");
var ws = new Vector3();
var Ps = new Vector3();
var Rs = new Quaternion();
var Ds = new Quaternion();
var Bs = new Vector3();
function cr() {
  const a = this._fadeManager, e = this.tiles;
  this._fadingBefore = a.fadeCount, this._displayActiveTiles = e.displayActiveTiles, e.displayActiveTiles = true;
}
function ur() {
  const a = this._fadeManager, e = this._fadeMaterialManager, t = this._displayActiveTiles, s = this._fadingBefore, n = this._prevCameraTransforms, { tiles: i, maximumFadeOutTiles: r, batchedMesh: o } = this, { cameras: l2 } = i;
  i.displayActiveTiles = t, a.update();
  const c = a.fadeCount;
  if (s !== 0 && c !== 0 && (i.dispatchEvent({ type: "fade-change" }), i.dispatchEvent({ type: "needs-render" })), t || i.visibleTiles.forEach((u2) => {
    const h = u2.engineData.scene;
    h && (h.visible = u2.traversal.inFrustum), this.forEachBatchIds(u2, (d, m, f) => {
      m.setVisibleAt(d, u2.traversal.inFrustum), f.batchedMesh.setVisibleAt(d, u2.traversal.inFrustum);
    });
  }), r < this._fadingOutCount) {
    let u2 = true;
    l2.forEach((h) => {
      if (!n.has(h))
        return;
      const d = h.matrixWorld, m = n.get(h);
      d.decompose(Ps, Ds, Bs), m.decompose(ws, Rs, Bs);
      const f = Ds.angleTo(Rs), p = Ps.distanceTo(ws);
      u2 = u2 && (f > 0.25 || p > 0.1);
    }), u2 && a.completeAllFades();
  }
  if (l2.forEach((u2) => {
    n.get(u2).copy(u2.matrixWorld);
  }), a.forEachObject((u2, { fadeIn: h, fadeOut: d }) => {
    const m = u2.engineData.scene, f = a.isFadingOut(u2);
    i.markTileUsed(u2), m && (e.setFade(m, h, d), f && (m.visible = true)), this.forEachBatchIds(u2, (p, g, y2) => {
      g.setFadeAt(p, h, d), g.setVisibleAt(p, true), y2.batchedMesh.setVisibleAt(p, false);
    });
  }), o) {
    const u2 = i.getPluginByName("BATCHED_TILES_PLUGIN").batchedMesh.material;
    o.material.map = u2.map;
  }
}
var uo = class {
  get fadeDuration() {
    return this._fadeManager.duration;
  }
  set fadeDuration(e) {
    this._fadeManager.duration = Number(e);
  }
  get fadingTiles() {
    return this._fadeManager.fadeCount;
  }
  constructor(e) {
    e = {
      maximumFadeOutTiles: 50,
      fadeRootTiles: false,
      fadeDuration: 250,
      ...e
    }, this.name = "FADE_TILES_PLUGIN", this.priority = -2, this.tiles = null, this.batchedMesh = null, this._quickFadeTiles = /* @__PURE__ */ new Set(), this._fadeManager = new ir(), this._fadeMaterialManager = new rr(), this._prevCameraTransforms = null, this._fadingOutCount = 0, this.maximumFadeOutTiles = e.maximumFadeOutTiles, this.fadeRootTiles = e.fadeRootTiles, this.fadeDuration = e.fadeDuration;
  }
  init(e) {
    this._onLoadModel = ({ scene: n }) => {
      this._fadeMaterialManager.prepareScene(n);
    }, this._onDisposeModel = ({ tile: n, scene: i }) => {
      this.tiles.visibleTiles.has(n) && this._quickFadeTiles.add(n.parent), this._fadeManager.deleteObject(n), this._fadeMaterialManager.deleteScene(i);
    }, this._onAddCamera = ({ camera: n }) => {
      this._prevCameraTransforms.set(n, new Matrix4());
    }, this._onDeleteCamera = ({ camera: n }) => {
      this._prevCameraTransforms.delete(n);
    }, this._onTileVisibilityChange = ({ tile: n, visible: i }) => {
      const r = n.engineData.scene;
      r && (r.visible = true), this.forEachBatchIds(n, (o, l2, c) => {
        l2.setFadeAt(o, 0, 0), l2.setVisibleAt(o, false), c.batchedMesh.setVisibleAt(o, false);
      });
    }, this._onUpdateBefore = () => {
      cr.call(this);
    }, this._onUpdateAfter = () => {
      ur.call(this);
    }, e.addEventListener("load-model", this._onLoadModel), e.addEventListener("dispose-model", this._onDisposeModel), e.addEventListener("add-camera", this._onAddCamera), e.addEventListener("delete-camera", this._onDeleteCamera), e.addEventListener("update-before", this._onUpdateBefore), e.addEventListener("update-after", this._onUpdateAfter), e.addEventListener("tile-visibility-change", this._onTileVisibilityChange);
    const t = this._fadeManager;
    t.onFadeSetStart = () => {
      e.dispatchEvent({ type: "fade-start" }), e.dispatchEvent({ type: "needs-render" });
    }, t.onFadeSetComplete = () => {
      e.dispatchEvent({ type: "fade-end" }), e.dispatchEvent({ type: "needs-render" });
    }, t.onFadeComplete = (n, i) => {
      this._fadeMaterialManager.setFade(n.engineData.scene, 0, 0), this.forEachBatchIds(n, (r, o, l2) => {
        o.setFadeAt(r, 0, 0), o.setVisibleAt(r, false), l2.batchedMesh.setVisibleAt(r, i);
      }), i || (e.invokeOnePlugin((r) => r !== this && r.setTileVisible && r.setTileVisible(n, false)), this._fadingOutCount--);
    };
    const s = /* @__PURE__ */ new Map();
    e.cameras.forEach((n) => {
      s.set(n, new Matrix4());
    }), e.forEachLoadedModel((n, i) => {
      this._onLoadModel({ scene: n });
    }), this.tiles = e, this._fadeManager = t, this._prevCameraTransforms = s;
  }
  // initializes the batched mesh if it needs to be, dispose if it it's no longer needed
  initBatchedMesh() {
    var t;
    const e = (t = this.tiles.getPluginByName("BATCHED_TILES_PLUGIN")) == null ? void 0 : t.batchedMesh;
    if (e) {
      if (this.batchedMesh === null) {
        this._onBatchedMeshDispose = () => {
          this.batchedMesh.dispose(), this.batchedMesh.removeFromParent(), this.batchedMesh = null, e.removeEventListener("dispose", this._onBatchedMeshDispose);
        };
        const s = e.material.clone();
        s.onBeforeCompile = e.material.onBeforeCompile, this.batchedMesh = new ar(e, s), this.tiles.group.add(this.batchedMesh);
      }
    } else
      this.batchedMesh !== null && (this._onBatchedMeshDispose(), this._onBatchedMeshDispose = null);
  }
  // callback for fading to prevent tiles from being removed until the fade effect has completed
  setTileVisible(e, t) {
    const s = this._fadeManager, n = s.isFading(e);
    if (s.isFadingOut(e) && this._fadingOutCount--, t ? e.internal.depthFromRenderedParent === 1 ? ((e[Es] || this.fadeRootTiles) && this._fadeManager.fadeIn(e), e[Es] = true) : this._fadeManager.fadeIn(e) : (this._fadingOutCount++, s.fadeOut(e)), this._quickFadeTiles.has(e) && (this._fadeManager.completeFade(e), this._quickFadeTiles.delete(e)), n)
      return true;
    const i = this._fadeManager.isFading(e);
    return !!(!t && i);
  }
  dispose() {
    const e = this.tiles;
    this._fadeManager.completeAllFades(), this.batchedMesh !== null && this._onBatchedMeshDispose(), e.removeEventListener("load-model", this._onLoadModel), e.removeEventListener("dispose-model", this._onDisposeModel), e.removeEventListener("add-camera", this._onAddCamera), e.removeEventListener("delete-camera", this._onDeleteCamera), e.removeEventListener("update-before", this._onUpdateBefore), e.removeEventListener("update-after", this._onUpdateAfter), e.removeEventListener("tile-visibility-change", this._onTileVisibilityChange), e.forEachLoadedModel((t, s) => {
      this._fadeManager.deleteObject(s), t && (t.visible = true);
    });
  }
  // helper for iterating over the batch ids for a given tile
  forEachBatchIds(e, t) {
    if (this.initBatchedMesh(), this.batchedMesh) {
      const s = this.tiles.getPluginByName("BATCHED_TILES_PLUGIN"), n = s.getTileBatchIds(e);
      n && n.forEach((i) => {
        t(i, this.batchedMesh, s);
      });
    }
  }
};
var St = new Matrix4();
var Os = new Vector3();
var Us = new Vector3();
var hr = class extends BatchedMesh {
  constructor(...e) {
    super(...e), this.resetDistance = 1e4, this._matricesTextureHandle = null, this._lastCameraPos = new Matrix4(), this._forceUpdate = true, this._matrices = [];
  }
  setMatrixAt(e, t) {
    super.setMatrixAt(e, t), this._forceUpdate = true;
    const s = this._matrices;
    for (; s.length <= e; )
      s.push(new Matrix4());
    s[e].copy(t);
  }
  setInstanceCount(...e) {
    super.setInstanceCount(...e);
    const t = this._matrices;
    for (; t.length > this.instanceCount; )
      t.pop();
  }
  onBeforeRender(e, t, s, n, i, r) {
    super.onBeforeRender(e, t, s, n, i, r), Os.setFromMatrixPosition(s.matrixWorld), Us.setFromMatrixPosition(this._lastCameraPos);
    const o = this._matricesTexture;
    let l2 = this._modelViewMatricesTexture;
    if ((!l2 || l2.image.width !== o.image.width || l2.image.height !== o.image.height) && (l2 && l2.dispose(), l2 = o.clone(), l2.source = new Source({
      ...l2.image,
      data: l2.image.data.slice()
    }), this._modelViewMatricesTexture = l2), this._forceUpdate || Os.distanceTo(Us) > this.resetDistance) {
      const c = this._matrices, u2 = l2.image.data;
      for (let h = 0; h < this.maxInstanceCount; h++) {
        const d = c[h];
        d ? St.copy(d) : St.identity(), St.premultiply(this.matrixWorld).premultiply(s.matrixWorldInverse).toArray(u2, h * 16);
      }
      l2.needsUpdate = true, this._lastCameraPos.copy(s.matrixWorld), this._forceUpdate = false;
    }
    this._matricesTextureHandle = this._matricesTexture, this._matricesTexture = this._modelViewMatricesTexture, this.matrixWorld.copy(this._lastCameraPos);
  }
  onAfterRender() {
    this.updateMatrixWorld(), this._matricesTexture = this._matricesTextureHandle, this._matricesTextureHandle = null;
  }
  onAfterShadow(e, t, s, n, i, r) {
    this.onAfterRender(e, null, n, i, r);
  }
  dispose() {
    super.dispose(), this._modelViewMatricesTexture && this._modelViewMatricesTexture.dispose();
  }
};
var ee = new Mesh();
var We = [];
var dr = class extends hr {
  constructor(...e) {
    super(...e), this.expandPercent = 0.25, this.maxInstanceExpansionSize = 1 / 0, this._freeGeometryIds = [];
  }
  // Finds a free id that can fit the geometry with the requested ranges. Returns -1 if it could not be found.
  findFreeId(e, t, s) {
    const n = !!this.geometry.index, i = Math.max(n ? e.index.count : -1, s), r = Math.max(e.attributes.position.count, t);
    let o = -1, l2 = 1 / 0;
    const c = this._freeGeometryIds;
    if (c.forEach((u2, h) => {
      const d = this.getGeometryRangeAt(u2), { reservedIndexCount: m, reservedVertexCount: f } = d;
      if (m >= i && f >= r) {
        const p = i - m + (r - f);
        p < l2 && (o = h, l2 = p);
      }
    }), o !== -1) {
      const u2 = c[o];
      return c.splice(o, 1), u2;
    } else
      return -1;
  }
  // Overrides addGeometry to find an option geometry slot, expand, or optimized if needed
  addGeometry(e, t, s) {
    const n = !!this.geometry.index;
    s = Math.max(n ? e.index.count : -1, s), t = Math.max(e.attributes.position.count, t);
    const { expandPercent: i, _freeGeometryIds: r } = this;
    let o = this.findFreeId(e, t, s);
    if (o !== -1)
      this.setGeometryAt(o, e);
    else {
      const l2 = () => {
        const h = this.unusedVertexCount < t, d = this.unusedIndexCount < s;
        return h || d;
      }, c = e.index, u2 = e.attributes.position;
      if (t = Math.max(t, u2.count), s = Math.max(s, c ? c.count : 0), l2() && (r.forEach((h) => this.deleteGeometry(h)), r.length = 0, this.optimize(), l2())) {
        const h = this.geometry.index, d = this.geometry.attributes.position;
        let m, f;
        if (h) {
          const p = Math.ceil(i * h.count);
          m = Math.max(p, s, c.count) + h.count;
        } else
          m = Math.max(this.unusedIndexCount, s);
        if (d) {
          const p = Math.ceil(i * d.count);
          f = Math.max(p, t, u2.count) + d.count;
        } else
          f = Math.max(this.unusedVertexCount, t);
        this.setGeometrySize(f, m);
      }
      o = super.addGeometry(e, t, s);
    }
    return o;
  }
  // add an instance and automatically expand the number of instances if necessary
  addInstance(e) {
    if (this.maxInstanceCount === this.instanceCount) {
      const t = Math.ceil(this.maxInstanceCount * (1 + this.expandPercent));
      this.setInstanceCount(Math.min(t, this.maxInstanceExpansionSize));
    }
    return super.addInstance(e);
  }
  // delete an instance, keeping note that the geometry id is now unused
  deleteInstance(e) {
    const t = this.getGeometryIdAt(e);
    return t !== -1 && this._freeGeometryIds.push(t), super.deleteInstance(e);
  }
  // add a function for raycasting per tile
  raycastInstance(e, t, s) {
    const n = this.geometry, i = this.getGeometryIdAt(e);
    ee.material = this.material, ee.geometry.index = n.index, ee.geometry.attributes = n.attributes;
    const r = this.getGeometryRangeAt(i);
    ee.geometry.setDrawRange(r.start, r.count), ee.geometry.boundingBox === null && (ee.geometry.boundingBox = new Box3()), ee.geometry.boundingSphere === null && (ee.geometry.boundingSphere = new Sphere()), this.getMatrixAt(e, ee.matrixWorld).premultiply(this.matrixWorld), this.getBoundingBoxAt(i, ee.geometry.boundingBox), this.getBoundingSphereAt(i, ee.geometry.boundingSphere), ee.raycast(t, We);
    for (let o = 0, l2 = We.length; o < l2; o++) {
      const c = We[o];
      c.object = this, c.batchId = e, s.push(c);
    }
    We.length = 0;
  }
};
function pr(a) {
  return a.r === 1 && a.g === 1 && a.b === 1;
}
function fr(a) {
  a.needsUpdate = true, a.onBeforeCompile = (e) => {
    e.vertexShader = e.vertexShader.replace(
      "#include <common>",
      /* glsl */
      `
				#include <common>
				varying float texture_index;
				`
    ).replace(
      "#include <uv_vertex>",
      /* glsl */
      `
				#include <uv_vertex>
				texture_index = getIndirectIndex( gl_DrawID );
				`
    ), e.fragmentShader = e.fragmentShader.replace(
      "#include <map_pars_fragment>",
      /* glsl */
      `
				#ifdef USE_MAP
				precision highp sampler2DArray;
				uniform sampler2DArray map;
				varying float texture_index;
				#endif
				`
    ).replace(
      "#include <map_fragment>",
      /* glsl */
      `
				#ifdef USE_MAP
					diffuseColor *= texture( map, vec3( vMapUv, texture_index ) );
				#endif
				`
    );
  };
}
var Mt2 = new FullScreenQuad(new MeshBasicMaterial());
var Vt = new DataTexture(new Uint8Array([255, 255, 255, 255]), 1, 1);
Vt.needsUpdate = true;
var ho = class {
  constructor(e = {}) {
    if (parseInt(REVISION) < 170)
      throw new Error("BatchedTilesPlugin: Three.js revision 170 or higher required.");
    e = {
      instanceCount: 500,
      vertexCount: 750,
      indexCount: 2e3,
      expandPercent: 0.25,
      maxInstanceCount: 1 / 0,
      discardOriginalContent: true,
      textureSize: null,
      material: null,
      renderer: null,
      ...e
    }, this.name = "BATCHED_TILES_PLUGIN", this.priority = -1;
    const t = e.renderer.getContext();
    this.instanceCount = e.instanceCount, this.vertexCount = e.vertexCount, this.indexCount = e.indexCount, this.material = e.material ? e.material.clone() : null, this.expandPercent = e.expandPercent, this.maxInstanceCount = Math.min(e.maxInstanceCount, t.getParameter(t.MAX_3D_TEXTURE_SIZE)), this.renderer = e.renderer, this.discardOriginalContent = e.discardOriginalContent, this.textureSize = e.textureSize, this.batchedMesh = null, this.arrayTarget = null, this.tiles = null, this._onLoadModel = null, this._onDisposeModel = null, this._onVisibilityChange = null, this._tileToInstanceId = /* @__PURE__ */ new Map();
  }
  init(e) {
    this._onDisposeModel = ({ scene: t, tile: s }) => {
      this.removeSceneFromBatchedMesh(t, s);
    }, e.addEventListener("dispose-model", this._onDisposeModel), this.tiles = e;
  }
  initTextureArray(e) {
    if (this.arrayTarget !== null || e.material.map === null)
      return;
    const { instanceCount: t, renderer: s, textureSize: n, batchedMesh: i } = this, r = e.material.map, o = {
      colorSpace: r.colorSpace,
      wrapS: r.wrapS,
      wrapT: r.wrapT,
      wrapR: r.wrapS,
      // TODO: Generating mipmaps for the volume every time a new texture is added is extremely slow
      // generateMipmaps: map.generateMipmaps,
      // minFilter: map.minFilter,
      magFilter: r.magFilter
    }, l2 = new WebGLArrayRenderTarget(n || r.image.width, n || r.image.height, t);
    Object.assign(l2.texture, o), s.initRenderTarget(l2), i.material.map = l2.texture, this.arrayTarget = l2, this._tileToInstanceId.forEach((c) => {
      c.forEach((u2) => {
        this.assignTextureToLayer(Vt, u2);
      });
    });
  }
  // init the batched mesh if it's not ready
  initBatchedMesh(e) {
    if (this.batchedMesh !== null)
      return;
    const { instanceCount: t, vertexCount: s, indexCount: n, tiles: i } = this, r = this.material ? this.material : new e.material.constructor(), o = new dr(t, t * s, t * n, r);
    o.name = "BatchTilesPlugin", o.frustumCulled = false, i.group.add(o), o.updateMatrixWorld(), fr(o.material), this.batchedMesh = o;
  }
  setTileVisible(e, t) {
    const s = e.engineData.scene;
    if (t && this.addSceneToBatchedMesh(s, e), this._tileToInstanceId.has(e)) {
      this._tileToInstanceId.get(e).forEach((r) => {
        this.batchedMesh.setVisibleAt(r, t);
      });
      const i = this.tiles;
      return t ? i.visibleTiles.add(e) : i.visibleTiles.delete(e), i.dispatchEvent({
        type: "tile-visibility-change",
        scene: s,
        tile: e,
        visible: t
      }), true;
    }
    return false;
  }
  unloadTileFromGPU(e, t) {
    return !this.discardOriginalContent && this._tileToInstanceId.has(t) ? (this.removeSceneFromBatchedMesh(e, t), true) : false;
  }
  // render the given into the given layer
  assignTextureToLayer(e, t) {
    if (!this.arrayTarget)
      return;
    this.expandArrayTargetIfNeeded();
    const { renderer: s } = this, n = s.getRenderTarget();
    s.setRenderTarget(this.arrayTarget, t), Mt2.material.map = e, Mt2.render(s), s.setRenderTarget(n), Mt2.material.map = null, e.dispose();
  }
  // check if the array texture target needs to be expanded
  expandArrayTargetIfNeeded() {
    const { batchedMesh: e, arrayTarget: t, renderer: s } = this, n = Math.min(e.maxInstanceCount, this.maxInstanceCount);
    if (n > t.depth) {
      const i = {
        colorSpace: t.texture.colorSpace,
        wrapS: t.texture.wrapS,
        wrapT: t.texture.wrapT,
        generateMipmaps: t.texture.generateMipmaps,
        minFilter: t.texture.minFilter,
        magFilter: t.texture.magFilter
      }, r = new WebGLArrayRenderTarget(t.width, t.height, n);
      Object.assign(r.texture, i), s.initRenderTarget(r), s.copyTextureToTexture(t.texture, r.texture), t.dispose(), e.material.map = r.texture, this.arrayTarget = r;
    }
  }
  removeSceneFromBatchedMesh(e, t) {
    if (this._tileToInstanceId.has(t)) {
      const s = this._tileToInstanceId.get(t);
      this._tileToInstanceId.delete(t), s.forEach((n) => {
        this.batchedMesh.deleteInstance(n);
      });
    }
  }
  addSceneToBatchedMesh(e, t) {
    if (this._tileToInstanceId.has(t))
      return;
    const s = [];
    e.traverse((r) => {
      r.isMesh && s.push(r);
    });
    let n = true;
    s.forEach((r) => {
      if (this.batchedMesh && n) {
        const o = r.geometry.attributes, l2 = this.batchedMesh.geometry.attributes;
        for (const c in l2)
          if (!(c in o)) {
            n = false;
            return;
          }
      }
    });
    const i = !this.batchedMesh || this.batchedMesh.instanceCount + s.length <= this.maxInstanceCount;
    if (n && i) {
      e.updateMatrixWorld();
      const r = [];
      this._tileToInstanceId.set(t, r), s.forEach((o) => {
        this.initBatchedMesh(o), this.initTextureArray(o);
        const { geometry: l2, material: c } = o, { batchedMesh: u2, expandPercent: h } = this;
        u2.expandPercent = h;
        const d = u2.addGeometry(l2, this.vertexCount, this.indexCount), m = u2.addInstance(d);
        r.push(m), u2.setMatrixAt(m, o.matrixWorld), u2.setVisibleAt(m, false), pr(c.color) || (c.color.setHSL(Math.random(), 0.5, 0.5), u2.setColorAt(m, c.color));
        const f = c.map;
        f ? this.assignTextureToLayer(f, m) : this.assignTextureToLayer(Vt, m);
      }), this.discardOriginalContent && (t.engineData.textures.forEach((o) => {
        o.image instanceof ImageBitmap && o.image.close();
      }), t.engineData.scene = null, t.engineData.materials = [], t.engineData.geometries = [], t.engineData.textures = []);
    }
  }
  // Override raycasting per tile to defer to the batched mesh
  raycastTile(e, t, s, n) {
    return this._tileToInstanceId.has(e) ? (this._tileToInstanceId.get(e).forEach((r) => {
      this.batchedMesh.raycastInstance(r, s, n);
    }), true) : false;
  }
  dispose() {
    const { arrayTarget: e, tiles: t, batchedMesh: s } = this;
    e && e.dispose(), s && (s.material.dispose(), s.geometry.dispose(), s.dispose(), s.removeFromParent()), t.removeEventListener("dispose-model", this._onDisposeModel);
  }
  getTileBatchIds(e) {
    return this._tileToInstanceId.get(e);
  }
};
var Ct = new Sphere();
var je = new Vector3();
var Ee = new Matrix4();
var Ns = new Matrix4();
var At = new Raycaster();
var mr = new MeshBasicMaterial({ side: DoubleSide });
var Vs = new Box3();
var It = 1e5;
function Fs(a, e) {
  return a.isBufferGeometry ? (a.boundingSphere === null && a.computeBoundingSphere(), e.copy(a.boundingSphere)) : (Vs.setFromObject(a), Vs.getBoundingSphere(e), e);
}
var po = class {
  constructor() {
    this.name = "TILE_FLATTENING_PLUGIN", this.priority = -100, this.tiles = null, this.shapes = /* @__PURE__ */ new Map(), this.positionsMap = /* @__PURE__ */ new Map(), this.positionsUpdated = /* @__PURE__ */ new Set(), this.needsUpdate = false;
  }
  init(e) {
    this.tiles = e, this.needsUpdate = true, this._updateBeforeCallback = () => {
      this.needsUpdate && (this._updateTiles(), this.needsUpdate = false);
    }, this._disposeModelCallback = ({ tile: t }) => {
      this.positionsMap.delete(t), this.positionsUpdated.delete(t);
    }, e.addEventListener("update-before", this._updateBeforeCallback), e.addEventListener("dispose-model", this._disposeModelCallback);
  }
  // update tile flattening state if it has not been made visible, yet
  setTileActive(e, t) {
    t && !this.positionsUpdated.has(e) && this._updateTile(e);
  }
  _updateTile(e) {
    const { positionsUpdated: t, positionsMap: s, shapes: n, tiles: i } = this;
    t.add(e);
    const r = e.engineData.scene;
    if (s.has(e)) {
      const o = s.get(e);
      r.traverse((l2) => {
        if (l2.geometry) {
          const c = o.get(l2.geometry);
          c && (l2.geometry.attributes.position.array.set(c), l2.geometry.attributes.position.needsUpdate = true);
        }
      });
    } else {
      const o = /* @__PURE__ */ new Map();
      s.set(e, o), r.traverse((l2) => {
        l2.geometry && o.set(l2.geometry, l2.geometry.attributes.position.array.slice());
      });
    }
    r.updateMatrixWorld(true), r.traverse((o) => {
      const { geometry: l2 } = o;
      l2 && (Ee.copy(o.matrixWorld), r.parent !== null && Ee.premultiply(i.group.matrixWorldInverse), Ns.copy(Ee).invert(), Fs(l2, Ct).applyMatrix4(Ee), n.forEach(({
        shape: c,
        direction: u2,
        sphere: h,
        thresholdMode: d,
        threshold: m,
        flattenRange: f
      }) => {
        je.subVectors(Ct.center, h.center), je.addScaledVector(u2, -u2.dot(je));
        const p = (Ct.radius + h.radius) ** 2;
        if (je.lengthSq() > p)
          return;
        const { position: g } = l2.attributes, { ray: y2 } = At;
        y2.direction.copy(u2).multiplyScalar(-1);
        for (let x2 = 0, M = g.count; x2 < M; x2++) {
          y2.origin.fromBufferAttribute(g, x2).applyMatrix4(Ee).addScaledVector(u2, It), At.far = It;
          const T = At.intersectObject(c)[0];
          if (T) {
            let S = (It - T.distance) / m;
            const _ = S >= 1;
            (!_ || _ && d === "flatten") && (S = Math.min(S, 1), T.point.addScaledVector(y2.direction, MathUtils.mapLinear(S, 0, 1, -f, 0)), T.point.applyMatrix4(Ns), g.setXYZ(x2, ...T.point));
          }
        }
      }));
    }), this.tiles.dispatchEvent({ type: "needs-render" });
  }
  _updateTiles() {
    this.positionsUpdated.clear(), this.tiles.activeTiles.forEach((e) => this._updateTile(e));
  }
  // API for updating and shapes to flatten the vertices
  hasShape(e) {
    return this.shapes.has(e);
  }
  addShape(e, t = new Vector3(0, 0, -1), s = {}) {
    if (this.hasShape(e))
      throw new Error("TileFlatteningPlugin: Shape is already used.");
    typeof s == "number" && (console.warn('TileFlatteningPlugin: "addShape" function signature has changed. Please use an options object, instead.'), s = {
      threshold: s
    }), this.needsUpdate = true;
    const n = e.clone();
    n.updateMatrixWorld(true), n.traverse((r) => {
      r.material && (r.material = mr);
    });
    const i = Fs(n, new Sphere());
    this.shapes.set(e, {
      shape: n,
      direction: t.clone(),
      sphere: i,
      // "flatten": Flattens the vertices above the shape
      // "none": leaves the vertices above the shape as they are
      thresholdMode: "none",
      // only flatten within this range above the object
      threshold: 1 / 0,
      // the range to flatten vertices in to. 0 is completely flat
      // while 0.1 means a 10cm range.
      flattenRange: 0,
      ...s
    });
  }
  updateShape(e) {
    if (!this.hasShape(e))
      throw new Error("TileFlatteningPlugin: Shape is not present.");
    const { direction: t, threshold: s, thresholdMode: n, flattenRange: i } = this.shapes.get(e);
    this.deleteShape(e), this.addShape(e, t, {
      threshold: s,
      thresholdMode: n,
      flattenRange: i
    });
  }
  deleteShape(e) {
    return this.needsUpdate = true, this.shapes.delete(e);
  }
  clearShapes() {
    this.shapes.size !== 0 && (this.needsUpdate = true, this.shapes.clear());
  }
  // reset the vertex positions and remove the update callback
  dispose() {
    this.tiles.removeEventListener("before-update", this._updateBeforeCallback), this.tiles.removeEventListener("dispose-model", this._disposeModelCallback), this.positionsMap.forEach((e) => {
      e.forEach((t, s) => {
        const { position: n } = s.attributes;
        n.array.set(t), n.needsUpdate = true;
      });
    });
  }
};
var gr = class extends Ne {
  constructor(e = {}) {
    const {
      subdomains: t = ["t0"],
      ...s
    } = e;
    super(s), this.subdomains = t, this.subDomainIndex = 0;
  }
  getUrl(e, t, s) {
    return this.url.replace(/{\s*subdomain\s*}/gi, this._getSubdomain()).replace(/{\s*quadkey\s*}/gi, this._tileToQuadKey(e, t, s));
  }
  _tileToQuadKey(e, t, s) {
    let n = "";
    for (let i = s; i > 0; i--) {
      let r = 0;
      const o = 1 << i - 1;
      (e & o) !== 0 && (r += 1), (t & o) !== 0 && (r += 2), n += r.toString();
    }
    return n;
  }
  _getSubdomain() {
    return this.subDomainIndex = (this.subDomainIndex + 1) % this.subdomains.length, this.subdomains[this.subDomainIndex];
  }
};
function Lt(a, e, t, s) {
  let [n, i, r, o] = a;
  i += 1e-8, n += 1e-8, o -= 1e-8, r -= 1e-8;
  const l2 = Math.max(Math.min(e, t.maxLevel), t.minLevel), [c, u2, h, d] = t.getTilesInRange(n, i, r, o, l2, true);
  for (let m = c; m <= h; m++)
    for (let f = u2; f <= d; f++)
      s(m, f, l2);
}
function yr(a, e, t) {
  const s = new Vector3(), n = {}, i = [], r = a.getAttribute("position");
  a.computeBoundingBox(), a.boundingBox.getCenter(s).applyMatrix4(e), t.getPositionToCartographic(s, n);
  const o = n.lat, l2 = n.lon;
  let c = 1 / 0, u2 = 1 / 0, h = 1 / 0, d = -1 / 0, m = -1 / 0, f = -1 / 0;
  for (let y2 = 0; y2 < r.count; y2++)
    s.fromBufferAttribute(r, y2).applyMatrix4(e), t.getPositionToCartographic(s, n), Math.abs(Math.abs(n.lat) - Math.PI / 2) < 1e-5 && (n.lon = l2), Math.abs(l2 - n.lon) > Math.PI && (n.lon += Math.sign(l2 - n.lon) * Math.PI * 2), Math.abs(o - n.lat) > Math.PI && (n.lat += Math.sign(o - n.lat) * Math.PI * 2), i.push(n.lon, n.lat, n.height), c = Math.min(c, n.lat), d = Math.max(d, n.lat), u2 = Math.min(u2, n.lon), m = Math.max(m, n.lon), h = Math.min(h, n.height), f = Math.max(f, n.height);
  const p = [u2, c, m, d], g = [...p, h, f];
  return {
    uv: i,
    range: p,
    region: g
  };
}
function ks(a, e, t = null, s = null) {
  let n = 1 / 0, i = 1 / 0, r = 1 / 0, o = -1 / 0, l2 = -1 / 0, c = -1 / 0;
  const u2 = [], h = new Matrix4();
  a.forEach((m) => {
    h.copy(m.matrixWorld), t && h.premultiply(t);
    const { uv: f, region: p } = yr(m.geometry, h, e);
    u2.push(f), n = Math.min(n, p[1]), o = Math.max(o, p[3]), i = Math.min(i, p[0]), l2 = Math.max(l2, p[2]), r = Math.min(r, p[4]), c = Math.max(c, p[5]);
  });
  let d = [i, n, l2, o];
  if (s !== null) {
    d = s.clampToBounds([i, n, l2, o]);
    const [m, f, p, g] = s.toNormalizedRange(d);
    u2.forEach((y2) => {
      for (let x2 = 0, M = y2.length; x2 < M; x2 += 3) {
        const T = y2[x2 + 0], S = y2[x2 + 1], _ = y2[x2 + 2], [C2, L] = s.toNormalizedPoint(T, S);
        y2[x2 + 0] = MathUtils.mapLinear(C2, m, p, 0, 1), y2[x2 + 1] = MathUtils.mapLinear(L, f, g, 0, 1), y2[x2 + 2] = MathUtils.mapLinear(_, r, c, 0, 1);
      }
    });
  }
  return {
    uvs: u2,
    range: d,
    region: [i, n, l2, o, r, c]
  };
}
function xr(a, e) {
  const t = new Vector3(), s = [], n = a.getAttribute("position");
  let i = 1 / 0, r = 1 / 0, o = 1 / 0, l2 = -1 / 0, c = -1 / 0, u2 = -1 / 0;
  for (let d = 0; d < n.count; d++)
    t.fromBufferAttribute(n, d).applyMatrix4(e), s.push(t.x, t.y, t.z), i = Math.min(i, t.x), l2 = Math.max(l2, t.x), r = Math.min(r, t.y), c = Math.max(c, t.y), o = Math.min(o, t.z), u2 = Math.max(u2, t.z);
  return {
    uv: s,
    range: [i, r, l2, c],
    heightRange: [o, u2]
  };
}
function Tr(a, e) {
  let t = 1 / 0, s = 1 / 0, n = 1 / 0, i = -1 / 0, r = -1 / 0, o = -1 / 0;
  const l2 = [], c = new Matrix4();
  return a.forEach((u2) => {
    c.copy(u2.matrixWorld), e && c.premultiply(e);
    const { uv: h, range: d, heightRange: m } = xr(u2.geometry, c);
    l2.push(h), t = Math.min(t, d[0]), i = Math.max(i, d[2]), s = Math.min(s, d[1]), r = Math.max(r, d[3]), n = Math.min(n, m[0]), o = Math.max(o, m[1]);
  }), l2.forEach((u2) => {
    for (let h = 0, d = u2.length; h < d; h += 3) {
      const m = u2[h + 0], f = u2[h + 1];
      u2[h + 0] = MathUtils.mapLinear(m, t, i, 0, 1), u2[h + 1] = MathUtils.mapLinear(f, s, r, 0, 1);
    }
  }), {
    uvs: l2,
    range: [t, s, i, r],
    heightRange: [n, o]
  };
}
var vt = /* @__PURE__ */ Symbol("OVERLAY_PARAMS");
function br(a, e) {
  if (a[vt])
    return a[vt];
  const t = {
    layerMaps: { value: [] },
    layerInfo: { value: [] }
  };
  return a[vt] = t, a.defines = {
    ...a.defines || {},
    LAYER_COUNT: 0
  }, a.onBeforeCompile = (s) => {
    e && e(s), s.uniforms = {
      ...s.uniforms,
      ...t
    }, s.vertexShader = s.vertexShader.replace(/void main\(\s*\)\s*{/, (n) => (
      /* glsl */
      `

				#pragma unroll_loop_start
					for ( int i = 0; i < 10; i ++ ) {

						#if UNROLLED_LOOP_INDEX < LAYER_COUNT

							attribute vec3 layer_uv_UNROLLED_LOOP_INDEX;
							varying vec3 v_layer_uv_UNROLLED_LOOP_INDEX;

						#endif


					}
				#pragma unroll_loop_end

				${n}

				#pragma unroll_loop_start
					for ( int i = 0; i < 10; i ++ ) {

						#if UNROLLED_LOOP_INDEX < LAYER_COUNT

							v_layer_uv_UNROLLED_LOOP_INDEX = layer_uv_UNROLLED_LOOP_INDEX;

						#endif

					}
				#pragma unroll_loop_end

			`
    )), s.fragmentShader = s.fragmentShader.replace(/void main\(/, (n) => (
      /* glsl */
      `

				#if LAYER_COUNT != 0
					struct LayerInfo {
						vec3 color;
						float opacity;

						int alphaMask;
						int alphaInvert;
					};

					uniform sampler2D layerMaps[ LAYER_COUNT ];
					uniform LayerInfo layerInfo[ LAYER_COUNT ];
				#endif

				#pragma unroll_loop_start
					for ( int i = 0; i < 10; i ++ ) {

						#if UNROLLED_LOOP_INDEX < LAYER_COUNT

							varying vec3 v_layer_uv_UNROLLED_LOOP_INDEX;

						#endif

					}
				#pragma unroll_loop_end

				${n}

			`
    )).replace(/#include <color_fragment>/, (n) => (
      /* glsl */
      `

				${n}

				#if LAYER_COUNT != 0
				{
					vec4 tint;
					vec3 layerUV;
					float layerOpacity;
					float wOpacity;
					float wDelta;
					#pragma unroll_loop_start
						for ( int i = 0; i < 10; i ++ ) {

							#if UNROLLED_LOOP_INDEX < LAYER_COUNT

								layerUV = v_layer_uv_UNROLLED_LOOP_INDEX;
								tint = texture( layerMaps[ i ], layerUV.xy );

								// discard texture outside 0, 1 on w - offset the stepped value by an epsilon to avoid cases
								// where wDelta is near 0 (eg a flat surface) at the w boundary, resulting in artifacts on some
								// hardware.
								wDelta = max( fwidth( layerUV.z ), 1e-7 );
								wOpacity =
									smoothstep( - wDelta, 0.0, layerUV.z ) *
									smoothstep( 1.0 + wDelta, 1.0, layerUV.z );

								// apply tint & opacity
								tint.rgb *= layerInfo[ i ].color;
								tint.rgba *= layerInfo[ i ].opacity * wOpacity;

								// invert the alpha
								if ( layerInfo[ i ].alphaInvert > 0 ) {

									tint.a = 1.0 - tint.a;

								}

								// apply the alpha across all existing layers if alpha mask is true
								if ( layerInfo[ i ].alphaMask > 0 ) {

									diffuseColor.a *= tint.a;

								} else {

									tint.rgb *= tint.a;
									diffuseColor = tint + diffuseColor * ( 1.0 - tint.a );

								}

							#endif

						}
					#pragma unroll_loop_end
				}
				#endif
			`
    ));
  }, t;
}
var An = class {
  constructor() {
    this.canvas = null, this.context = null, this.range = [0, 0, 1, 1];
  }
  // set the target render texture and the range that represents the full span
  setTarget(e, t) {
    this.canvas = e.image, this.context = e.image.getContext("2d"), this.range = [...t];
  }
  // draw the given texture at the given span with the provided projection
  draw(e, t) {
    const { canvas: s, range: n, context: i } = this, { width: r, height: o } = s, { image: l2 } = e, c = Math.round(MathUtils.mapLinear(t[0], n[0], n[2], 0, r)), u2 = Math.round(MathUtils.mapLinear(t[1], n[1], n[3], 0, o)), h = Math.round(MathUtils.mapLinear(t[2], n[0], n[2], 0, r)), d = Math.round(MathUtils.mapLinear(t[3], n[1], n[3], 0, o)), m = h - c, f = d - u2;
    l2 instanceof ImageBitmap ? (i.save(), i.translate(c, o - u2), i.scale(1, -1), i.drawImage(l2, 0, 0, m, f), i.restore()) : i.drawImage(l2, c, o - u2, m, -f);
  }
  // clear the set target
  clear() {
    const { context: e, canvas: t } = this;
    e.clearRect(0, 0, t.width, t.height);
  }
};
var In = class extends pn {
  hasContent(...e) {
    return true;
  }
};
var _r = class extends In {
  constructor(e) {
    super(), this.tiledImageSource = e, this.tileComposer = new An(), this.resolution = 256;
  }
  hasContent(e, t, s, n, i) {
    const r = this.tiledImageSource.tiling;
    let o = 0;
    return Lt([e, t, s, n], i, r, () => {
      o++;
    }), o !== 0;
  }
  async fetchItem([e, t, s, n, i], r) {
    const o = [e, t, s, n], l2 = this.tiledImageSource, c = this.tileComposer, u2 = l2.tiling, h = document.createElement("canvas");
    h.width = this.resolution, h.height = this.resolution;
    const d = new CanvasTexture(h);
    return d.colorSpace = SRGBColorSpace, d.generateMipmaps = false, d.tokens = [...o, i], await this._markImages(o, i, false), c.setTarget(d, o), c.clear(16777215, 0), Lt(o, i, u2, (m, f, p) => {
      const g = u2.getTileBounds(m, f, p, true, false), y2 = l2.get(m, f, p);
      c.draw(y2, g);
    }), d;
  }
  disposeItem(e) {
    e.dispose();
    const [t, s, n, i, r] = e.tokens;
    this._markImages([t, s, n, i], r, true);
  }
  dispose() {
    super.dispose(), this.tiledImageSource.dispose();
  }
  _markImages(e, t, s = false) {
    const n = this.tiledImageSource, i = n.tiling, r = [];
    Lt(e, t, i, (l2, c, u2) => {
      s ? n.release(l2, c, u2) : r.push(n.lock(l2, c, u2));
    });
    const o = r.filter((l2) => l2 instanceof Promise);
    return o.length !== 0 ? Promise.all(o) : null;
  }
};
var Et = new Vector3();
var Xe = new Vector3();
function Sr(a, e, t) {
  a.getCartographicToPosition(e, t, 0, Et), a.getCartographicToPosition(e + 0.01, t, 0, Xe);
  const n = Et.distanceTo(Xe);
  return a.getCartographicToPosition(e, t + 0.01, 0, Xe), Et.distanceTo(Xe) / n;
}
var Mr = class extends In {
  constructor({
    geojson: e = null,
    url: t = null,
    // URL or GeoJson object can be provided
    resolution: s = 256,
    pointRadius: n = 6,
    strokeStyle: i = "white",
    strokeWidth: r = 2,
    fillStyle: o = "rgba( 255, 255, 255, 0.5 )",
    ...l2
  } = {}) {
    super(l2), this.geojson = e, this.url = t, this.resolution = s, this.pointRadius = n, this.strokeStyle = i, this.strokeWidth = r, this.fillStyle = o, this.features = null, this.featureBounds = /* @__PURE__ */ new Map(), this.contentBounds = null, this.projection = new re(), this.fetchData = (...c) => fetch(...c);
  }
  async init() {
    const { geojson: e, url: t } = this;
    if (!e && t) {
      const s = await this.fetchData(t);
      this.geojson = await s.json();
    }
    this._updateCache(true);
  }
  hasContent(e, t, s, n) {
    const i = [e, t, s, n].map((r) => r * Math.RAD2DEG);
    return this._boundsIntersectBounds(i, this.contentBounds);
  }
  // main fetch per region -> returns CanvasTexture
  async fetchItem(e, t) {
    const s = document.createElement("canvas"), n = new CanvasTexture(s);
    return n.colorSpace = SRGBColorSpace, n.generateMipmaps = false, this._drawToCanvas(s, e), n.needsUpdate = true, n;
  }
  disposeItem(e) {
    e.dispose();
  }
  redraw() {
    this._updateCache(true), this.forEachItem((e, t) => {
      this._drawToCanvas(e.image, t), e.needsUpdate = true;
    });
  }
  _updateCache(e = false) {
    const { geojson: t, featureBounds: s } = this;
    if (!t || this.features && !e)
      return;
    s.clear();
    let n = 1 / 0, i = 1 / 0, r = -1 / 0, o = -1 / 0;
    this.features = this._featuresFromGeoJSON(t), this.features.forEach((l2) => {
      const c = this._getFeatureBounds(l2);
      s.set(l2, c);
      const [u2, h, d, m] = c;
      n = Math.min(n, u2), i = Math.min(i, h), r = Math.max(r, d), o = Math.max(o, m);
    }), this.contentBounds = [n, i, r, o];
  }
  _drawToCanvas(e, t) {
    this._updateCache();
    const [s, n, i, r] = t, { projection: o, resolution: l2, features: c } = this;
    e.width = l2, e.height = l2;
    const u2 = o.convertNormalizedToLongitude(s), h = o.convertNormalizedToLatitude(n), d = o.convertNormalizedToLongitude(i), m = o.convertNormalizedToLatitude(r), f = [
      u2 * MathUtils.RAD2DEG,
      h * MathUtils.RAD2DEG,
      d * MathUtils.RAD2DEG,
      m * MathUtils.RAD2DEG
    ], p = e.getContext("2d");
    for (let g = 0; g < c.length; g++) {
      const y2 = c[g];
      this._featureIntersectsTile(y2, f) && this._drawFeatureOnCanvas(p, y2, f, e.width, e.height);
    }
  }
  // bounding box quick test in projected units
  _featureIntersectsTile(e, t) {
    const s = this.featureBounds.get(e);
    return s ? this._boundsIntersectBounds(s, t) : false;
  }
  _boundsIntersectBounds(e, t) {
    const [s, n, i, r] = e, [o, l2, c, u2] = t;
    return !(i < o || s > c || r < l2 || n > u2);
  }
  _getFeatureBounds(e) {
    const { geometry: t } = e;
    if (!t)
      return null;
    const { type: s, coordinates: n } = t;
    let i = 1 / 0, r = 1 / 0, o = -1 / 0, l2 = -1 / 0;
    const c = (u2, h) => {
      i = Math.min(i, u2), o = Math.max(o, u2), r = Math.min(r, h), l2 = Math.max(l2, h);
    };
    return s === "Point" ? c(n[0], n[1]) : s === "MultiPoint" || s === "LineString" ? n.forEach((u2) => c(u2[0], u2[1])) : s === "MultiLineString" || s === "Polygon" ? n.forEach((u2) => u2.forEach((h) => c(h[0], h[1]))) : s === "MultiPolygon" && n.forEach(
      (u2) => u2.forEach((h) => h.forEach((d) => c(d[0], d[1])))
    ), [i, r, o, l2];
  }
  // Normalize top-level geojson into an array of Feature objects
  _featuresFromGeoJSON(e) {
    const t = e.type, s = /* @__PURE__ */ new Set(["Point", "MultiPoint", "LineString", "MultiLineString", "Polygon", "MultiPolygon"]);
    return t === "FeatureCollection" ? e.features : t === "Feature" ? [e] : t === "GeometryCollection" ? e.geometries.map((n) => ({ type: "Feature", geometry: n, properties: {} })) : s.has(t) ? [{ type: "Feature", geometry: e, properties: {} }] : [];
  }
  // draw feature on canvas ( assumes intersects already )
  _drawFeatureOnCanvas(e, t, s, n, i) {
    const { geometry: r = null, properties: o = {} } = t;
    if (!r)
      return;
    const [l2, c, u2, h] = s, d = o.strokeStyle || this.strokeStyle, m = o.fillStyle || this.fillStyle, f = o.pointRadius || this.pointRadius, p = o.strokeWidth || this.strokeWidth;
    e.save(), e.strokeStyle = d, e.fillStyle = m, e.lineWidth = p;
    const g = new Array(2), y2 = (T, S, _ = g) => {
      const C2 = MathUtils.mapLinear(T, l2, u2, 0, n), L = i - MathUtils.mapLinear(S, c, h, 0, i);
      return _[0] = Math.round(C2), _[1] = Math.round(L), _;
    }, x2 = (T, S) => {
      const _ = S * MathUtils.DEG2RAD, C2 = T * MathUtils.DEG2RAD, L = (h - c) / i;
      return (u2 - l2) / n / L * Sr(Nt, _, C2);
    }, M = r.type;
    if (M === "Point") {
      const [T, S] = r.coordinates, [_, C2] = y2(T, S), L = x2(T, S);
      e.beginPath(), e.ellipse(_, C2, f / L, f, 0, 0, Math.PI * 2), e.fill(), e.stroke();
    } else M === "MultiPoint" ? r.coordinates.forEach(([T, S]) => {
      const [_, C2] = y2(T, S), L = x2(T, S);
      e.beginPath(), e.ellipse(_, C2, f / L, f, 0, 0, Math.PI * 2), e.fill(), e.stroke();
    }) : M === "LineString" ? (e.beginPath(), r.coordinates.forEach(([T, S], _) => {
      const [C2, L] = y2(T, S);
      _ === 0 ? e.moveTo(C2, L) : e.lineTo(C2, L);
    }), e.stroke()) : M === "MultiLineString" ? (e.beginPath(), r.coordinates.forEach((T) => {
      T.forEach(([S, _], C2) => {
        const [L, P] = y2(S, _);
        C2 === 0 ? e.moveTo(L, P) : e.lineTo(L, P);
      });
    }), e.stroke()) : M === "Polygon" ? (e.beginPath(), r.coordinates.forEach((T, S) => {
      T.forEach(([_, C2], L) => {
        const [P, R] = y2(_, C2);
        L === 0 ? e.moveTo(P, R) : e.lineTo(P, R);
      }), e.closePath();
    }), e.fill("evenodd"), e.stroke()) : M === "MultiPolygon" && r.coordinates.forEach((T) => {
      e.beginPath(), T.forEach((S, _) => {
        S.forEach(([C2, L], P) => {
          const [R, V] = y2(C2, L);
          P === 0 ? e.moveTo(R, V) : e.lineTo(R, V);
        }), e.closePath();
      }), e.fill("evenodd"), e.stroke();
    });
    e.restore();
  }
};
var _e = new Matrix4();
var Ye = new Vector3();
var wt = new Vector3();
var Pt = new Vector3();
var se = new Vector3();
var Cr = new Box3();
var Gs = /* @__PURE__ */ Symbol("SPLIT_TILE_DATA");
var $e = /* @__PURE__ */ Symbol("SPLIT_HASH");
var fo = class {
  get enableTileSplitting() {
    return this._enableTileSplitting;
  }
  set enableTileSplitting(e) {
    this._enableTileSplitting !== e && (this._enableTileSplitting = e, this._markNeedsUpdate());
  }
  constructor(e = {}) {
    const {
      overlays: t = [],
      resolution: s = 256,
      enableTileSplitting: n = true
    } = e;
    this.name = "IMAGE_OVERLAY_PLUGIN", this.priority = -15, this.resolution = s, this._enableTileSplitting = n, this.overlays = [], this.needsUpdate = false, this.tiles = null, this.tileComposer = null, this.tileControllers = /* @__PURE__ */ new Map(), this.overlayInfo = /* @__PURE__ */ new Map(), this.meshParams = /* @__PURE__ */ new WeakMap(), this.pendingTiles = /* @__PURE__ */ new Map(), this.processedTiles = /* @__PURE__ */ new Set(), this.processQueue = null, this._onUpdateAfter = null, this._onTileDownloadStart = null, this._virtualChildResetId = 0, this._bytesUsed = /* @__PURE__ */ new WeakMap(), t.forEach((i) => {
      this.addOverlay(i);
    });
  }
  // plugin functions
  init(e) {
    const t = new An(), s = new G();
    s.maxJobs = 10, s.priorityCallback = (n, i) => {
      const r = n.tile, o = i.tile, l2 = e.visibleTiles.has(r), c = e.visibleTiles.has(o);
      return l2 !== c ? l2 ? 1 : -1 : e.downloadQueue.priorityCallback(r, o);
    }, this.tiles = e, this.tileComposer = t, this.processQueue = s, e.forEachLoadedModel((n, i) => {
      this._processTileModel(n, i, true);
    }), this._onUpdateAfter = async () => {
      let n = false;
      if (this.overlayInfo.forEach((i, r) => {
        if (!!r.frame != !!i.frame || r.frame && i.frame && !i.frame.equals(r.frame)) {
          const o = i.order;
          this.deleteOverlay(r), this.addOverlay(r, o), n = true;
        }
      }), n) {
        const i = s.maxJobs;
        let r = 0;
        s.items.forEach((o) => {
          e.visibleTiles.has(o.tile) && r++;
        }), s.maxJobs = r + s.currJobs, s.tryRunJobs(), s.maxJobs = i, this.needsUpdate = true;
      }
      if (this.needsUpdate) {
        this.needsUpdate = false;
        const { overlays: i, overlayInfo: r } = this;
        i.sort((o, l2) => r.get(o).order - r.get(l2).order), this.processedTiles.forEach((o) => {
          this._updateLayers(o);
        }), this.resetVirtualChildren(!this.enableTileSplitting), e.recalculateBytesUsed(), e.dispatchEvent({ type: "needs-rerender" });
      }
    }, this._onTileDownloadStart = ({ tile: n, url: i }) => {
      !/\.json$/i.test(i) && !/\.subtree/i.test(i) && (this.processedTiles.add(n), this._initTileOverlayInfo(n));
    }, e.addEventListener("update-after", this._onUpdateAfter), e.addEventListener("tile-download-start", this._onTileDownloadStart), this.overlays.forEach((n) => {
      this._initOverlay(n);
    });
  }
  disposeTile(e) {
    const { overlayInfo: t, tileControllers: s, processQueue: n, pendingTiles: i, processedTiles: r } = this;
    r.delete(e), s.has(e) && (s.get(e).abort(), s.delete(e), i.delete(e)), t.forEach((({ tileInfo: o }, l2) => {
      if (o.has(e)) {
        const { meshInfo: c, range: u2 } = o.get(e);
        u2 !== null && l2.releaseTexture(u2, e), o.delete(e), c.clear();
      }
    })), n.removeByFilter((o) => o.tile === e);
  }
  calculateBytesUsed(e) {
    const { overlayInfo: t } = this, s = this._bytesUsed;
    let n = null;
    return t.forEach(({ tileInfo: i }, r) => {
      if (i.has(e)) {
        const { target: o } = i.get(e);
        n = n || 0, n += ft(o);
      }
    }), n !== null ? (s.set(e, n), n) : s.has(e) ? s.get(e) : 0;
  }
  processTileModel(e, t) {
    return this._processTileModel(e, t);
  }
  async _processTileModel(e, t, s = false) {
    const { tileControllers: n, processedTiles: i, pendingTiles: r } = this;
    n.set(t, new AbortController()), s || r.set(t, e), i.add(t), this._wrapMaterials(e), this._initTileOverlayInfo(t), await this._initTileSceneOverlayInfo(e, t), this.expandVirtualChildren(e, t), this._updateLayers(t), r.delete(t);
  }
  dispose() {
    const { tiles: e } = this;
    [...this.overlays].forEach((s) => {
      this.deleteOverlay(s);
    }), this.processedTiles.forEach((s) => {
      this._updateLayers(s), this.disposeTile(s), delete s[$e];
    }), e.removeEventListener("update-after", this._onUpdateAfter), this.resetVirtualChildren(true);
  }
  getAttributions(e) {
    this.overlays.forEach((t) => {
      t.opacity > 0 && t.getAttributions(e);
    });
  }
  parseToMesh(e, t, s, n) {
    if (s === "image_overlay_tile_split")
      return t[Gs];
  }
  async resetVirtualChildren(e = false) {
    this._virtualChildResetId++;
    const t = this._virtualChildResetId;
    if (await Promise.all(this.overlays.map((r) => r.whenReady())), t !== this._virtualChildResetId)
      return;
    const { tiles: s } = this, n = /* @__PURE__ */ new Set();
    this.processedTiles.forEach((r) => {
      $e in r && n.add(r);
    }), n.forEach((r) => {
      if (r.parent === null)
        return;
      const o = r.engineData.scene.clone();
      if (o.updateMatrixWorld(), e || r[$e] !== this._getSplitVectors(o, r).hash) {
        const l2 = i(r);
        l2.sort((c, u2) => (u2.internal.depth || 0) - (c.internal.depth || 0)), l2.forEach((c) => {
          s.processNodeQueue.remove(c), s.lruCache.remove(c), c.parent = null;
        }), r.children.length = 0;
      }
    }), e || s.forEachLoadedModel((r, o) => {
      this.expandVirtualChildren(r, o);
    });
    function i(r, o = []) {
      return r.children.forEach((l2) => {
        o.push(l2), i(l2, o);
      }), o;
    }
  }
  _getSplitVectors(e, t, s = wt) {
    const { tiles: n, overlayInfo: i } = this, r = new Box3();
    r.setFromObject(e), r.getCenter(s);
    const o = [], l2 = [];
    i.forEach(({ tileInfo: u2 }, h) => {
      const d = u2.get(t);
      if (d && d.target && h.shouldSplit(d.range, t)) {
        h.frame ? se.set(0, 0, 1).transformDirection(h.frame) : (n.ellipsoid.getPositionToNormal(s, se), se.length() < 1e-6 && se.set(1, 0, 0));
        const m = `${se.x.toFixed(3)},${se.y.toFixed(3)},${se.z.toFixed(3)}_`;
        l2.includes(m) || l2.push(m);
        const f = Ye.set(0, 0, 1);
        Math.abs(se.dot(f)) > 1 - 1e-4 && f.set(1, 0, 0);
        const p = new Vector3().crossVectors(se, f).normalize(), g = new Vector3().crossVectors(se, p).normalize();
        o.push(p, g);
      }
    });
    const c = [];
    for (; o.length !== 0; ) {
      const u2 = o.pop().clone(), h = u2.clone();
      for (let d = 0; d < o.length; d++) {
        const m = o[d], f = u2.dot(m);
        Math.abs(f) > Math.cos(Math.PI / 8) && (h.addScaledVector(m, Math.sign(f)), u2.copy(h).normalize(), o.splice(d, 1), d--);
      }
      c.push(h.normalize());
    }
    return { directions: c, hash: l2.join("") };
  }
  async expandVirtualChildren(e, t) {
    if (t.children.length !== 0 || this.enableTileSplitting === false)
      return;
    const s = e.clone();
    s.updateMatrixWorld();
    const { directions: n, hash: i } = this._getSplitVectors(s, t, wt);
    if (t[$e] = i, n.length === 0)
      return;
    const r = new yn();
    r.attributeList = (l2) => !/^layer_uv_\d+/.test(l2), n.map((l2) => {
      r.addSplitOperation((c, u2, h, d, m, f) => (Triangle.getInterpolatedAttribute(c.attributes.position, u2, h, d, m, Ye), Ye.applyMatrix4(f).sub(wt).dot(l2)));
    });
    const o = [];
    r.forEachSplitPermutation(() => {
      const l2 = r.clipObject(s);
      l2.matrix.premultiply(t.engineData.transformInverse).decompose(l2.position, l2.quaternion, l2.scale);
      const c = [];
      if (l2.traverse((h) => {
        if (h.isMesh) {
          const d = h.material.clone();
          h.material = d;
          for (const m in d) {
            const f = d[m];
            if (f && f.isTexture && f.source.data instanceof ImageBitmap) {
              const p = document.createElement("canvas");
              p.width = f.image.width, p.height = f.image.height;
              const g = p.getContext("2d");
              g.scale(1, -1), g.drawImage(f.source.data, 0, 0, p.width, -p.height);
              const y2 = new CanvasTexture(p);
              y2.mapping = f.mapping, y2.wrapS = f.wrapS, y2.wrapT = f.wrapT, y2.minFilter = f.minFilter, y2.magFilter = f.magFilter, y2.format = f.format, y2.type = f.type, y2.anisotropy = f.anisotropy, y2.colorSpace = f.colorSpace, y2.generateMipmaps = f.generateMipmaps, d[m] = y2;
            }
          }
          c.push(h);
        }
      }), c.length === 0)
        return;
      const u2 = {};
      if (t.boundingVolume.region && (u2.region = ks(c, this.tiles.ellipsoid).region), t.boundingVolume.box || t.boundingVolume.sphere) {
        Cr.setFromObject(l2, true).getCenter(Pt);
        let h = 0;
        l2.traverse((d) => {
          const m = d.geometry;
          if (m) {
            const f = m.attributes.position;
            for (let p = 0, g = f.count; p < g; p++) {
              const y2 = Ye.fromBufferAttribute(f, p).applyMatrix4(d.matrixWorld).distanceToSquared(Pt);
              h = Math.max(h, y2);
            }
          }
        }), u2.sphere = [...Pt, Math.sqrt(h)];
      }
      o.push({
        refine: "REPLACE",
        geometricError: t.geometricError * 0.5,
        boundingVolume: u2,
        content: { uri: "./child.image_overlay_tile_split" },
        children: [],
        [Gs]: l2
      });
    }), t.refine = "REPLACE", t.children.push(...o);
  }
  fetchData(e, t) {
    if (/image_overlay_tile_split/.test(e))
      return new ArrayBuffer();
  }
  // public
  addOverlay(e, t = null) {
    const { tiles: s, overlays: n, overlayInfo: i } = this;
    t === null && (t = n.reduce((o, l2) => Math.max(o, l2.order + 1), 0));
    const r = new AbortController();
    n.push(e), i.set(e, {
      order: t,
      uniforms: {},
      tileInfo: /* @__PURE__ */ new Map(),
      controller: r,
      frame: e.frame ? e.frame.clone() : null
    }), s !== null && this._initOverlay(e);
  }
  setOverlayOrder(e, t) {
    this.overlays.indexOf(e) !== -1 && (this.overlayInfo.get(e).order = t, this._markNeedsUpdate());
  }
  deleteOverlay(e) {
    const { overlays: t, overlayInfo: s, processQueue: n, processedTiles: i } = this, r = t.indexOf(e);
    if (r !== -1) {
      const { tileInfo: o, controller: l2 } = s.get(e);
      i.forEach((c) => {
        if (!o.has(c))
          return;
        const {
          meshInfo: u2,
          range: h
        } = o.get(c);
        h !== null && e.releaseTexture(h, c), o.delete(c), u2.clear();
      }), o.clear(), s.delete(e), l2.abort(), n.removeByFilter((c) => c.overlay === e), t.splice(r, 1), i.forEach((c) => {
        this._updateLayers(c);
      }), this._markNeedsUpdate();
    }
  }
  // initialize the overlay to use the right fetch options, load all data for existing tiles
  _initOverlay(e) {
    const { tiles: t } = this;
    e.isInitialized || (e.init(), e.whenReady().then(() => {
      e.setResolution(this.resolution);
      const i = e.fetch.bind(e);
      e.fetch = (...r) => t.downloadQueue.add({ priority: -performance.now() }, () => i(...r));
    }));
    const s = [], n = async (i, r) => {
      this._initTileOverlayInfo(r, e);
      const o = this._initTileSceneOverlayInfo(i, r, e);
      s.push(o), await o, this._updateLayers(r);
    };
    t.forEachLoadedModel((i, r) => {
      n(i, r);
    }), this.pendingTiles.forEach((i, r) => {
      n(i, r);
    }), Promise.all(s).then(() => {
      this._markNeedsUpdate();
    });
  }
  // wrap all materials in the given scene wit the overlay material shader
  _wrapMaterials(e) {
    e.traverse((t) => {
      if (t.material) {
        const s = br(t.material, t.material.onBeforeCompile);
        this.meshParams.set(t, s);
      }
    });
  }
  // Initialize per-tile overlay information. This function triggers an async function but
  // does not need to be awaited for use since it's just locking textures which are awaited later.
  _initTileOverlayInfo(e, t = this.overlays) {
    if (Array.isArray(t)) {
      t.forEach((i) => this._initTileOverlayInfo(e, i));
      return;
    }
    const { overlayInfo: s } = this;
    if (s.get(t).tileInfo.has(e))
      return;
    const n = {
      range: null,
      target: null,
      meshInfo: /* @__PURE__ */ new Map()
    };
    if (s.get(t).tileInfo.set(e, n), t.isReady && !t.isPlanarProjection) {
      if (e.boundingVolume.region) {
        const [i, r, o, l2] = e.boundingVolume.region, c = t.projection.toNormalizedRange([i, r, o, l2]);
        n.range = c, t.lockTexture(c, e);
      }
    }
  }
  // initialize the scene meshes
  async _initTileSceneOverlayInfo(e, t, s = this.overlays) {
    if (Array.isArray(s))
      return Promise.all(s.map((T) => this._initTileSceneOverlayInfo(e, t, T)));
    const { tiles: n, overlayInfo: i, tileControllers: r, processQueue: o } = this, { ellipsoid: l2 } = n, { controller: c, tileInfo: u2 } = i.get(s), h = r.get(t);
    if (s.isReady || await s.whenReady(), c.signal.aborted || h.signal.aborted)
      return;
    const d = [];
    e.updateMatrixWorld(), e.traverse((T) => {
      T.isMesh && d.push(T);
    });
    const { aspectRatio: m, projection: f } = s, p = u2.get(t);
    let g, y2, x2;
    if (s.isPlanarProjection) {
      _e.makeScale(1 / m, 1, 1).multiply(s.frame), e.parent !== null && _e.multiply(n.group.matrixWorldInverse);
      let T;
      ({ range: g, uvs: y2, heightRange: T } = Tr(d, _e)), x2 = !(T[0] > 1 || T[1] < 0);
    } else
      _e.identity(), e.parent !== null && _e.copy(n.group.matrixWorldInverse), { range: g, uvs: y2 } = ks(d, l2, _e, f), g = f.toNormalizedRange(g), x2 = true;
    p.range === null ? (p.range = g, s.lockTexture(g, t)) : g = p.range;
    let M = null;
    x2 && s.hasContent(g, t) && (M = await o.add({ tile: t, overlay: s }, async () => {
      if (c.signal.aborted || h.signal.aborted)
        return null;
      const T = await s.getTexture(g, t);
      return c.signal.aborted || h.signal.aborted ? null : T;
    }).catch((T) => {
      if (!(T instanceof C))
        throw T;
    })), p.target = M, d.forEach((T, S) => {
      const _ = new Float32Array(y2[S]), C2 = new BufferAttribute(_, 3);
      p.meshInfo.set(T, { attribute: C2 });
    });
  }
  _updateLayers(e) {
    const { overlayInfo: t, overlays: s, tileControllers: n } = this, i = n.get(e);
    this.tiles.recalculateBytesUsed(e), !(!i || i.signal.aborted) && s.forEach((r, o) => {
      const { tileInfo: l2 } = t.get(r), { meshInfo: c, target: u2 } = l2.get(e);
      c.forEach(({ attribute: h }, d) => {
        const { geometry: m, material: f } = d, p = this.meshParams.get(d), g = `layer_uv_${o}`;
        m.getAttribute(g) !== h && (m.setAttribute(g, h), m.dispose()), p.layerMaps.length = s.length, p.layerInfo.length = s.length, p.layerMaps.value[o] = u2 !== null ? u2 : null, p.layerInfo.value[o] = r, f.defines[`LAYER_${o}_EXISTS`] = +(u2 !== null), f.defines[`LAYER_${o}_ALPHA_INVERT`] = Number(r.alphaInvert), f.defines[`LAYER_${o}_ALPHA_MASK`] = Number(r.alphaMask), f.defines.LAYER_COUNT = s.length, f.needsUpdate = true;
      });
    });
  }
  _markNeedsUpdate() {
    this.needsUpdate === false && (this.needsUpdate = true, this.tiles !== null && this.tiles.dispatchEvent({ type: "needs-update" }));
  }
};
var Ln = class {
  get isPlanarProjection() {
    return !!this.frame;
  }
  constructor(e = {}) {
    const {
      opacity: t = 1,
      color: s = 16777215,
      frame: n = null,
      preprocessURL: i = null,
      alphaMask: r = false,
      alphaInvert: o = false
    } = e;
    this.preprocessURL = i, this.opacity = t, this.color = new Color(s), this.frame = n !== null ? n.clone() : null, this.alphaMask = r, this.alphaInvert = o, this._whenReady = null, this.isReady = false, this.isInitialized = false;
  }
  init() {
    this.isInitialized = true, this._whenReady = this._init().then(() => this.isReady = true);
  }
  whenReady() {
    return this._whenReady;
  }
  // overrideable
  _init() {
  }
  fetch(e, t = {}) {
    return this.preprocessURL && (e = this.preprocessURL(e)), fetch(e, t);
  }
  getAttributions(e) {
  }
  hasContent(e, t) {
    return false;
  }
  async getTexture(e, t) {
    return null;
  }
  async lockTexture(e, t) {
    return null;
  }
  releaseTexture(e, t) {
  }
  setResolution(e) {
  }
  shouldSplit(e, t) {
    return false;
  }
};
var Ce = class extends Ln {
  get tiling() {
    return this.imageSource.tiling;
  }
  get projection() {
    return this.tiling.projection;
  }
  get aspectRatio() {
    return this.tiling && this.isReady ? this.tiling.aspectRatio : 1;
  }
  get fetchOptions() {
    return this.imageSource.fetchOptions;
  }
  set fetchOptions(e) {
    this.imageSource.fetchOptions = e;
  }
  constructor(e = {}) {
    const { imageSource: t = null, ...s } = e;
    super(s), this.imageSource = t, this.regionImageSource = null;
  }
  _init() {
    return this._initImageSource().then(() => {
      this.imageSource.fetchData = (...e) => this.fetch(...e), this.regionImageSource = new _r(this.imageSource);
    });
  }
  _initImageSource() {
    return this.imageSource.init();
  }
  // Texture acquisition API implementations
  calculateLevel(e, t) {
    if (this.isPlanarProjection) {
      const [s, n, i, r] = e, o = i - s, l2 = r - n;
      let c = 0;
      const u2 = this.regionImageSource.resolution, h = this.tiling.maxLevel;
      for (; c < h; c++) {
        const d = u2 / o, m = u2 / l2, { pixelWidth: f, pixelHeight: p } = this.tiling.getLevel(c);
        if (f >= d || p >= m)
          break;
      }
      return c;
    } else
      return t.internal.depthFromRenderedParent - 1;
  }
  hasContent(e, t) {
    return this.regionImageSource.hasContent(...e, this.calculateLevel(e, t));
  }
  getTexture(e, t) {
    return this.regionImageSource.get(...e, this.calculateLevel(e, t));
  }
  lockTexture(e, t) {
    return this.regionImageSource.lock(...e, this.calculateLevel(e, t));
  }
  releaseTexture(e, t) {
    this.regionImageSource.release(...e, this.calculateLevel(e, t));
  }
  setResolution(e) {
    this.regionImageSource.resolution = e;
  }
  shouldSplit(e, t) {
    return this.tiling.maxLevel > this.calculateLevel(e, t);
  }
};
var mo = class extends Ce {
  constructor(e = {}) {
    super(e), this.imageSource = new Ne(e);
  }
};
var go = class extends Ln {
  get projection() {
    return this.imageSource.projection;
  }
  get aspectRatio() {
    return 2;
  }
  get pointRadius() {
    return this.imageSource.pointRadius;
  }
  set pointRadius(e) {
    this.imageSource.pointRadius = e;
  }
  get strokeStyle() {
    return this.imageSource.strokeStyle;
  }
  set strokeStyle(e) {
    this.imageSource.strokeStyle = e;
  }
  get strokeWidth() {
    return this.imageSource.strokeWidth;
  }
  set strokeWidth(e) {
    this.imageSource.strokeWidth = e;
  }
  get fillStyle() {
    return this.imageSource.fillStyle;
  }
  set fillStyle(e) {
    this.imageSource.fillStyle = e;
  }
  get geojson() {
    return this.imageSource.geojson;
  }
  set geojson(e) {
    this.imageSource.geojson = e;
  }
  constructor(e = {}) {
    super(e), this.imageSource = new Mr(e);
  }
  _init() {
    return this.imageSource.init();
  }
  hasContent(e) {
    return this.imageSource.hasContent(...e);
  }
  getTexture(e) {
    return this.imageSource.get(...e);
  }
  lockTexture(e) {
    return this.imageSource.lock(...e);
  }
  releaseTexture(e) {
    this.imageSource.release(...e);
  }
  setResolution(e) {
    this.imageSource.resolution = e;
  }
  shouldSplit(e, t) {
    return true;
  }
  redraw() {
    this.imageSource.redraw();
  }
};
var yo = class extends Ce {
  constructor(e = {}) {
    super(e), this.imageSource = new gn(e);
  }
};
var xo = class extends Ce {
  constructor(e = {}) {
    super(e), this.imageSource = new mn(e);
  }
};
var To = class extends Ce {
  constructor(e = {}) {
    super(e), this.imageSource = new Xt(e);
  }
};
var bo = class extends Ce {
  constructor(e = {}) {
    super(e);
    const { apiToken: t, autoRefreshToken: s, assetId: n } = e;
    this.options = e, this.assetId = n, this.auth = new it({ apiToken: t, autoRefreshToken: s }), this.auth.authURL = `https://api.cesium.com/v1/assets/${n}/endpoint`, this._attributions = [], this.externalType = false;
  }
  _initImageSource() {
    return this.auth.refreshToken().then(async (e) => {
      if (this._attributions = e.attributions.map((t) => ({
        value: t.html,
        type: "html",
        collapsible: t.collapsible
      })), e.type !== "IMAGERY")
        throw new Error("CesiumIonOverlay: Only IMAGERY is supported as overlay type.");
      switch (this.externalType = !!e.externalType, e.externalType) {
        case "GOOGLE_2D_MAPS": {
          const { url: t, session: s, key: n, tileWidth: i } = e.options, r = `${t}/v1/2dtiles/{z}/{x}/{y}?session=${s}&key=${n}`;
          this.imageSource = new Ne({
            ...this.options,
            url: r,
            tileDimension: i,
            // Google maps tiles have a fixed depth of 22
            // https://developers.google.com/maps/documentation/tile/2d-tiles-overview
            levels: 22
          });
          break;
        }
        case "BING": {
          const { url: t, mapStyle: s, key: n } = e.options, i = `${t}/REST/v1/Imagery/Metadata/${s}?incl=ImageryProviders&key=${n}&uriScheme=https`, o = (await fetch(i).then((l2) => l2.json())).resourceSets[0].resources[0];
          this.imageSource = new gr({
            ...this.options,
            url: o.imageUrl,
            subdomains: o.imageUrlSubdomains,
            tileDimension: o.tileWidth,
            levels: o.zoomMax
          });
          break;
        }
        default:
          this.imageSource = new Xt({
            ...this.options,
            url: e.url
          });
      }
      return this.imageSource.fetchData = (...t) => this.fetch(...t), this.imageSource.init();
    });
  }
  fetch(...e) {
    return this.externalType ? super.fetch(...e) : this.auth.fetch(...e);
  }
  getAttributions(e) {
    e.push(...this._attributions);
  }
};
var _o = class extends Ce {
  constructor(e = {}) {
    super(e);
    const { apiToken: t, sessionOptions: s, autoRefreshToken: n, logoUrl: i } = e;
    this.logoUrl = i, this.auth = new at({ apiToken: t, sessionOptions: s, autoRefreshToken: n }), this.imageSource = new Ne(), this.imageSource.fetchData = (...r) => this.fetch(...r), this._logoAttribution = {
      value: "",
      type: "image",
      collapsible: false
    };
  }
  _initImageSource() {
    return this.auth.refreshToken().then((e) => (this.imageSource.tileDimension = e.tileWidth, this.imageSource.url = "https://tile.googleapis.com/v1/2dtiles/{z}/{x}/{y}", this.imageSource.init()));
  }
  fetch(...e) {
    return this.auth.fetch(...e);
  }
  getAttributions(e) {
    this.logoUrl && (this._logoAttribution.value = this.logoUrl, e.push(this._logoAttribution));
  }
};
var So = class {
  constructor() {
    this.name = "LOAD_REGION_PLUGIN", this.regions = [], this.tiles = null;
  }
  init(e) {
    this.tiles = e;
  }
  addRegion(e) {
    this.regions.indexOf(e) === -1 && this.regions.push(e);
  }
  removeRegion(e) {
    const t = this.regions.indexOf(e);
    t !== -1 && this.regions.splice(t, 1);
  }
  hasRegion(e) {
    return this.regions.indexOf(e) !== -1;
  }
  clearRegions() {
    this.regions = [];
  }
  // Calculates shape intersections and associated error values to use. If "mask" shapes are present then
  // tiles are only loaded if they are within those shapes.
  calculateTileViewError(e, t) {
    const s = e.engineData.boundingVolume, { regions: n, tiles: i } = this;
    let r = false, o = null, l2 = 0, c = 1 / 0;
    for (const u2 of n) {
      const h = u2.intersectsTile(s, e, i);
      r = r || h, h && (l2 = Math.max(u2.calculateError(e, i), l2), c = Math.min(u2.calculateDistance(s, e, i))), u2.mask && (o = o || h);
    }
    return t.inView = r && o !== false, t.error = l2, t.distance = c, t.inView || o !== null;
  }
  dispose() {
    this.regions = [];
  }
};
var Qt = class {
  constructor(e = {}) {
    typeof e == "number" && (console.warn("LoadRegionPlugin: Region constructor has been changed to take options as an object."), e = { errorTarget: e });
    const {
      errorTarget: t = 10,
      mask: s = false
    } = e;
    this.errorTarget = t, this.mask = s;
  }
  intersectsTile(e, t, s) {
    return false;
  }
  calculateDistance(e, t, s) {
    return 1 / 0;
  }
  calculateError(e, t) {
    return e.geometricError - this.errorTarget + t.errorTarget;
  }
};
var Mo = class extends Qt {
  constructor(e = {}) {
    typeof e == "number" && (console.warn("SphereRegion: Region constructor has been changed to take options as an object."), e = {
      errorTarget: arguments[0],
      sphere: arguments[1]
    });
    const { sphere: t = new Sphere() } = e;
    super(e), this.sphere = t.clone();
  }
  intersectsTile(e) {
    return e.intersectsSphere(this.sphere);
  }
};
var Co = class extends Qt {
  constructor(e = {}) {
    typeof e == "number" && (console.warn("RayRegion: Region constructor has been changed to take options as an object."), e = {
      errorTarget: arguments[0],
      ray: arguments[1]
    });
    const { ray: t = new Ray() } = e;
    super(e), this.ray = t.clone();
  }
  intersectsTile(e) {
    return e.intersectsRay(this.ray);
  }
};
var Ao = class extends Qt {
  constructor(e = {}) {
    typeof e == "number" && (console.warn("RayRegion: Region constructor has been changed to take options as an object."), e = {
      errorTarget: arguments[0],
      obb: arguments[1]
    });
    const { obb: t = new jt() } = e;
    super(e), this.obb = t.clone(), this.obb.update();
  }
  intersectsTile(e) {
    return e.intersectsOBB(this.obb);
  }
};
var te = new Vector3();
var zs = ["x", "y", "z"];
var Ar = class extends LineSegments {
  constructor(e, t = 16776960, s = 40) {
    const n = new BufferGeometry(), i = [];
    for (let r = 0; r < 3; r++) {
      const o = zs[r], l2 = zs[(r + 1) % 3];
      te.set(0, 0, 0);
      for (let c = 0; c < s; c++) {
        let u2;
        u2 = 2 * Math.PI * c / (s - 1), te[o] = Math.sin(u2), te[l2] = Math.cos(u2), i.push(te.x, te.y, te.z), u2 = 2 * Math.PI * (c + 1) / (s - 1), te[o] = Math.sin(u2), te[l2] = Math.cos(u2), i.push(te.x, te.y, te.z);
      }
    }
    n.setAttribute("position", new BufferAttribute(new Float32Array(i), 3)), n.computeBoundingSphere(), super(n, new LineBasicMaterial({ color: t, toneMapped: false })), this.sphere = e, this.type = "SphereHelper";
  }
  updateMatrixWorld(e) {
    const t = this.sphere;
    this.position.copy(t.center), this.scale.setScalar(t.radius), super.updateMatrixWorld(e);
  }
};
var Rt = new Vector3();
var Qe = new Vector3();
var ne = new Vector3();
function Ir(a, { computeNormals: e = false } = {}) {
  const {
    latStart: t = -Math.PI / 2,
    latEnd: s = Math.PI / 2,
    lonStart: n = 0,
    lonEnd: i = 2 * Math.PI,
    heightStart: r = 0,
    heightEnd: o = 0
  } = a, l2 = new BoxGeometry(1, 1, 1, 32, 32), { normal: c, position: u2 } = l2.attributes, h = u2.clone();
  for (let d = 0, m = u2.count; d < m; d++) {
    ne.fromBufferAttribute(u2, d);
    const f = MathUtils.mapLinear(ne.x, -0.5, 0.5, t, s), p = MathUtils.mapLinear(ne.y, -0.5, 0.5, n, i);
    let g = r;
    a.getCartographicToNormal(f, p, Rt), ne.z < 0 && (g = o), a.getCartographicToPosition(f, p, g, ne), u2.setXYZ(d, ...ne);
  }
  e && l2.computeVertexNormals();
  for (let d = 0, m = h.count; d < m; d++) {
    ne.fromBufferAttribute(h, d);
    const f = MathUtils.mapLinear(ne.x, -0.5, 0.5, t, s), p = MathUtils.mapLinear(ne.y, -0.5, 0.5, n, i);
    Rt.fromBufferAttribute(c, d), a.getCartographicToNormal(f, p, Qe), Math.abs(Rt.dot(Qe)) > 0.1 && (ne.z > 0 && Qe.multiplyScalar(-1), c.setXYZ(d, ...Qe));
  }
  return l2;
}
var Lr = class extends LineSegments {
  constructor(e = new $t(), t = 16776960) {
    super(), this.ellipsoidRegion = e, this.material.color.set(t), this.update();
  }
  update() {
    const e = Ir(this.ellipsoidRegion);
    this.geometry.dispose(), this.geometry = new EdgesGeometry(e, 80);
  }
  dispose() {
    this.geometry.dispose(), this.material.dispose();
  }
};
var Hs = /* @__PURE__ */ Symbol("ORIGINAL_MATERIAL");
var Dt = /* @__PURE__ */ Symbol("HAS_RANDOM_COLOR");
var Bt = /* @__PURE__ */ Symbol("HAS_RANDOM_NODE_COLOR");
var Ot = /* @__PURE__ */ Symbol("LOAD_TIME");
var ge = /* @__PURE__ */ Symbol("PARENT_BOUND_REF_COUNT");
var qs = new Sphere();
var Ze = () => {
};
var Ut = {};
function Je(a) {
  if (!Ut[a]) {
    const e = Math.random(), t = 0.5 + Math.random() * 0.5, s = 0.375 + Math.random() * 0.25;
    Ut[a] = new Color().setHSL(e, t, s);
  }
  return Ut[a];
}
var Re = 0;
var vn = 1;
var En = 2;
var wn = 3;
var Pn = 4;
var Rn = 5;
var Dn = 6;
var Ke = 7;
var et = 8;
var Bn = 9;
var Ft = 10;
var vr = Object.freeze({
  NONE: Re,
  SCREEN_ERROR: vn,
  GEOMETRIC_ERROR: En,
  DISTANCE: wn,
  DEPTH: Pn,
  RELATIVE_DEPTH: Rn,
  IS_LEAF: Dn,
  RANDOM_COLOR: Ke,
  RANDOM_NODE_COLOR: et,
  CUSTOM_COLOR: Bn,
  LOAD_ORDER: Ft
});
var Io = class {
  static get ColorModes() {
    return vr;
  }
  get unlit() {
    return this._unlit;
  }
  set unlit(e) {
    e !== this._unlit && (this._unlit = e, this.materialsNeedUpdate = true);
  }
  get colorMode() {
    return this._colorMode;
  }
  set colorMode(e) {
    e !== this._colorMode && (this._colorMode = e, this.materialsNeedUpdate = true);
  }
  get enabled() {
    return this._enabled;
  }
  set enabled(e) {
    e !== this._enabled && this.tiles !== null && (this._enabled = e, e ? this.init(this.tiles) : this.dispose());
  }
  get displayParentBounds() {
    return this._displayParentBounds;
  }
  set displayParentBounds(e) {
    this._displayParentBounds !== e && (this._displayParentBounds = e, e ? this.tiles.traverse((t) => {
      t.traversal.visible && this._onTileVisibilityChange(t, true);
    }) : this.tiles.traverse((t) => {
      t[ge] = null, this._onTileVisibilityChange(t, t.traversal.visible);
    }));
  }
  constructor(e) {
    e = {
      displayParentBounds: false,
      displayBoxBounds: false,
      displaySphereBounds: false,
      displayRegionBounds: false,
      colorMode: Re,
      maxDebugDepth: -1,
      maxDebugDistance: -1,
      maxDebugError: -1,
      customColorCallback: null,
      unlit: false,
      enabled: true,
      ...e
    }, this.name = "DEBUG_TILES_PLUGIN", this.tiles = null, this._colorMode = null, this._unlit = null, this.materialsNeedUpdate = false, this.extremeDebugDepth = -1, this.extremeDebugError = -1, this.boxGroup = null, this.sphereGroup = null, this.regionGroup = null, this._enabled = e.enabled, this._displayParentBounds = e.displayParentBounds, this.displayBoxBounds = e.displayBoxBounds, this.displaySphereBounds = e.displaySphereBounds, this.displayRegionBounds = e.displayRegionBounds, this.colorMode = e.colorMode, this.maxDebugDepth = e.maxDebugDepth, this.maxDebugDistance = e.maxDebugDistance, this.maxDebugError = e.maxDebugError, this.customColorCallback = e.customColorCallback, this.unlit = e.unlit, this.getDebugColor = (t, s) => {
      s.setRGB(t, t, t);
    };
  }
  // initialize the groups for displaying helpers, register events, and initialize existing tiles
  init(e) {
    if (this.tiles = e, !this.enabled)
      return;
    const t = e.group;
    this.boxGroup = new Group(), this.boxGroup.name = "DebugTilesRenderer.boxGroup", t.add(this.boxGroup), this.boxGroup.updateMatrixWorld(), this.sphereGroup = new Group(), this.sphereGroup.name = "DebugTilesRenderer.sphereGroup", t.add(this.sphereGroup), this.sphereGroup.updateMatrixWorld(), this.regionGroup = new Group(), this.regionGroup.name = "DebugTilesRenderer.regionGroup", t.add(this.regionGroup), this.regionGroup.updateMatrixWorld(), this._onLoadTilesetCB = () => {
      this._initExtremes();
    }, this._onLoadModelCB = ({ scene: s, tile: n }) => {
      this._onLoadModel(s, n);
    }, this._onDisposeModelCB = ({ tile: s }) => {
      this._onDisposeModel(s);
    }, this._onUpdateAfterCB = () => {
      this.update();
    }, this._onTileVisibilityChangeCB = ({ scene: s, tile: n, visible: i }) => {
      this._onTileVisibilityChange(n, i);
    }, e.addEventListener("load-tileset", this._onLoadTilesetCB), e.addEventListener("load-model", this._onLoadModelCB), e.addEventListener("dispose-model", this._onDisposeModelCB), e.addEventListener("update-after", this._onUpdateAfterCB), e.addEventListener("tile-visibility-change", this._onTileVisibilityChangeCB), this._initExtremes(), e.traverse((s) => {
      s.engineData.scene && this._onLoadModel(s.engineData.scene, s);
    }), e.visibleTiles.forEach((s) => {
      this._onTileVisibilityChange(s, true);
    });
  }
  getTileFromObject3D(e) {
    let t = null;
    return this.tiles.activeTiles.forEach((n) => {
      if (t)
        return true;
      const i = n.engineData.scene;
      i && i.traverse((r) => {
        r === e && (t = n);
      });
    }), t;
  }
  _initExtremes() {
    if (!(this.tiles && this.tiles.root))
      return;
    let e = -1, t = -1;
    this.tiles.traverse(null, (s, n, i) => {
      e = Math.max(e, i), t = Math.max(t, s.geometricError);
    }, false), this.extremeDebugDepth = e, this.extremeDebugError = t;
  }
  update() {
    const { tiles: e, colorMode: t } = this;
    if (!e.root)
      return;
    this.materialsNeedUpdate && (e.forEachLoadedModel((c) => {
      this._updateMaterial(c);
    }), this.materialsNeedUpdate = false), this.boxGroup.visible = this.displayBoxBounds, this.sphereGroup.visible = this.displaySphereBounds, this.regionGroup.visible = this.displayRegionBounds;
    let s = -1;
    this.maxDebugDepth === -1 ? s = this.extremeDebugDepth : s = this.maxDebugDepth;
    let n = -1;
    this.maxDebugError === -1 ? n = this.extremeDebugError : n = this.maxDebugError;
    let i = -1;
    this.maxDebugDistance === -1 ? (e.getBoundingSphere(qs), i = qs.radius) : i = this.maxDebugDistance;
    const { errorTarget: r, visibleTiles: o } = e;
    let l2;
    t === Ft && (l2 = Array.from(o).sort((c, u2) => c[Ot] - u2[Ot])), o.forEach((c) => {
      const u2 = c.engineData.scene;
      let h, d, m;
      t === Ke && (h = Math.random(), d = 0.5 + Math.random() * 0.5, m = 0.375 + Math.random() * 0.25), u2.traverse((f) => {
        if (t === et && (h = Math.random(), d = 0.5 + Math.random() * 0.5, m = 0.375 + Math.random() * 0.25), f.material)
          switch (t !== Ke && delete f.material[Dt], t !== et && delete f.material[Bt], t) {
            case Pn: {
              const p = c.internal.depth / s;
              this.getDebugColor(p, f.material.color);
              break;
            }
            case Rn: {
              const p = c.internal.depthFromRenderedParent / s;
              this.getDebugColor(p, f.material.color);
              break;
            }
            case vn: {
              const p = c.traversal.error / r;
              p > 1 ? f.material.color.setRGB(1, 0, 0) : this.getDebugColor(p, f.material.color);
              break;
            }
            case En: {
              const p = Math.min(c.geometricError / n, 1);
              this.getDebugColor(p, f.material.color);
              break;
            }
            case wn: {
              const p = Math.min(c.traversal.distanceFromCamera / i, 1);
              this.getDebugColor(p, f.material.color);
              break;
            }
            case Dn: {
              !c.children || c.children.length === 0 ? this.getDebugColor(1, f.material.color) : this.getDebugColor(0, f.material.color);
              break;
            }
            case et: {
              f.material[Bt] || (f.material.color.setHSL(h, d, m), f.material[Bt] = true);
              break;
            }
            case Ke: {
              f.material[Dt] || (f.material.color.setHSL(h, d, m), f.material[Dt] = true);
              break;
            }
            case Bn: {
              this.customColorCallback ? this.customColorCallback(c, f) : console.warn("DebugTilesRenderer: customColorCallback not defined");
              break;
            }
            case Ft: {
              const p = l2.indexOf(c);
              this.getDebugColor(p / (l2.length - 1), f.material.color);
              break;
            }
          }
      });
    });
  }
  _onTileVisibilityChange(e, t) {
    this.displayParentBounds ? u(e, (s) => {
      s[ge] == null && (s[ge] = 0), t ? s[ge]++ : s[ge] > 0 && s[ge]--;
      const n = s === e && t || this.displayParentBounds && s[ge] > 0;
      this._updateBoundHelper(s, n);
    }) : this._updateBoundHelper(e, t);
  }
  _createBoundHelper(e) {
    const t = this.tiles, s = e.engineData, { sphere: n, obb: i, region: r } = s.boundingVolume;
    if (i) {
      const o = new Group();
      o.name = "DebugTilesRenderer.boxHelperGroup", o.matrix.copy(i.transform), o.matrixAutoUpdate = false, s.boxHelperGroup = o;
      const l2 = new Box3Helper(i.box, Je(e.internal.depth));
      l2.raycast = Ze, o.add(l2);
      const c = new Mesh(new BoxGeometry(), new MeshBasicMaterial({
        color: Je(e.internal.depth),
        transparent: true,
        depthWrite: false,
        opacity: 0.05,
        side: DoubleSide
      }));
      i.box.getSize(c.scale), c.raycast = Ze, o.add(c), t.visibleTiles.has(e) && this.displayBoxBounds && (this.boxGroup.add(o), o.updateMatrixWorld(true));
    }
    if (n) {
      const o = new Ar(n, Je(e.internal.depth));
      o.raycast = Ze, s.sphereHelper = o, t.visibleTiles.has(e) && this.displaySphereBounds && (this.sphereGroup.add(o), o.updateMatrixWorld(true));
    }
    if (r) {
      const o = new Lr(r, Je(e.internal.depth));
      o.raycast = Ze;
      const l2 = new Sphere();
      r.getBoundingSphere(l2), o.position.copy(l2.center), l2.center.multiplyScalar(-1), o.geometry.translate(...l2.center), s.regionHelper = o, t.visibleTiles.has(e) && this.displayRegionBounds && (this.regionGroup.add(o), o.updateMatrixWorld(true));
    }
  }
  _updateHelperMaterials(e, t) {
    t.traverse((s) => {
      const { material: n } = s;
      if (!n)
        return;
      e.traversal.visible || !this.displayParentBounds ? n.opacity = s.isMesh ? 0.05 : 1 : n.opacity = s.isMesh ? 0.01 : 0.2;
      const i = n.transparent;
      n.transparent = n.opacity < 1, n.transparent !== i && (n.needsUpdate = true);
    });
  }
  _updateBoundHelper(e, t) {
    const s = e.engineData;
    if (!s)
      return;
    const n = this.sphereGroup, i = this.boxGroup, r = this.regionGroup;
    t && s.boxHelperGroup == null && s.sphereHelper == null && s.regionHelper == null && this._createBoundHelper(e);
    const o = s.boxHelperGroup, l2 = s.sphereHelper, c = s.regionHelper;
    t ? (o && (i.add(o), o.updateMatrixWorld(true), this._updateHelperMaterials(e, o)), l2 && (n.add(l2), l2.updateMatrixWorld(true), this._updateHelperMaterials(e, l2)), c && (r.add(c), c.updateMatrixWorld(true), this._updateHelperMaterials(e, c))) : (o && i.remove(o), l2 && n.remove(l2), c && r.remove(c));
  }
  _updateMaterial(e) {
    const { colorMode: t, unlit: s } = this;
    e.traverse((n) => {
      if (!n.material)
        return;
      const i = n.material, r = n[Hs];
      if (i !== r && i.dispose(), t !== Re || s) {
        if (n.isPoints) {
          const o = new PointsMaterial();
          o.size = r.size, o.sizeAttenuation = r.sizeAttenuation, n.material = o;
        } else s ? n.material = new MeshBasicMaterial() : (n.material = new MeshStandardMaterial(), n.material.flatShading = true);
        t === Re && (n.material.map = r.map, n.material.color.set(r.color));
      } else
        n.material = r;
    });
  }
  _onLoadModel(e, t) {
    t[Ot] = performance.now(), e.traverse((s) => {
      const n = s.material;
      n && (s[Hs] = n);
    }), this._updateMaterial(e);
  }
  _onDisposeModel(e) {
    const t = e.engineData;
    t != null && t.boxHelperGroup && (t.boxHelperGroup.children[0].geometry.dispose(), delete t.boxHelperGroup), t != null && t.sphereHelper && (t.sphereHelper.geometry.dispose(), delete t.sphereHelper), t != null && t.regionHelper && (t.regionHelper.geometry.dispose(), delete t.regionHelper);
  }
  dispose() {
    var t, s, n;
    const e = this.tiles;
    e.removeEventListener("load-tileset", this._onLoadTilesetCB), e.removeEventListener("load-model", this._onLoadModelCB), e.removeEventListener("dispose-model", this._onDisposeModelCB), e.removeEventListener("update-after", this._onUpdateAfterCB), e.removeEventListener("tile-visibility-change", this._onTileVisibilityChangeCB), this.colorMode = Re, this.unlit = false, e.forEachLoadedModel((i) => {
      this._updateMaterial(i);
    }), e.traverse((i) => {
      this._onDisposeModel(i);
    }, null, false), (t = this.boxGroup) == null || t.removeFromParent(), (s = this.sphereGroup) == null || s.removeFromParent(), (n = this.regionGroup) == null || n.removeFromParent();
  }
};
var Er = class extends Ue {
  constructor(e = {}) {
    const { url: t = null, ...s } = e;
    super(s), this.url = t, this.format = null, this.stem = null;
  }
  getUrl(e, t, s) {
    return `${this.stem}_files/${s}/${e}_${t}.${this.format}`;
  }
  init() {
    const { url: e } = this;
    return this.fetchData(e, this.fetchOptions).then((t) => t.text()).then((t) => {
      const s = new DOMParser().parseFromString(t, "text/xml");
      if (s.querySelector("DisplayRects") || s.querySelector("Collection"))
        throw new Error("DeepZoomImagesPlugin: DisplayRect and Collection DZI files not supported.");
      const n = s.querySelector("Image"), i = n.querySelector("Size"), r = parseInt(i.getAttribute("Width")), o = parseInt(i.getAttribute("Height")), l2 = parseInt(n.getAttribute("TileSize")), c = parseInt(n.getAttribute("Overlap")), u2 = n.getAttribute("Format");
      this.format = u2, this.stem = e.split(/\.[^.]+$/g)[0];
      const { tiling: h } = this, d = Math.ceil(Math.log2(Math.max(r, o))) + 1;
      h.flipY = true, h.pixelOverlap = c, h.generateLevels(d, 1, 1, {
        tilePixelWidth: l2,
        tilePixelHeight: l2,
        pixelWidth: r,
        pixelHeight: o
      });
    });
  }
};
var Lo = class extends dn {
  constructor(e = {}) {
    const { url: t, ...s } = e;
    super(s), this.name = "DZI_TILES_PLUGIN", this.imageSource = new Er({ url: t });
  }
};
var rt2 = N * Math.PI * 2;
var Ws = new re("EPSG:3857");
function wr(a) {
  return /:4326$/i.test(a);
}
function On(a) {
  return /:3857$/i.test(a);
}
function kt(a) {
  return a.trim().split(/\s+/).map((e) => parseFloat(e));
}
function Gt(a, e) {
  wr(e) && ([a[1], a[0]] = [a[0], a[1]]);
}
function ot(a, e) {
  if (On(e))
    return a[0] = Ws.convertNormalizedToLongitude(0.5 + a[0] / rt2), a[1] = Ws.convertNormalizedToLatitude(0.5 + a[1] / rt2), a[0] *= MathUtils.RAD2DEG, a[1] *= MathUtils.RAD2DEG, a;
}
function at2(a) {
  a[0] *= MathUtils.DEG2RAD, a[1] *= MathUtils.DEG2RAD;
}
var vo = class extends y {
  parse(e) {
    const t = new TextDecoder("utf-8").decode(new Uint8Array(e)), s = new DOMParser().parseFromString(t, "text/xml"), n = s.querySelector("Contents"), i = ue(n, "TileMatrixSet").map((l2) => Ur(l2)), r = ue(n, "Layer").map((l2) => Rr(l2)), o = Pr(s.querySelector("ServiceIdentification"));
    return r.forEach((l2) => {
      l2.tileMatrixSets = l2.tileMatrixSetLinks.map((c) => i.find((u2) => u2.identifier === c));
    }), {
      serviceIdentification: o,
      tileMatrixSets: i,
      layers: r
    };
  }
};
function Pr(a) {
  var i;
  const e = a.querySelector("Title").textContent, t = ((i = a.querySelector("Abstract")) == null ? void 0 : i.textContent) || "", s = a.querySelector("ServiceType").textContent, n = a.querySelector("ServiceTypeVersion").textContent;
  return {
    title: e,
    abstract: t,
    serviceType: s,
    serviceTypeVersion: n
  };
}
function Rr(a) {
  const e = a.querySelector("Title").textContent, t = a.querySelector("Identifier").textContent, s = a.querySelector("Format").textContent, n = ue(a, "ResourceURL").map((c) => Dr(c)), i = ue(a, "TileMatrixSetLink").map((c) => ue(c, "TileMatrixSet")[0].textContent), r = ue(a, "Style").map((c) => Or(c)), o = ue(a, "Dimension").map((c) => Br(c));
  let l2 = js(a.querySelector("WGS84BoundingBox"));
  return l2 || (l2 = js(a.querySelector("BoundingBox"))), {
    title: e,
    identifier: t,
    format: s,
    dimensions: o,
    tileMatrixSetLinks: i,
    styles: r,
    boundingBox: l2,
    resourceUrls: n
  };
}
function Dr(a) {
  const e = a.getAttribute("template"), t = a.getAttribute("format"), s = a.getAttribute("resourceType");
  return {
    template: e,
    format: t,
    resourceType: s
  };
}
function Br(a) {
  var r, o;
  const e = a.querySelector("Identifier").textContent, t = ((r = a.querySelector("UOM")) == null ? void 0 : r.textContent) || "", s = a.querySelector("Default").textContent, n = ((o = a.querySelector("Current")) == null ? void 0 : o.textContent) === "true", i = ue(a, "Value").map((l2) => l2.textContent);
  return {
    identifier: e,
    uom: t,
    defaultValue: s,
    current: n,
    values: i
  };
}
function js(a) {
  if (!a)
    return null;
  const e = a.nodeName.endsWith("WGS84BoundingBox") ? "urn:ogc:def:crs:CRS::84" : a.getAttribute("crs"), t = kt(a.querySelector("LowerCorner").textContent), s = kt(a.querySelector("UpperCorner").textContent);
  return Gt(t, e), Gt(s, e), ot(t, e), ot(s, e), at2(t), at2(s), {
    crs: e,
    lowerCorner: t,
    upperCorner: s,
    bounds: [...t, ...s]
  };
}
function Or(a) {
  var n;
  const e = ((n = a.querySelector("Title")) == null ? void 0 : n.textContent) || null, t = a.querySelector("Identifier").textContent, s = a.getAttribute("isDefault") === "true";
  return {
    title: e,
    identifier: t,
    isDefault: s
  };
}
function Ur(a) {
  var r, o;
  const e = a.querySelector("SupportedCRS").textContent, t = ((r = a.querySelector("Title")) == null ? void 0 : r.textContent) || "", s = a.querySelector("Identifier").textContent, n = ((o = a.querySelector("Abstract")) == null ? void 0 : o.textContent) || "", i = [];
  return a.querySelectorAll("TileMatrix").forEach((l2, c) => {
    const u2 = Nr(l2), h = 28e-5 * u2.scaleDenominator, d = u2.tileWidth * u2.matrixWidth * h, m = u2.tileHeight * u2.matrixHeight * h;
    let f;
    Gt(u2.topLeftCorner, e), On(e) ? f = [
      u2.topLeftCorner[0] + d,
      u2.topLeftCorner[1] - m
    ] : f = [
      u2.topLeftCorner[0] + 360 * d / rt2,
      u2.topLeftCorner[1] - 360 * m / rt2
    ], ot(f, e), ot(u2.topLeftCorner, e), at2(f), at2(u2.topLeftCorner), u2.bounds = [...u2.topLeftCorner, ...f], [u2.bounds[1], u2.bounds[3]] = [u2.bounds[3], u2.bounds[1]], i.push(u2);
  }), {
    title: t,
    identifier: s,
    abstract: n,
    supportedCRS: e,
    tileMatrices: i
  };
}
function Nr(a) {
  const e = a.querySelector("Identifier").textContent, t = parseFloat(a.querySelector("TileWidth").textContent), s = parseFloat(a.querySelector("TileHeight").textContent), n = parseFloat(a.querySelector("MatrixWidth").textContent), i = parseFloat(a.querySelector("MatrixHeight").textContent), r = parseFloat(a.querySelector("ScaleDenominator").textContent), o = kt(a.querySelector("TopLeftCorner").textContent);
  return {
    identifier: e,
    tileWidth: t,
    tileHeight: s,
    matrixWidth: n,
    matrixHeight: i,
    scaleDenominator: r,
    topLeftCorner: o,
    bounds: null
  };
}
function ue(a, e) {
  return [...a.children].filter((t) => t.tagName === e);
}
var Xs = N * Math.PI * 2;
var Ys = new re("EPSG:3857");
function Vr(a) {
  return /:4326$/i.test(a);
}
function Fr(a) {
  return /:3857$/i.test(a);
}
function $s(a, e) {
  return Fr(e) && (a[0] = Ys.convertNormalizedToLongitude(0.5 + a[0] / (Math.PI * 2 * Xs)), a[1] = Ys.convertNormalizedToLatitude(0.5 + a[1] / (Math.PI * 2 * Xs)), a[0] *= MathUtils.RAD2DEG, a[1] *= MathUtils.RAD2DEG), a;
}
function Qs(a, e, t) {
  const [s, n] = t.split(".").map((r) => parseInt(r)), i = s === 1 && n < 3 || s < 1;
  Vr(e) && i && ([a[0], a[1]] = [a[1], a[0]]);
}
function Me(a) {
  a[0] *= MathUtils.DEG2RAD, a[1] *= MathUtils.DEG2RAD;
}
function kr(a, e) {
  if (!a)
    return null;
  const t = a.getAttribute("CRS") || a.getAttribute("crs") || a.getAttribute("SRS") || "", s = parseFloat(a.getAttribute("minx")), n = parseFloat(a.getAttribute("miny")), i = parseFloat(a.getAttribute("maxx")), r = parseFloat(a.getAttribute("maxy")), o = [s, n], l2 = [i, r];
  return Qs(o, t, e), Qs(l2, t, e), $s(o, t), $s(l2, t), Me(o), Me(l2), { crs: t, bounds: [...o, ...l2] };
}
function Gr(a) {
  const e = parseFloat(a.querySelector("westBoundLongitude").textContent), t = parseFloat(a.querySelector("eastBoundLongitude").textContent), s = parseFloat(a.querySelector("southBoundLatitude").textContent), n = parseFloat(a.querySelector("northBoundLatitude").textContent), i = [e, s], r = [t, n];
  return Me(i), Me(r), [...i, ...r];
}
function zr(a) {
  const e = parseFloat(a.getAttribute("minx").textContent), t = parseFloat(a.getAttribute("maxx").textContent), s = parseFloat(a.getAttribute("miny").textContent), n = parseFloat(a.getAttribute("maxy").textContent), i = [e, s], r = [t, n];
  return Me(i), Me(r), [...i, ...r];
}
function Hr(a) {
  const e = a.querySelector("Name").textContent, t = a.querySelector("Title").textContent, s = [...a.querySelectorAll("LegendURL")].map((n) => {
    const i = parseInt(n.getAttribute("width")), r = parseInt(n.getAttribute("height")), o = n.querySelector("Format").textContent, l2 = n.querySelector("OnlineResource"), c = zt(l2);
    return {
      width: i,
      height: r,
      format: o,
      url: c
    };
  });
  return {
    name: e,
    title: t,
    legends: s
  };
}
function Un(a, e, t = {}) {
  var p, g, y2;
  let {
    styles: s = [],
    crs: n = [],
    contentBoundingBox: i = null,
    queryable: r = false,
    opaque: o = false
  } = t;
  const l2 = ((p = a.querySelector(":scope > Name")) == null ? void 0 : p.textContent) || null, c = ((g = a.querySelector(":scope > Title")) == null ? void 0 : g.textContent) || "", u2 = ((y2 = a.querySelector(":scope > Abstract")) == null ? void 0 : y2.textContent) || "", h = [...a.querySelectorAll(":scope > Keyword")].map((x2) => x2.textContent), m = [...a.querySelectorAll(":scope > BoundingBox")].map((x2) => kr(x2, e));
  n = [
    ...n,
    ...Array.from(a.querySelectorAll("CRS")).map((x2) => x2.textContent)
  ], s = [
    ...s,
    ...Array.from(a.querySelectorAll(":scope > Style")).map((x2) => Hr(x2))
  ], a.hasAttribute("queryable") && (r = a.getAttribute("queryable") === "1"), a.hasAttribute("opaque") && (o = a.getAttribute("opaque") === "1"), a.querySelector("EX_GeographicBoundingBox") ? i = Gr(a.querySelector("EX_GeographicBoundingBox")) : a.querySelector("LatLonBoundingBox") && (i = zr(a.querySelector("LatLonBoundingBox")));
  const f = Array.from(a.querySelectorAll(":scope > Layer")).map((x2) => Un(x2, e, {
    // add
    styles: s,
    crs: n,
    // replace
    contentBoundingBox: i,
    queryable: r,
    opaque: o
  }));
  return {
    name: l2,
    title: c,
    abstract: u2,
    queryable: r,
    opaque: o,
    keywords: h,
    crs: n,
    boundingBoxes: m,
    contentBoundingBox: i,
    styles: s,
    subLayers: f
  };
}
function qr(a) {
  var e, t, s;
  return {
    name: ((e = a.querySelector("Name")) == null ? void 0 : e.textContent) || "",
    title: ((t = a.querySelector("Title")) == null ? void 0 : t.textContent) || "",
    abstract: ((s = a.querySelector("Abstract")) == null ? void 0 : s.textContent) || "",
    keywords: Array.from(a.querySelectorAll("Keyword")).map((n) => n.textContent),
    maxWidth: parseFloat(a.querySelector("MaxWidth")) || null,
    maxHeight: parseFloat(a.querySelector("MaxHeight")) || null,
    layerLimit: parseFloat(a.querySelector("LayerLimit")) || null
  };
}
function zt(a) {
  return a ? (a.getAttribute("xlink:href") || a.getAttributeNS("http://www.w3.org/1999/xlink", "href") || "").trim() : "";
}
function Wr(a) {
  const e = Array.from(a.querySelectorAll("Format")).map((s) => s.textContent.trim()), t = Array.from(a.querySelectorAll("DCPType")).map((s) => {
    const n = s.querySelector("HTTP"), i = n.querySelector("Get OnlineResource") || n.querySelector("Get > OnlineResource") || n.querySelector("Get"), r = n.querySelector("Post OnlineResource") || n.querySelector("Post > OnlineResource") || n.querySelector("Post"), o = zt(i), l2 = zt(r);
    return { type: "HTTP", get: o, post: l2 };
  });
  return { formats: e, dcp: t, href: t[0].get };
}
function jr(a) {
  const e = {};
  return Array.from(a.querySelectorAll(":scope > *")).forEach((t) => {
    const s = t.localName;
    e[s] = Wr(t);
  }), e;
}
function Nn(a, e = []) {
  return a.forEach((t) => {
    t.name !== null && e.push(t), Nn(t.subLayers, e);
  }), e;
}
var Eo = class extends y {
  parse(e) {
    const t = new TextDecoder("utf-8").decode(new Uint8Array(e)), s = new DOMParser().parseFromString(t, "text/xml"), i = (s.querySelector("WMS_Capabilities") || s.querySelector("WMT_MS_Capabilities")).getAttribute("version"), r = s.querySelector("Capability"), o = qr(s.querySelector(":scope > Service")), l2 = jr(r.querySelector(":scope > Request")), c = Array.from(r.querySelectorAll(":scope > Layer")).map((h) => Un(h, i)), u2 = Nn(c);
    return { version: i, service: o, layers: u2, request: l2 };
  }
};

// node_modules/3d-tiles-renderer/build/index.plugins.js
var l = class extends no {
  constructor(e) {
    super({
      ...e,
      __suppress_warning__: true
    });
  }
};
export {
  Qt as BaseRegion,
  ho as BatchedTilesPlugin,
  it as CesiumIonAuth,
  l as CesiumIonAuthPlugin,
  bo as CesiumIonOverlay,
  Io as DebugTilesPlugin,
  Lo as DeepZoomImagePlugin,
  x as EnforceNonZeroErrorPlugin,
  sr as GLTFCesiumRTCExtension,
  ao as GLTFExtensionsPlugin,
  tr as GLTFMeshFeaturesExtension,
  Ji as GLTFStructuralMetadataExtension,
  go as GeoJSONOverlay,
  at as GoogleCloudAuth,
  rt as GoogleCloudAuthPlugin,
  _o as GoogleMapsOverlay,
  fo as ImageOverlayPlugin,
  w as ImplicitTilingPlugin,
  So as LoadRegionPlugin,
  Ao as OBBRegion,
  ct as QuantizedMeshLoaderBase,
  Oi as QuantizedMeshPlugin,
  Co as RayRegion,
  lo as ReorientationPlugin,
  Mo as SphereRegion,
  To as TMSTilesOverlay,
  Ci as TMSTilesPlugin,
  oo as TileCompressionPlugin,
  po as TileFlatteningPlugin,
  uo as TilesFadePlugin,
  co as UnloadTilesPlugin,
  ro as UpdateOnChangePlugin,
  Eo as WMSCapabilitiesLoader,
  yo as WMSTilesOverlay,
  so as WMSTilesPlugin,
  vo as WMTSCapabilitiesLoader,
  xo as WMTSTilesOverlay,
  to as WMTSTilesPlugin,
  mo as XYZTilesOverlay,
  eo as XYZTilesPlugin,
  v as zigZagDecode
};
//# sourceMappingURL=3d-tiles-renderer_plugins.js.map
